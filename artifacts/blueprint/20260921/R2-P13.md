# P13 — revision 2 execution trace
Status: planning-only. Read16–26; source edits require execution go.
Depends on: P01, P07; amendments in24.
## Exact allowed files (repository-relative)
- `ios/Shared/TaxDocuments.swift` (existing)
- `ios/LifeOS/TaxDocumentsView.swift` (existing)
- `ios/Sync/TaxSyncAdapter.swift` (new)
- `ios/LifeOSMacSnapshotTests/TaxRetentionTests.swift` (new)
## Existing anchors and ordered trace
TaxDocumentStore.save, TaxDocument pages, parser identifierCandidate/candidate(for:), TaxCSVExporter.escape.
1. Separate protected raw extraction cache from publication fields; migration19 M6.
2. Explicit sanitized DTO allowlist; no encodable TaxDocument forwarded as generic payload.
3. Mask identifier/reference together; no raw match line in publication or logs.
4. Retain raw cache for pending review/conflict; purge only eligible accepted extraction after30days.
5. Compact list/detail; explicit scoped preview/export, no hidden remote URL opening.
## Proposed signatures
struct TaxPublicationRecord: Codable, Sendable { let id:UUID; let revision:String; let kind:String;
let taxYear:Int?; let maskedReference:String?; let amountMinorUnits:Int64?; let currency:String? }.
enum TaxPublicationError:Error,Sendable { case invalidField, sensitiveContent, unsupportedSchema }.
TaxPublicationRecord.make(from:TaxDocument) throws -> TaxPublicationRecord.
TaxDocumentStore.purgeExtractionCache(now:Date) throws -> Int; protected raw cache reference stays local.
actor TaxSyncAdapter:SyncDomainAdapter; applyRemote never creates raw extraction pages.
CP-B binds document identity/current money wrappers; no lossy UUID/int conversion if original differs.
## State / security
Unsupported new document field retained locally; excluded publication by default. Validation failure preserves source.
Duplicate migration validates cache hash; interrupted write recovers backup. Deleted document tombstone prevents resurrection.
CSV leading =,+,-,@,tab,CR neutralized after leading-space/control normalization, string escaping also retained.
Never parse unlimited regex input; existing parser caps retained; no silent truncation of accepted financial amount.
## Complexity / evidence
ParseO(bounded bytes) target; regex pathological case explicitly tested; cache scan bounded page, no startup full PDF decode.
W2 extraction/export/privacy cases; W3 encode-path raw-field sentinel check, migration crash/restart and deletion replay.
P02/P16 consume only TaxPublicationRecord; raw OCR/PDF never sync blob/widget/notification.

## Dispatch rule
Resolve named signature/checkpoint before changing its caller or persistence boundary.
Inputs/outputs not fully specified here inherit the named existing API unchanged, never a guessed replacement.
No implementation, builds, tests, commits or pushes in this phase.
