# P12 — revision 2 execution trace
Status: planning-only. Read16–26; source edits require execution go.
Depends on: P07; amendments in24.
## Exact allowed files (repository-relative)
- `ios/Shared/UsageProviderRegistry.swift` (existing)
- `ios/Shared/UsageRegistryAdapter.swift` (existing)
- `ios/Shared/UsageRegistryPreferences.swift` (existing)
- `ios/Shared/UsageManualReadingStore.swift` (existing)
- `ios/Shared/UsageConnectionActions.swift` (existing)
- `ios/Shared/UsageCoordinator.swift` (existing)
- `ios/LifeOS/CodexView.swift` (existing)
- `ios/LifeOS/Usage/UsageConnectionsView.swift` (existing)
- `ios/LifeOS/Usage/UsageRegistryDetailView.swift` (existing)
- `ios/LifeOS/Usage/UsageProjectionChart.swift` (existing)
- `ios/LifeOS/Usage/UsageManualReadingView.swift` (existing)
- `ios/LifeOS/Usage/UsageFactsView.swift` (existing)
- `ios/LifeOSMacSnapshotTests/UsageCompletionTests.swift` (new)
## Existing anchors and ordered trace
UsageProviderRegistry/UsageRegistryAdapter/UsageRegistryPreferences/UsageManualReadingStore;
UsageConnectionActions and UsageCoordinator.refresh; existing usage views.
1. Extend registry descriptor capability metadata, not executable configuration.
2. Refresh routes compile-time adapterID allowlist; manual products offer Edit, not pretend refresh.
3. Pin/hide/reorder preferences local per device; retain Claude even hidden; unknown provider manual only.
4. Replace oversized ring with compact meter/reset/chart/facts; one range state across detail tabs.
## Proposed signatures
struct UsageAdapterDescriptor: Codable, Sendable { let adapterID:String; let product:UsageProductKind;
let authKind:UsageAuthKind; let capabilities:Set<UsageCapability>; let automaticRefresh:Bool }.
UsageRegistryAdapter.descriptor(for providerID:UsageProviderID)->UsageAdapterDescriptor?.
@MainActor UsageCoordinator.refresh(connectionID:UsageConnectionID) async; cancel(connectionID:UsageConnectionID).
Existing UsageManualReading validation/store remains authority; no new quota table or credential cache in view.
CP-F binds actual method overload to current registry snapshot type before worker alters public API.
## Behavior / migration
Manual enteredAt/window/reset timezone retained; stale after declared window reset, never promoted to live.
Gemini subscription manual distinct from API metering; no token-count→remaining-quota inference.
Unknown old provider enum decode preserves bounded manual metadata or reports unsupported, never executes command.
Same selected provider tap no-op; cancelled/stale generation response cannot replace active connection view.
Missing connection empty action; unauthorized actionable state; no fabricated history/banked resets.
## Complexity / evidence
Descriptor lookup expectedO(1), sorting preferencesO(providers log providers) on change only;
chart precomputeO(points), scrubO(log points), no recalculation per pointer frame.
W2 manual Gemini resets/entry edits, retained Claude, actual Codex, provider hide/order, expired collector state.
Collector auth stays host; no browser cookies or arbitrary shell. Optional automatic endpoint requires new reviewed adapter.

## Dispatch rule
Resolve named signature/checkpoint before changing its caller or persistence boundary.
Inputs/outputs not fully specified here inherit the named existing API unchanged, never a guessed replacement.
No implementation, builds, tests, commits or pushes in this phase.
