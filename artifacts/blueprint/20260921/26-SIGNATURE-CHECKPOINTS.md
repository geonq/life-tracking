# Explicit remaining architecture checkpoints
These are planner-owned tasks before dependent coding, not Luna implementation choices.
No plan can enumerate every future local variable; all public boundaries and irreversible behavior must be sealed.

## CP-A — independent Astra Medium review
Unavailable callable worker in current tool inventory. Required output: ACCEPT/REQUEST CHANGES by packet.
Review actual revision2 docs and source anchors, not just prose summary; record actual model/effort provenance.
Do not treat current GPT-6 controller as a spawned Astra Medium worker.

## CP-B — protocol and existing store bridge seal
P01 SyncOperation/SyncPage/SyncAck/SyncCheckpointReceipt and Python/TS equivalents need full field declarations.
Specify exact signed operation framing after storeID addition; canonical encoding, unknown field policy and vectors.
Vectors: empty body, Unicode entity ID, sequence2^53+1, reordered parents, epoch mismatch, duplicate key, altered hash.
Define bootstrap owner key, server key protection, epoch rotation and pre-rotation unsigned operation disposition.
Define replica ack trust: only signed origin applies receipts; relay cannot forge an applying-device ACK.
Map each existing store envelope coding key→new optional replication field→validation/default/migration.
Per-store payload must name exact current entity DTO, current mutation API and same-file atomic persistence seam.
Do not serialize arbitrary Any/Dictionary payloads or guess existing private helper signatures.
Allowed result: signed schema annex + exact adapters + reviewed additive coding map in <=200-line pieces.
Until then transport/store refactor packets cannot be dispatched as "no guessing".

## CP-C — SDK seal
P00 reports current host/selected SDK; planner reads exact installed declaration for each adopted new member.
P16 records two layers of guard, SDK flag, minimum runtime and fallback from17.
Resolve current SWIFT_VERSION5.9 acceptance and inherited strict-concurrency warnings without broad suppression.
Verify SF Symbol availability before new glyph; fallback is predefined in20.
No OS install/build/test in planning. Later branch compile is execution evidence.

## CP-D — optional Document API change
Not required under current decision. If reopened, name one writer, one UndoManager authority,
base-version conflict interception and lossless journal migration; never parallel DocumentGroup autosave.

## CP-E — package/tool seal
Verify exact cryptography wheel/lock hashes for real platforms, existing lock generation policy, MIT references,
CI action commit SHAs and XcodeGen checksum. Record provenance; don't choose "latest" during implementation.

## CP-F — reference and full-product leaf seal
P00 expands all source reference requirements into individual IDs, not merely22 family rows.
Inspect existing nutrition photo/barcode/recipe/recent pipeline and Clipper collector actual contracts.
Assign missing gateway image sanitation/provider response/consent/budget paths to P02 and app proposal adapters to P16/P10.
Name exact symbols/file ownership and error schemas before those new paths are implemented.
Carry original fitness retention/notification/widget and photo-quality requirements into evidence ledger.
Calorie AI paid provider credentials are not automatically authorized by the desire for photo estimation;
reuse existing configured authorized provider or surface unavailable/manual path; no purchases/new billing assumed.
Existing referenced features cannot be removed as "optional" by a worker to close a completion gate.

## Why these remain explicit
This revision inspected critical symbols and first-pass artifacts, not every function body or every reference image.
Reporting sealed signatures for unseen/undefined contracts would reproduce the earlier false certainty.
All checkpoints can be resolved by architecture work except actual SDK availability and five U categories in22.
