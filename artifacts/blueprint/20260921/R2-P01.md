# P01 — revision 2 execution trace
Status: planning-only. Read16–26; source edits require execution go.
Depends on: P00; amendments in24.
## Exact allowed files (repository-relative)
- `ios/Shared/SyncContract.swift` (new)
- `ios/Sync/SyncWireCodec.swift` (new)
- `ios/Sync/SyncIdentityStore.swift` (new)
- `ios/Sync/SyncTransport.swift` (new)
- `ios/Sync/SyncEngine.swift` (new)
- `ios/Sync/SyncDomainAdapter.swift` (new)
- `packages/contracts/src/replication.ts` (new)
- `packages/contracts/src/replication.test.ts` (new)
- `ios/LifeOSMacSnapshotTests/SyncProtocolTests.swift` (new)
## Existing anchors and ordered trace
All owned source paths are new. Existing domain stores are read-only to this packet.
1. Seal CP-B byte/schema/types first; implement validated Swift DTOs in SyncContract.swift.
2. Wire codec uses fixed length-prefixed bytes and CryptoKit signing; TS validator consumes same vectors.
3. Keychain private-key store serializes identity creation; never regenerate identity on temporary read failure.
4. Transport URLSession delegate blocks redirects, bounds bytes during receive, validates MIME/status/signature.
5. Engine enumerates adapter pages, sends, applies and advances frontiers only from durable receipts.
## Proposed functions and isolation
actor SyncIdentityStore: loadOrCreateKey() throws -> Data; revoke(deviceID:UUID,epoch:UInt64) throws.
Private Data key never exposed to views; public callers use sign(_ bytes:Data) throws -> Data instead.
SyncWireCodec.signingBytes(for:SyncOperation) throws -> Data; decodeOperation(_ bytes:Data) throws -> SyncOperation.
actor SyncTransport: exchange(_ request:Data,endpoint:SyncEndpoint) async throws -> Data.
actor SyncEngine: synchronizeOnce(reason:SyncReason) async throws -> SyncCycleReport; stop() async; resume() async.
TS parseSyncOperation(input:unknown):SyncOperationWire; operationSigningBytes(op:SyncOperationWire):Uint8Array.
Pure TS codec never executes provider metadata. CP-B defines every DTO before dispatch; no any-casts.
## Inputs, outcomes, failures
Input cap1MiB/page128/operation64KiB; larger payload is blob reference, not arbitrary embedded file.
SyncFailure errors in18; offline/retry state stays engine-owned; auth/hash/schema failures not auto-retried.
Duplicate mutation same hash idempotent; changed hash rejects; stale generation suppresses future work.
Empty adapters/page return zero-count report; cancellation after durable apply leaves receipt pending ACK.
## Transactions / complexity / evidence
No universal store; pending entries live in existing domain envelope; no network while domain lock held.
Wire O(bytes+parents log parents), memoryO(pageBytes+devices), engine O(new operations) excluding store I/O.
W1 vectors Swift↔TS↔Python, rejected replay/tamper, cancellation, gap frontier, revoked key and key unavailable.
CP-B blocks undefined envelope/rotation/cursor types; P16 composes endpoints and target membership.

## Dispatch rule
Resolve named signature/checkpoint before changing its caller or persistence boundary.
Inputs/outputs not fully specified here inherit the named existing API unchanged, never a guessed replacement.
No implementation, builds, tests, commits or pushes in this phase.
