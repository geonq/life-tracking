# P02 — revision 2 execution trace
Status: planning-only. Read16–26; source edits require execution go.
Depends on: P01; amendments in24.
## Exact allowed files (repository-relative)
- `services/gateway/replication.py` (new)
- `services/gateway/test_replication.py` (new)
- `services/gateway/main.py` (existing)
- `services/gateway/requirements.txt` (existing)
- `services/gateway/requirements.lock` (existing)
- `services/mac-relay/main.py` (new)
- `services/mac-relay/install.sh` (new)
- `services/mac-relay/README.md` (new)
- `services/mac-relay/test_relay.py` (new)
## Existing anchors and ordered trace
Existing main.py require_tailscale_identity/_request_has_allowed_tailscale_identity and protected storage gate stay.
1. Implement pure replication.py; it must not import Windows ctypes/config/key-loading module.
2. Create migrations/user_version transaction for operations,streams,acks,membership,blobs; preserve old DB.
3. Bind validate_signed_request before every route; recheck epoch under serialized writer immediately before commit.
4. Register Windows routes through existing gate; Mac independently verifies signatures on loopback requests too.
5. Install recipe snapshots existing Serve config; merge only owned route; install user LaunchAgent after execution approval.
## Proposed Python interfaces
class ReplicationStore: append(self,request:VerifiedExchange)->ExchangeResult;
read_page(self,dataset_id:str,stream_id:str,after:int,limit:int)->OperationPage;
ack(self,receipt:VerifiedAck)->None; checkpoint(self,request:CheckpointRequest)->CheckpointReceipt.
def validate_signed_request(method:str,path:str,body:bytes,trust:TrustSnapshot)->VerifiedExchange.
async def exchange(request:Request)->Response; async def health(request:Request)->Response.
All typed wire records are sealed CP-B dataclasses; SQLite work through existing bounded worker executor.
Errors ReplicationError(code:SyncFailure-equivalent,retry_after_seconds:int|None); no raw payload response/log.
## State / transactions
SQLite BEGIN IMMEDIATE→row validation/insert→COMMIT→signed stored response. Details19 M5.
One writer,4 bounded requests, bounded semaphore before reading body; no unbounded task queue.
Identical duplicate returns stored receipt; schema/auth mismatch rejects; unsupported domain stored only per policy,
never applied ACK. Missing blobs block reference admission; incomplete temp uploads are not committed content.
CP-B defines signed blob reference/lease/nonce replay storage; do not invent unauthenticated blob download.
Mac offline iPhone retains pending; server restart reads DB, no in-memory-only ACK.
## Complexity / acceptance
Insert O(log rows), page O(log rows+page), memoryO(pageBytes), blob hashingO(bytes).
W1 local forged/oversized/duplicate/disk-full/restart cases; W3 real enrolled Mac relay roundtrip;
P17 later Windows ACL/service. CP-E pins cryptography before package install; P13 supplies sanitized tax DTO.

## Dispatch rule
Resolve named signature/checkpoint before changing its caller or persistence boundary.
Inputs/outputs not fully specified here inherit the named existing API unchanged, never a guessed replacement.
No implementation, builds, tests, commits or pushes in this phase.
