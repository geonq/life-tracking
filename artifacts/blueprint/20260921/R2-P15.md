# P15 — revision 2 execution trace
Status: planning-only. Read16–26; source edits require execution go.
Depends on: P02, P08, P09, P10, P12, P13; amendments in24.
## Exact allowed files (repository-relative)
- `ios/Shared/DemoFixtures.swift` (existing)
- `services/api/src/server.ts` (existing)
- `services/api/src/history.ts` (existing)
- `services/api/src/codex-adapter.ts` (existing)
- `services/api/src/claude-ingest.ts` (existing)
- `apps/dashboard/index.html` (existing)
- `.github/workflows/native-apple.yml` (existing)
- `package-lock.json` (existing)
- `artifacts/final/completion/dead-code-manifest.md` (new)
- `artifacts/final/completion/security-findings.md` (new)
## Existing anchors and ordered trace
server.ts Host admission/nosniff; history.ts locking; codex-adapter executable resolution;
claude-ingest constantTimeEqual; Dashboard index; native-apple.yml and package-lock.
1. Map all original Claude findings to actual current symbols/evidence; never assume old report still unfixed.
2. Recheck auth/replay/path/CSV/raw tax/regex/races; route cross-owner fixes to P03/P13/P17.
3. Enumerate dead code with target/string/intents references; delete only replacement-owner approved entries.
4. Scope lock/action pins to CP-E verified versions; do not upgrade unrelated libraries.
5. Deliver CSP at actual document-serving HTTP layer; frame-ancestors cannot be enforced by meta tag.
## Proposed function boundaries
server.ts existing request handler admission stays one owner; add isAllowedHost(host:string|undefined):boolean
only if equivalent helper absent; parse URL safely, compare normalized host+port, reject malformed/duplicate Host.
history.ts current serialized add/load keep signatures; add per-line recovery inside existing parser,
not catch-all [] on one corrupt line. Locks release in finally; no O_EXCL symlink fallback.
codex-adapter resolveExecutable(candidate:string):Promise<string> validates absolute regular executable path.
claude-ingest uses standard timingSafeEqual over fixed-length digest, secret minimum checked startup.
Do not replace already-correct equivalent functions just to match proposed names; bind in CP-A review.
## Invariants / review
No arbitrary ComSpec/CWD executable resolution; no dynamic eval/shell from provider metadata.
CSP hosting file outside allowlist needs owner amendment before code change; don't claim meta frame-ancestors works.
Remove no old JSON keys solely because UI label changed. Release fixtures unavailable without debug build flag.
Security findings record SHA/entrypoint/owned target/payload/outcome/fix; no third-party pentesting.
## Complexity / evidence
AdmissionO(header bytes), historyO(valid lines within cap), serialized append bounded; no unbounded recovery allocations.
W3 adversarial local cases + focused dependency tests; high/critical/data-loss findings block release.
No new test/build in planning; final security green light requires actual runtime evidence, not this checklist.

## Dispatch rule
Resolve named signature/checkpoint before changing its caller or persistence boundary.
Inputs/outputs not fully specified here inherit the named existing API unchanged, never a guessed replacement.
No implementation, builds, tests, commits or pushes in this phase.
