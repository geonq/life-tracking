# P16 — revision 2 execution trace
Status: planning-only. Read16–26; source edits require execution go.
Depends on: P02, P03, P04, P06, P08, P09, P10, P11, P12, P13, P14; amendments in24.
## Exact allowed files (repository-relative)
- `ios/project.yml` (existing)
- `ios/LifeOS/LifeOSApp.swift` (existing)
- `ios/LifeOSMac/LifeOSMacApp.swift` (existing)
- `ios/LifeOS/OverviewView.swift` (existing)
- `ios/LifeOS/Settings.swift` (existing)
- `ios/LifeOS/Modules/ModuleNavigation.swift` (existing)
- `ios/LifeOS/LifeOSBackgroundRefresh.swift` (existing)
- `ios/Shared/TailscaleSyncClient.swift` (existing)
- `packages/contracts/src/index.ts` (existing)
- `ios/LifeOS/Info.plist` (existing)
- `ios/LifeOS/LifeOS.entitlements` (existing)
- `ios/LifeOSMac/Info.plist` (existing)
- `ios/LifeOSMac/LifeOSMac.entitlements` (existing)
- `ios/LifeOSWidget/LifeOSWidget.entitlements` (existing)
- `ios/LifeOSMacWidget/LifeOSMacWidget.entitlements` (existing)
- `ios/Shared/LifeOSApplicationServices.swift` (new, addition24).
## Existing anchors and ordered trace
LifeOSApp, LifeOSMacApp/LifeOSMacRootView, OverviewView, Settings, LifeOSMacRouteReducer;
TailscaleSyncClient, LifeOSBackgroundRefresh, project.yml, contracts index.
1. Membership substep: add Sync to app targets only; exclude HealthKit/service code from Mac/widgets.
2. Add shared application services owner in24; instantiate stores once, inject adapters/coordinators/publisher.
3. Register exact paired relay endpoints; Windows provider client remains Windows-only.
4. Use existing route authority; Home tiles route same source IDs/scroll restoration; no screenshot overlay host.
5. Wire Settings pair/revoke/pending/conflicts separately per device/server; no launch auto-discovery.
6. Background expiry cancels owned tasks; startup recovers stores before sync/refresh; no duplicate scene refresh.
## Proposed signatures
@MainActor final class LifeOSApplicationServices: ObservableObject {
 func start() async; func stop() async; func refresh(reason:SyncReason) async
}
init(configuration:LifeOSRuntimeConfiguration) throws establishes injected stores/adapters.
LifeOSRuntimeConfiguration contains registered endpoint IDs, vault binding, production/debug mode, not raw secrets.
services exposes immutable snapshots plus coordinator interfaces, never public mutable store dictionary.
P16 creates this only once per process; additional windows share it; widget extension has separate bounded intent composition.
## State / migration / error behavior
Recover failure yields domain-specific repair state, not blank successful home. Other independent domains remain usable.
Saved locally vs synced Mac vs waiting Windows are distinct; suspended iPhone cannot be remotely forced to run.
Legacy write endpoint disabled only after P03 pending migration and negotiated signed v1; no auth downgrade fallback.
No real provider connection reset just because prior UI showed unavailable; inspect existing config.
OS flags/minimum targets17 apply; strict-concurrency configuration explicit; no global MainActor default.
## Complexity / evidence
StartupO(bounded store metadata+pending recovery), defer heavy media/graph until route opens.
W3 normal nonfixture Mac launch→all routes→save/restart→paired relay; task count stable after repeated navigation.
P18 final target matrix. CP-F seals any missing Clipper/nutrition integration before full composition dispatch.

## Dispatch rule
Resolve named signature/checkpoint before changing its caller or persistence boundary.
Inputs/outputs not fully specified here inherit the named existing API unchanged, never a guessed replacement.
No implementation, builds, tests, commits or pushes in this phase.
