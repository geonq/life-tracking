# P04 — revision 2 execution trace
Status: planning-only. Read16–26; source edits require execution go.
Depends on: P01; amendments in24.
## Exact allowed files (repository-relative)
- `ios/Shared/FitnessTrainingStore.swift` (existing)
- `ios/Shared/NutritionMealStore.swift` (existing)
- `ios/Shared/NutritionGoalStore.swift` (existing)
- `ios/Shared/SupplementStore.swift` (existing)
- `ios/Shared/FitnessJournalStore.swift` (existing)
- `ios/Shared/FitnessLifestyleLedger.swift` (existing)
- `ios/Sync/FitnessSyncAdapter.swift` (new)
- `ios/LifeOSMacSnapshotTests/FitnessSyncTests.swift` (new)
## Existing anchors and ordered trace
FitnessTrainingStore.begin/update/finish/delete/link/unlink/execute/persistCandidate.
NutritionMealStore.addConfirmed/correct/softDelete; NutritionGoalStore.setGoal.
SupplementStore.mutate/save and occurrence receipt; FitnessJournalStore.upsert/delete/setTagState.
FitnessLifestyleLedger existing validated event mutation seam must be pinned in CP-B.
1. Reuse training/occurrence receipts and extend their envelope; no parallel log.
2. Wrap confirmed meal/goal/journal/lifestyle mutation and receipt together,19 M2.
3. Construct one adapter per store; record kind in payload distinguishes templates/session/meal/etc.
4. Keep HealthKit anchors/grants/device-local drafts out of replication.
## Proposed Swift additions
actor FitnessSyncAdapter: SyncDomainAdapter; init(store: any FitnessReplicatedStore).
FitnessReplicatedStore exact domain methods require CP-B current signature map, not arbitrary Codable closure.
Training ledger receipt adds optional SyncOperation metadata only after validated old receipt migration.
@MainActor FitnessJournalStore keeps presentation API; delegate I/O to its existing protected seam,
not a detached task capturing ObservableObject. Add Sendable to immutable record if all fields prove safe.
No user-facing Bool success until actual persistence succeeds; preserve compatibility wrapper to result enum.
## Invariants / errors
Same occurrence Taken retry no extra inventory decrement; Skip/Snooze not consumption.
Concurrent same session or meal correction creates explicit conflict; independent entities union.
Finish session once; retry ID unchanged. Nutrition proposals excluded from durable totals until confirmed.
No-op equal content allocates no sequence. Corrupt store explicit error; never empty fallback.
Cancellation before write no-op; after durable write retain success receipt; UI generation controls presentation only.
## Transactions / complexity / evidence
19 M1/M2/M3 applies; retain unacknowledged receipts indefinitely within cap, stop accepting at capacity.
Snapshot saveO(bytes), indexed receipt lookup expectedO(1), bounded history queries existing cap.
W1 repeated finish/Take/meal correction + restart + disk-full; W3 offline8-day cross-device replay.
P11 receives completed TrainingSession snapshot, not live mutable store; P16 supplies platform exporter.

## Dispatch rule
Resolve named signature/checkpoint before changing its caller or persistence boundary.
Inputs/outputs not fully specified here inherit the named existing API unchanged, never a guessed replacement.
No implementation, builds, tests, commits or pushes in this phase.
