# P08 — revision 2 execution trace
Status: planning-only. Read16–26; source edits require execution go.
Depends on: P03, P07; amendments in24.
## Exact allowed files (repository-relative)
- `ios/LifeOS/CalendarView.swift` (existing)
- `ios/Shared/CalendarViews.swift` (existing)
- `ios/Shared/CalendarLayout.swift` (existing)
- `ios/LifeOSMacSnapshotTests/CalendarInteractionAcceptanceTests.swift` (new)
## Existing anchors and ordered trace
CalendarView, CalendarViews existing UIScrollView/NSScrollView bridges; CalendarLayout placement logic.
1. Build CalendarViewportTransform in CalendarLayout.swift using day interval/timezone;20 formulas.
2. Maintain one vertical scroll owner per platform; direction-lock paging separate from timeline scroll.
3. Route magnification to hourHeight/offset transform; remove slider UI, retain menu shortcuts.
4. Layout visible occurrences via sweep, cache by day/domain revision/timezone/density.
5. Render now line only today, single gutter label; no duplicate per-column time text.
## Proposed signatures
struct CalendarViewportTransform: Equatable, Sendable { let dayStart:Date; let dayEnd:Date; var hourHeight:CGFloat; var offset:CGFloat }.
func timeAt(y:CGFloat)->TimeInterval; func offsetFor(time:TimeInterval)->CGFloat;
mutating func zoomAt(focalY:CGFloat,scale:CGFloat).
CalendarLayout.positionVisibleEvents(...) retains existing domain input; exact existing signature bound before edit.
New helper CalendarOverlapColumn = {itemID:UUID,column:Int,columnCount:Int}.
allocateColumns(intervals:[(id:UUID,interval:DateInterval)])->[CalendarOverlapColumn]; no index-as-identity.
## Behavior / transactions
Create/edit/resize calls P03 coordinator once at gesture end; visual tracking no writes.
Locked direction until end; cancellation stops autoscroll then resets preview. View disappear same cancellation.
DST folds/gaps use calendar date mapping; nonexistent local time uses current domain policy, never invented arithmetic.
Empty day scroll still24h or actual DST interval; invalid geometry clamps; stale event edit fails preserving draft.
## Complexity / evidence
Overlap sort+min-heap sweepO(n log n), memoryO(n); no pairwise n² collision loops.
W2 Mac wheel/pinch reversal, iPhone vertical+horizontal ownership, DST23/25h, drag edge/cancel and recurrence.
P16 app route owns navigation; this packet must not install a second root NavigationStack.

## Dispatch rule
Resolve named signature/checkpoint before changing its caller or persistence boundary.
Inputs/outputs not fully specified here inherit the named existing API unchanged, never a guessed replacement.
No implementation, builds, tests, commits or pushes in this phase.
