# P11 — revision 2 execution trace
Status: planning-only. Read16–26; source edits require execution go.
Depends on: P04; amendments in24.
## Exact allowed files (repository-relative)
- `ios/Shared/HealthKitWorkoutExporter.swift` (new)
- `ios/Shared/HealthKitWorkoutReconciler.swift` (new)
- `ios/Shared/HealthKitAdapter.swift` (existing)
- `ios/Shared/HealthKitReconciliation.swift` (existing)
- `ios/Shared/HealthKitAnchorStore.swift` (existing)
- `ios/LifeOS/HealthKitProductionBridge.swift` (existing)
- `ios/LifeOSTests/HealthKitWorkoutTests.swift` (new)
## Existing anchors and ordered trace
HealthKitAdapter, HealthKitReconciliationCoordinator, HealthKitAnchorStore and HealthKitProductionClient.
1. Add export protocol and pure matching DTO; iOS exporter uses actual HKWorkoutBuilder only after permission.
2. Query stable LifeOS metadata identity before save; freeze export payload/ID before asynchronous builder call.
3. Persist export receipt through training owner P04; ambiguous save completion stays pending reconciliation.
4. Match only supported source workout observations; exclude LifeOS-exported source from Zepp candidate pool.
5. Apply anchor/sample deletion via existing reconciler; never erase app-entered sets from source deletion.
## Proposed signatures
protocol HealthKitWorkoutExporting: Sendable {
 func exportCompleted(_ session:TrainingSession,mutationID:UUID) async throws -> WorkoutExportReceipt
}
struct WorkoutExportReceipt: Sendable { let sessionID:TrainingRecordID; let healthKitID:UUID; let exportVersion:Int }.
actor HealthKitWorkoutExporter: HealthKitWorkoutExporting (iOS app only).
enum WorkoutExportError: Error, Sendable { case denied, unavailable, invalidSession, uncertainCommit, cancelled }.
struct WorkoutMatchCandidate:Sendable { let id:UUID; let sourceBundleID:String; let start:Date; let end:Date; let activity:String }.
HealthKitWorkoutReconciler.match(session:TrainingSession,candidates:[WorkoutMatchCandidate])->WorkoutMatch.
WorkoutMatch = unique(UUID), ambiguous([UUID]), unavailable; no fuzzy automatic winner.
## Algorithm / state
Activity equal; starts within5min; intersection duration / max(sessionDuration,candidateDuration)>=.8.
Only one unlinked qualified candidate permits auto-link; both durations positive finite.
Query read denial/empty cannot prove earlier write absent; retry uncertain state asks reconciliation, not blind duplicate save.
Builder/session references confined to exporter isolation; callback resumes continuation at most once.
Cancellation after save retains unknown/receipt state; no assertion that HealthKit write was rolled back.
## Complexity / evidence
MatchingO(candidates<=bounded query cap), UUID dedup expectedO(1). Cap must inherit existing HealthKit query limits.
W2 pure unique/ambiguous/deleted/duplicate cases; P physical grant/deny/write/readback and Zepp comparisons.
Mac consumes DTO observations only; no HealthKit import in native Mac target. P04 receipt interface checkpoint applies.

## Dispatch rule
Resolve named signature/checkpoint before changing its caller or persistence boundary.
Inputs/outputs not fully specified here inherit the named existing API unchanged, never a guessed replacement.
No implementation, builds, tests, commits or pushes in this phase.
