# P05 — revision 2 execution trace
Status: planning-only. Read16–26; source edits require execution go.
Depends on: P00; amendments in24.
## Exact allowed files (repository-relative)
- `ios/Planning/PlanningMarkdownLinks.swift` (existing)
- `ios/Planning/PlanningGraphProjection.swift` (existing)
- `ios/Planning/PlanningSpatialIndex.swift` (existing)
- `ios/Planning/PlanningCanvasEdit.swift` (existing)
- `ios/Planning/PlanningCanvasSession.swift` (existing)
- `ios/LifeOSMacSnapshotTests/PlanningGraphTests.swift` (existing)
- `ios/LifeOSMacSnapshotTests/PlanningCanvasSessionTests.swift` (existing)
- `ios/LifeOSTests/PlanningGraphTests.swift` (existing)
## Existing anchors and exact APIs retained
PlanningCanvasReducer.apply(_ edit:PlanningCanvasEdit,to state:PlanningCanvasEditState) throws -> PlanningCanvasEditApplication.
PlanningCanvasReducer.apply(_ edit:PlanningCanvasEdit,to document:PlanningCanvasDocument) throws -> PlanningCanvasDocument.
PlanningSpatialIndex.rebuild(nodes:[PlanningSpatialNode],edges:[PlanningSpatialEdge]) throws.
mutating queryNodes(in:PlanningSpatialRect)->[String]; queryEdges(in:PlanningSpatialRect)->[String].
mutating hitTest(_ point:PlanningSpatialPoint,tolerance:Double=0)->[PlanningSpatialHit].
PlanningGraphProjector.project, ReferenceResolver.resolve, MarkdownLinkScanner.scan retain current signatures.
@MainActor PlanningCanvasSession commitInteraction/commitInspectorEdit/undo/redo/retryPending retain actual contracts.
## Ordered trace
1. Fingerprint eight untracked candidates; compare historical result source hashes, don't discard or rewrite.
2. Audit unknown fields/order/duplicate IDs/link ambiguity and bounded scan before publishing ACCEPT.
3. Verify transient drag path: if reducer copies full document per update, replace preview with node delta overlay;
only committed edit runs full reducer. Any session API change first goes to Astra, then P06 gets updated signature.
4. Audit single-node firstIndex:O(n) once per commit acceptable; multi-selection repeated scan must index once.
5. Preserve history100commands/16MiB, stable IDs, stale generation and pending publication recovery.
## Exceptional behavior / complexity
No-op/cancel causes no persistence; retry reuses mutation; undo/redo produce new durable mutation.
Ambiguous/outside reference never guessed/opened. Decoder/geometry errors retain old state.
ScanO(bytes); radix BVH rebuildO(V+E); worst overlap queryO(V+E), not guaranteedO(log n+k).
Drawing/transient updates must not call rebuild. Query metrics remain observable for future performance proof.
## Acceptance
W1 existing graph/session tests mapped to hashes; reuse unchanged passing evidence, rerun modified cases once.
Include1000 transient updates zero writes, one commit, stale/outside/ambiguous cases and brute-force index oracle.
No new UI files or vault binding here. P06 starts only with accepted actual candidate signatures.

## Dispatch rule
Resolve named signature/checkpoint before changing its caller or persistence boundary.
Inputs/outputs not fully specified here inherit the named existing API unchanged, never a guessed replacement.
No implementation, builds, tests, commits or pushes in this phase.
