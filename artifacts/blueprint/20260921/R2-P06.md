# P06 — revision 2 execution trace
Status: planning-only. Read16–26; source edits require execution go.
Depends on: P01, P05, P07; amendments in24.
## Exact allowed files (repository-relative)
- `ios/LifeOS/Modules/Planning/PlanningCanvasView.swift` (new)
- `ios/LifeOS/Modules/Planning/PlanningNodeInspector.swift` (new)
- `ios/LifeOS/Modules/Planning/PlanningNoteView.swift` (new)
- `ios/Planning/PlanningCanvasViewport.swift` (new)
- `ios/Planning/PlanningGestureBridge.swift` (new)
- `ios/Planning/PlanningVaultObserver.swift` (new)
- `ios/Planning/PlanningProjectCoordinator.swift` (new)
- `ios/Planning/PlanningVaultAccess.swift` (existing)
- `ios/Planning/PlanningVaultStore.swift` (existing)
- `ios/Sync/PlanningSyncAdapter.swift` (new)
- `ios/LifeOSMacSnapshotTests/PlanningInteractionTests.swift` (new)
## Existing anchors and ordered trace
PlanningVaultAccess.select/restore; PlanningVaultStore.read/stage/publish/publishPendingPage/resolveConflict.
P05 owns session/reducer. Never bypass store with Data.write or new DocumentGroup autosave.
1. Implement pure viewport value from20, cull visible IDs via P05 index.
2. Layer Canvas edges/grid, then SwiftUI node views/editors, then toolbar/inspector.
3. One platform gesture bridge emits events into coordinator; no simultaneous competing recognizer tree.
4. Explicit node Open loads bounded scoped note; save uses base hash and existing stage/publish.
5. Observer250ms coalesce→coordinated read→hash compare; draft conflicts retained.
## Proposed signatures
struct PlanningCanvasViewport: Equatable, Sendable { var scale:CGFloat; var translation:CGPoint }.
func worldPoint(screen:CGPoint)->CGPoint; func screenPoint(world:CGPoint)->CGPoint;
mutating func zoom(to:CGFloat,around:CGPoint); mutating func pan(by:CGSize);
mutating func fit(bounds:CGRect,in:CGSize,padding:CGFloat).
@MainActor final class PlanningProjectCoordinator: open(path:PlanningStoredPath) async throws;
selectNode(id:String?); commit(edit:PlanningCanvasEdit) async throws -> PlanningCanvasCommitOutcome;
connectNotes(source:PlanningStoredPath,target:PlanningStoredPath) async throws -> PlanningCanvasCommitOutcome.
PlanningVaultObserver.invalidate(path:PlanningStoredPath); start()/stop() idempotent; callback marshals MainActor.
actor PlanningSyncAdapter: SyncDomainAdapter, using existing journal publication; no remote path-to-file copying.
## Errors / persistence
Use existing PlanningCanvasSessionError/PlanningGraphError; new UI error enum: noSelection, invalidViewport,
externalChange, unavailableFile, unsupportedLink, permissionLost. Never replace old error by generic empty canvas.
Empty project shows Create action; selection removed externally clears selection but retains open draft as conflict.
Scheme only http/https explicit open; local note references through scoped resolver. No arbitrary file URL.
Pan/pinch zero writes; drag one commit; escape restores preview; unknown shape fallback rectangle preserves extension.
## Complexity / evidence
TransformsO(1), dragO(selection<=256), visible renderingO(k), worst index queryO(V+E).
W2 Mac/phone gesture captures, focal invariant, note save/undo; W3 external Obsidian edit/conflict/permission loss.
U1 vault selection needed only for live roundtrip; synthetic fixtures in tests cannot substitute personal-vault proof.

## Dispatch rule
Resolve named signature/checkpoint before changing its caller or persistence boundary.
Inputs/outputs not fully specified here inherit the named existing API unchanged, never a guessed replacement.
No implementation, builds, tests, commits or pushes in this phase.
