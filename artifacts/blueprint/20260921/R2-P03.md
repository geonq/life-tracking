# P03 — revision 2 execution trace
Status: planning-only. Read16–26; source edits require execution go.
Depends on: P01; amendments in24.
## Exact allowed files (repository-relative)
- `ios/Shared/CalendarStore.swift` (existing)
- `ios/Shared/CalendarDomain.swift` (existing)
- `ios/Shared/CalendarCoordinator.swift` (existing)
- `ios/Shared/CalendarPeerSync.swift` (existing)
- `ios/Shared/FinanceImportedTransactionStore.swift` (existing)
- `ios/Shared/FinanceRecurringPaymentStore.swift` (existing)
- `ios/Shared/FinanceInvestmentActivityStore.swift` (existing)
- `ios/Shared/FinanceBudgetStore.swift` (existing)
- `ios/Shared/FinanceAllocationStore.swift` (existing)
- `ios/Shared/FinanceTrackingPreferencesStore.swift` (existing)
- `ios/Sync/CalendarSyncAdapter.swift` (new)
- `ios/Sync/FinanceSyncAdapter.swift` (new)
- `ios/LifeOSMacSnapshotTests/DomainSyncTests.swift` (new)
## Existing anchors and ordered trace
CalendarStore.load/save/mutate/merge; CalendarCoordinator.save/delete/performPersist/synchronizeRemoteSnapshot.
FinanceImportedTransactionStore envelope/attempted request; Recurring saveOverride/clearOverride;
Investment merge/upsertAccountSnapshot; Budget setBudget/remove; Allocation create/update/remove;
TrackingPreferences saveDraft/commitDraft/discardDraft/commit.
1. Add optional per-store replication envelope; preserve old pending attempt bytes/IDs,19 M1.
2. Route successful local mutators through same locked candidate+receipt transaction,19 M2.
3. Read-only provider observations never become editable replicated commands.
4. Add one adapter per durable finance store using storeID, not one shared finance sequence.
5. Disable legacy incoming calendar snapshot mutation after v1 cutover; never weaken pairing fence.
## Proposed Swift additions
CalendarStore.commitReplicatedMutation(_ command:CalendarReplicatedCommand,context:SyncMutationContext) throws -> SyncCommitReceipt.
CalendarReplicatedCommand: upsert(CalendarItem), delete(UUID), series([CalendarItem],deletedIDs:[UUID]).
Each series validated as one aggregate; only existing date/recurrence validators decide validity.
actor CalendarSyncAdapter: SyncDomainAdapter; actor FinanceSyncAdapter: SyncDomainAdapter.
Finance adapter init(store: any FinanceReplicatedStore) — CP-B seals protocol with concrete current envelope types.
Existing synchronous locked finance stores stay locked; do not blanket convert them to actors.
Add pendingPage/applyReplicated/acknowledge/checkpoint at existing persistence seam; CP-B supplies typed body per store.
## State / migration / errors
Keep drafts local until explicit commit; equal edit no-op; duplicate receipt returns prior result.
Conflicting entity base→retainConflict; remote time never wins; invalid domain decode rejects without ACK.
Finance legacy attempted request remains owned by old protocol until ACK or explicit inspected conversion,
never simultaneously submitted through legacy and v1 writer. Endpoint cutover P16 depends on this invariant.
Cancellation/network outage cannot undo local save; storage errors preserve old envelope and form input.
## Complexity / evidence
Snapshot persistenceO(encoded bytes), ID lookup expectedO(1), opening indexO(records); merge sortO(n log n).
W1 legacy reopen/old pending/disk-full/series atomicity/replay; W3 concurrent Mac/phone delete/edit + restart.
CP-B per-envelope coding map required; this packet does not redesign recurring/money semantics (P09).

## Dispatch rule
Resolve named signature/checkpoint before changing its caller or persistence boundary.
Inputs/outputs not fully specified here inherit the named existing API unchanged, never a guessed replacement.
No implementation, builds, tests, commits or pushes in this phase.
