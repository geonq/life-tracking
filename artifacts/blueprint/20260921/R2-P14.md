# P14 — revision 2 execution trace
Status: planning-only. Read16–26; source edits require execution go.
Depends on: P03, P04, P07, P11; amendments in24.
## Exact allowed files (repository-relative)
- `ios/LifeOS/WidgetSnapshotPublisher.swift` (existing)
- `ios/Shared/FutureWidgetSnapshot.swift` (existing)
- `ios/LifeOSWidget/LifeOSWidget.swift` (existing)
- `ios/LifeOSWidget/UsageWidget.swift` (existing)
- `ios/LifeOSWidget/CalendarWidget.swift` (existing)
- `ios/LifeOSWidget/NextEventWidget.swift` (existing)
- `ios/LifeOSWidget/FutureModuleWidgets.swift` (existing)
- `ios/LifeOSMacWidget/LifeOSMacWidget.swift` (existing)
- `ios/LifeOS/Modules/Automation/LifeOSAppIntents.swift` (existing)
- `ios/Shared/SigningStatus.swift` (existing)
- `scripts/install_personal_device.sh` (existing)
- `scripts/install_personal_device_checks.py` (existing)
- `scripts/tests/test_personal_device_installer.py` (existing)
- `ios/LifeOSWidgetSnapshotTests/WidgetCompletionTests.swift` (new)
- `docs/personal-shortcuts.md` (new)
## Existing anchors and ordered trace
WidgetSnapshotPublisher.publish; FutureWidgetSnapshot; widget bundles and NextEventWidget accessoryRectangular;
LifeOSAppIntents, SigningStatus, install_personal_device.sh and installer checks.
1. Inventory every registered widget kind and source producer; preserve existing kinds/config IDs.
2. Build snapshots only from accepted domain revisions; digest no-op suppresses redundant write/reload.
3. Render system outer corners/background; dark/tint/locked states explicit; deep link stable entity IDs.
4. Intent resolves validated entity and delegates to existing store receipt; no independent persistence path.
5. Document Mac Shortcut fixed installer path+UDID args; no uninstall to refresh signing.
## Proposed signatures
WidgetSnapshotPublisher.publishIfChanged(now:Date) async throws -> WidgetPublicationResult.
WidgetPublicationResult = unchanged, published(revision:String), deferred(reason:String).
Use current snapshot value types; add optional fields/schema migration only,19 M6.
AppIntent.perform() async throws -> some IntentResult invokes store; no HealthKit or bank fetch from widget extension.
Installer check function validate_install_target(udid:str,bundle_id:str,team_id:str)->InstallTargetResult
belongs install_personal_device_checks.py; returns ready/requiresTrust/profileMismatch/deviceMissing.
CP-C verifies real entitlement/profile; do not fake App Group container.
## Exceptional states / migration
Stale snapshot still visible with asOf; unsupported schema placeholder, previous file preserved.
Sensitive data redacts when locked; grey wallpaper cannot be assumed to supply contrast.
Intent duplicate uses stable command mutation ID persisted in store; return success only after durable commit.
Background refresh opportunistic; Morning status accurately distinguishes attempted/done/unavailable.
Zepp opens only verified supported action; fallback manual instruction; no false sync completion.
Profile expiration actual metadata; blocked renewal keeps user data; bundle/team migration requires explicit backup decision.
## Complexity / evidence
ProjectionO(records within snapshot cap), digestO(bytes), unchanged writes0; no widget network/timeline animation.
W2 every widget producer/deeplink/state map; P physical locked/tint/signed container and USB in-place renewal.
P16 owns shared target inclusion; intent file currently app-owned must not be blindly added wholesale to widget target.

## Dispatch rule
Resolve named signature/checkpoint before changing its caller or persistence boundary.
Inputs/outputs not fully specified here inherit the named existing API unchanged, never a guessed replacement.
No implementation, builds, tests, commits or pushes in this phase.
