# Feature coverage authority — revision3
Planning-only. Source inventory at321a2b5, not a fresh runtime certification.
PRESENT means the named function/type exists; PARTIAL means requested integration or evidence remains open.
PROPOSED means a named revision2 addition; no implication that it is implemented.
Every row below is binding together with its R2-Pxx, R3 schemas,20 design tokens and27 composition.
Unqualified Swift store paths mean ios/Shared/<Type>.swift. UI paths use their R2 packet allowlist.
CP-L:<ID> means an explicit pre-implementation Astra checkpoint, NOT permission for Luna to choose a function.
Resolve it by reading the ID's original source reference, tracing its current View.body→action→store call,
then writing the exact existing/new function signature, payload, error/cancel path and acceptance capture to a doc.
Missing named DTO fields use CP-S01…05 in R3-04; no source change until that domain gate resolves.
Source presence does not close a registry leaf whose historical status is foundation/missing/blocked-external.

## Read order and inherited row fields
F01 product/navigation/finance; F02 fitness/nutrition/usage; F03 planning/platform/operations;
F04 each registered widget; F05 every security report item; LEAVES files preserve all historical requirement IDs.
Rows inherit these explicit constraints unless their own cell narrows them:
- Visual/motion:20 and27 system SF Pro/SF Symbols, compact roles, semantic accents, interruption-safe motion;
  no repeating decorative animation on static data, no oversized ring or repeated unavailable paragraphs.
- Offline: preserve last committed local data with asOf; no missing→zero, no silent fixture fallback.
- Security: scoped read/write authority, bounded validated inputs, secrets excluded from logs/snapshots;
  no generic advisor/coaching generation. Photo calorie proposals are the only AI workflow.
- Release: accepted source SHA plus the listed integrated live/device/visual evidence; planning text is not proof.
- Every local mutation: owning store transaction→outbox→post-commit projection/widget publication;
  no transport arrival→widget path. R3-02 defines cancellation/disk-full/duplicate/concurrency.
- User-data deletions retain tombstones until approved checkpoint; no week-long outage expiry of pending edits.

## Registry precedence
Historical coaching routes in BF-0384/BF-0396/BF-0405 are superseded: factual explanations only, no AI advisor.
Historical demo-state checks apply to isolated visual tooling, never normal launch/provider fallback.
Historical OAuth/PKCE requirements are capability-specific: no invented OAuth for Gemini consumer quota.
Historical proprietary fitness parity is replaced by source-labelled supported metrics, never simulated Zepp accuracy.
Historical screenshot typography is superseded by20/27; functional requirements persist.
A no-data state can satisfy honest unsupported capability, but cannot substitute for a supported unimplemented action.
NextSemis optional gate remains outside mandatory release; Robinhood import/net worth remains mandatory.
Historical registry freeze is preserved, not overwritten; expanded rows retain original status for audit.
