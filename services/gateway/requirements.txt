# Exact pins keep the personal Windows runtime reproducible. The corresponding
# Windows 3.12 wheel hashes live in requirements.lock. Runtime-only packages
# belong here; test tooling is installed in the isolated CI/test environment.
fastapi==0.141.1
httpx==0.28.1
PyJWT[crypto]==2.13.0
python-multipart==0.0.32
starlette==0.51.0
# Windows Python does not ship the IANA timezone database used at import time.
tzdata==2026.3
uvicorn==0.52.4
# Keep the bounded HTTP and WebSocket protocol implementations explicit so the
# launcher cannot silently switch to an unreviewed optional implementation.
httptools==0.8.0
websockets==17.1
