# LifeOS — definitive native visual design specification

## 1. Authority, references, and product grammar
- Status: implementation-ready visual contract, reconciled 2026-09-12 against inspected source at `996ccf4` and current coordination files. Sections 19–20 consolidate visual rules and execution gates; §19.9 is the dispatch, supplemented by §20.2 coverage. This document specifies the target; it does not certify the current app or any device/provider runtime.
- Scope: native SwiftUI iPhone/macOS and WidgetKit. This authoring task changes only root `design.md`; no code, tests, coordination edits, or commits are authorized by it.
- Authority: AGENTS.md names /Users/georgdomke/Arbeit/VS Code/LifeOS Design/developers/design-coordination/00-READ-FIRST.md as governing context; this existing root design.md is the native implementation specification, not a relocation of that authority. Explicit user requirements supersede historical Iconoir/custom-font/amber-estimate guidance. Preserve current DECISIONS.md constraints and data authority, consent, privacy, and durable receipts. Historical plans apply only where compatible; visual requirements never override domain validation.
- Read with repo `AGENTS.md`, `Coordination/HANDOFF.md`, and `Coordination/DECISIONS.md`. Owners below are paths relative to this repo; named replacement contracts are requirements, not claims that those APIs already exist.
- Palette authority: `/Users/georgdomke/Library/Mobile Documents/com~apple~CloudDocs/geon/colors.md`; preserve its complete blue ramp, especially main blue `#0253C4`.
- [Skills UI Clean](https://www.skillsui.app/skills/clean): use whitespace to express hierarchy, one primary action, consistent primitives, limited neutral surfaces, explicit accent, complete hover feedback, and useful empty-state content. These are inspiration principles; all measurements below are LifeOS decisions.
- [Forever Components Infinite](https://forevercomponents.com/infinite/): supplied fetched-page observations describe an endless component wall, drag/scroll roaming, zoom/teleport/reset, lazy live tiles and performance mode. Use bounded canvas navigation and explicit reset as interaction references for Calendar or a separately planned mind map; do not copy web markup, add dependencies, or infer native performance from the reference.
- Implement these influences using SwiftUI, Core Animation, Swift Charts, and narrow UIKit/AppKit bridges only. No HTML, CSS, web view, copied markup, npm animation package, or runtime web dependency enters the app.
- Screenshot baseline is the seven user attachments visible in this conversation: Finance unavailable dashboard, sidebar, repeated Calendar now labels, Mac calendar expansion control, Recovery, Biology, and meal review. These are hard evidence of the reported build's defects, not proof that today's source still renders identically. Section 13 maps every capture to owners and rejection criteria. Scrolling failure is the user's runtime report; a still image alone cannot prove gesture behavior.
- North star: page identity → meaningful status/value → next action → supporting evidence. Spend visual space in proportion to information, not to the number of backend capabilities.
- Do: align edges; group related decisions; label provenance; use real zero; reveal detail on intent; keep controls predictable; make sparse content compact and useful.
- Don't: stack cards inside cards; repeat the same unavailable sentence; fill missing data with decorative graphs; color every module; animate numeric truth through invented intermediate values; hide a working escape from an empty filter.
- No Advisor, generic AI chat, coaching generator, or advice entry point. Usage remains factual provider consumption; meal-photo calorie estimation remains the sole in-app AI workflow.

## 2. Typography: one SF Pro facade
- Owner: `ios/Shared/Typography.swift`, with geometry tokens in `ios/Shared/DesignTokens.swift`. Use `.system(size: scaledSize, weight: roleWeight, design: .default)`; Apple chooses SF Pro optical rendering. No named SF font file, rounded face, or custom font registration.
- Canonical platform sizes are in §19.2; there is no shared Mac/iPhone size table. Use existing scaled semantic roles; custom scaled sizes use `@ScaledMetric` once, never double-scale a semantic font.
- Tracking values stay fixed in points; use tracking 0 at accessibility sizes. Use natural system line metrics; all roles use natural system leading with 0 additional line spacing; paragraphs separate by the 8 pt spacing token. No uppercase headers except short provenance labels such as `DEMO · NOT LIVE`.
- Use `.monospacedDigit()` for money, dates in changing controls, durations, percentages, and editable numeric values; ordinary names/copy remain proportional. Never use monospaced text as a substitute for tabular digits.
- Value and unit share first baseline with 4 pt gap; unit uses metadata. Keep currency sign and amount together with locale formatting; move a long unit to a second line before shrinking. App metrics never use minimumScaleFactor to defeat text size preferences.
- Preserve currency minor-unit precision; percentages use at most one decimal when the source supports it; source-defined health precision remains source-defined. Unavailable = em dash plus reason; observed zero = formatted 0 with unit. Do not round unknown into zero.
- App supports all Dynamic Type sizes without a global cap: at accessibility1 and above, use one-column cards, vertical metric/unit layouts, wrapping selectors or a labeled menu, and stacked form fields. Essential labels, validation, and state copy have unrestricted vertical growth.
- Nonessential account/event names may use two lines then truncate; full text remains in detail and accessibility labels. Do not truncate values, consent text, save failures, dates that distinguish repeated times, or action names.
- Widgets use fixed family geometry and constrained roles: hero 28, compact metric 22, title 15 semibold, body 13 medium, metadata 12 medium; scale with the above anchors only through `.xxxLarge`, then prioritize content removal, not more shrinkage.
- Widget numeric fallback is 28 → 24 → 22 pt using `ViewThatFits`; never below 22 for the primary value. Calendar grid dates alone may use 11 pt medium. Accessories use system-appropriate 13 pt text/20 pt metric; accessible labels contain unabridged values and state.
- Migration: expand `LifeOSTypography` deliberately; migrate callers and remove obsolete font wrappers, Inter/Space Grotesk/Manrope assets and registrations. Do not retain unused weight-overload APIs or legacy font aliases after the last call site moves.

## 3. Surfaces, colors, and dimensional tokens
- Owner: `ios/Shared/DesignTokens.swift`. Color pairs are foreground/background, opaque sRGB unless explicitly noted. Apply these roles throughout components; do not choose a convenient raw hue at a call site.
- Complete brand blue ramp: 50 #E6F0FF; 100 #B8D5FE; 200 #8BBBFE; 300 #5DA0FD; 400 #3085FD; 500 #036BFC; 600 #0253C4 (main); 700 #0244A2; 800 #013174; 900 #011E47; 950 #000B19. Preserve all values; assign semantic roles rather than using every stop in one surface.
| Role | Dark pair | Light pair |
| --- | --- | --- |
| Canvas primary | `#F7F7F8 / #000000` | `#101012 / #FAFAFA` |
| Card primary | `#F7F7F8 / #131315` | `#101012 / #FFFFFF` |
| Card secondary / metadata | `#A1A1AA / #131315` | `#52525B / #FFFFFF` |
| Raised/input primary | `#F7F7F8 / #1B1B1E` | `#101012 / #F4F4F5` |
| Raised/input secondary | `#A1A1AA / #1B1B1E` | `#52525B / #F4F4F5` |
| Floating sheet/popover | `#F7F7F8 / #1B1B1E` | `#101012 / #FFFFFF` |
| Primary button normal | `#FFFFFF / #0253C4` | `#FFFFFF / #0253C4` |
| Primary hover / pressed | `#FFFFFF / #0244A2`, `#FFFFFF / #013174` | Same pairs |
| Selected navigation label | `#F7F7F8 / #1B1B1E` | `#101012 / #F4F4F5` |
| Disabled control | `#A1A1AA / #1B1B1E` | `#52525B / #F4F4F5` |
- Disabled buttons use the listed neutral pair plus disabled semantics; don't multiply the whole control's opacity. Input placeholders use secondary text, not a lower-contrast tertiary invention.
- Decorative border: dark `#232329`, light `#E4E4E7`, 1/displayScale pt. Essential unfilled-control edge: dark `#73737D`, light `#767680`, 1 pt. Focus: dark `#5DA0FD`, light `#0253C4`, 2 pt outside with 2 pt clearance.
- Limit backgrounds to canvas, surface, and raised; floating uses an existing fill. Sidebar uses canvas. No permanent card shadows; only floating overlays use black opacity 0.24 dark/0.12 light, radius 16, x 0/y 8. Native sheet shadow is system-owned, never doubled.
- Radius: Home/Usage summary card 12 continuous, other content card 16 continuous; control/input/navigation 10 continuous; event 7 (joined overlap edge 2); tooltip 8; sheet 24 when configurable, otherwise system-native. Capsules only for progress arcs and truly compact status badges.
- Spacing scale: 4, 8, 12, 16, 24, 32, 48 pt. Page gutters 16 compact/24 regular; card padding 16; section gap 24; sibling card gap 16; label/value gap 8; label/helper gap 4. No screen-specific 13/18/22 pt padding fixes.
- Widths are measured after sidebar/safe-area reservation: under 720 pt content area = one column; at least 720 = two columns with 16 gap and minimum 300 each; maximum page width 1040; prose/form width 600. Calendar alone is a viewport surface, not a centered reading column.
| Semantic foreground / graphic | Dark on surface or raised | Light on surface or raised | Meaning |
| --- | --- | --- | --- |
| Observed chart | `#5DA0FD` | `#0253C4` | Solid measured series; never projected |
| Projection/estimate | `#60D386` | `#01773B` | Dashed 6/4 estimated series and `Estimated` label |
| Calories | `#FFB06E` | `#A25A03` | Measured/confirmed kcal, orange |
| Protein | `#63D2D2` | `#067878` | Teal; distinct from observed blue |
| Carbohydrate / fat | `#C4B5FD` / `#A1A1AA` | `#5E06DC` / `#52525B` | Violet / neutral, always labeled |
| Informational blue | `#B8D5FE` | `#013174` | Text/link context; underline links outside buttons |
| Error / warning | `#FC584F` / `#FBDD68` | `#B70112` / `#806000` | State symbol and copy, not whole metric recoloring |
- Semantic precedence: privacy/availability determines whether a value exists; explicit error/warning labels explain trust; estimate styling overrides the estimated numeric mark's metric color; observed metric color overrides module identity. An estimated calorie value is green and labeled Estimated; its `Calories · kcal` label remains orange.
- Goal/reference line = secondary neutral, 1 pt dashed 2/4; observed = 2 pt solid; estimate = 2 pt dashed 6/4; history = 1.5 pt secondary neutral with `History` label. Do not use green for both estimate and goal on the same graph.
- Chart fills are the matching semantic color at 10% fading to 0%, never the sole encoding. Add line pattern, direct label, or symbol for every semantic distinction. Status-green may appear beside a checkmark but cannot relabel estimates as healthy outcomes.
- Calendar events use one shared observed-blue accent with selected-navigation fill/text pairs; red is reserved for today's/now marker. Navigation icons remain neutral/selected blue, not a multicolor module palette.
- All text pairs must meet 4.5:1 and essential graphics/focus 3:1 in composited captures. Decorative separators need not meet 3:1. If a role fails, fix its token/pair globally; do not hide it with opacity or shadow.
- Grey-wallpaper full-color transparent widgets: clear outer container, inner readable-content backing `#000000` at 60% opacity, radius 12, padding 8; text `#FFFFFF`, supporting text `#E6E6E6`, no additional text opacity. The backing stays behind all essential content, not just the hero.
- That backing is the approved contrast-managed transparency, not a new card/clear user setting. On solid greys `#606060`, `#808080`, `#A0A0A0`, and even white, it provides a dark field; never assume white text alone is readable on grey.
- System-accented/vibrant/clear rendering may remap or remove custom backgrounds: keep text in the system primary group, accent only the key marker, and use shape/weight/state words instead of relying on hue. Never accent both a foreground and its backing into the same tint.
- Use native WidgetKit container/rendering environments with availability guards. If the system cannot preserve the backing in a mode, its native contrast treatment must pass real-device captures; a white-foreground SwiftUI preview is not acceptance. Full-color keeps green/orange; monochrome modes retain `Est.`, `kcal`, dash patterns, and state labels.

## 4. Canonical component contracts
- Owner: `ios/Shared/LifeOSComponents.swift`; chart mechanics remain in `LifeOSChartKit.swift`/`LifeOSBarChart.swift`; input ownership in `LifeOSInteractionKit.swift`; motion in `LifeOSMotionKit.swift`. Shared components render passed state and emit intents, never fetch data or mutate stores.
| Contract | Inputs and exact layout | Responsive / interaction contract |
| --- | --- | --- |
| `LifeOSPageFrame` | title, optional subtitle/status, primary action, content; 16/24 gutter, 24 section gap, max 1040 | Exactly one page scroll owner unless viewport mode; header action moves below title when it will not fit; bottom safe-area inset + 24 |
| `LifeOSSectionHeader` | title, optional subtitle, optional trailing action; 8 internal gap, first-baseline alignment | Trailing action remains a separate accessible button; stack at AX sizes; do not combine it into heading accessibility |
| `LifeOSCard` | level, content, optional explicit interaction; radius 12 for Home/Usage, otherwise 16, padding 16, one hairline | Intrinsic height; no blanket minHeight; interactive card uses a real Button/NavigationLink, never an onTap-only wrapper or nested buttons |
| `LifeOSStatusRow` | state, cause, optional timestamp, one recovery action; 20 icon box, 12 gap, 12 vertical inset | 56 pt minimum with one line, normally 72 with cause; grows for text; no chart silhouette or repeated status badge |
| `LifeOSMetricHeader` | label, nullable value, unit, provenance; 8 vertical gap, 4 value/unit gap | 28/30 Mac/phone hero ceiling or 22/24 compact (§19.2); value stays leading; unit drops below; nil shows em dash and reason, not 0 |
| `LifeOSButton` | primary/secondary/tertiary/destructive, label, icon, enabled, busy | iPhone min 44 high/44 wide; Mac min 32 high/32 wide; 16 horizontal padding; spinner reserves 16 box, stable label width |
| `LifeOSSelector` | stable option IDs, selected ID, availability reason | iPhone 44/Mac 32 high; equal cells only when labels fit at ≥64 width; otherwise labeled menu, not clipped scrolling pills; selected/focused distinct |
| `LifeOSChartShell` | title/metric, mode/range controls, plot, legend, status, presentation identity | Plot 220 compact/260 regular; inspection row 44 below; legend wraps; first-load/no-source switches to compact 72–104 pt state body |
| `LifeOSSheet` | title, contextual subtitle, form, footer action, dismiss intent | iPhone large detent for editing, safe-area footer; Mac width 520, min(420, available width−48) lower bound, max 600; height min(content, 760, available height−48); body scrolls |
| `LifeOSNavigationRow` | route ID, SF Symbol, label, selected, badge | iPhone 48/Mac 32 high; phone 24/Mac 20 icon box, glyphs per §19.3, phone 12/Mac 8 gap, 12 inset, neutral selected fill and no rail |
| `LifeOSStateView` | empty/stale/partial/error/redacted, cause, next action, retained content | Compact state row for absent content; inline notice over retained truthful content; no duplicate outer and inner empty panel |
- Touch targets may exceed icon size but may not overlap adjacent targets. Charts offer keyboard arrows and VoiceOver adjustable actions; tiny calendar events use a shared disambiguation hit region rather than overlapping 44 pt invisible buttons.
- Forms use 44 pt minimum input height (Mac 32), 12 horizontal padding, labels above fields with 8 gap; error below with 4 gap. Input text is 13 pt Mac/17 pt iPhone. Field frame expands at large text; focus ring is visible in every appearance.
- Secondary buttons use raised fill/primary text; tertiary uses informational blue with hover raised fill; destructive uses error foreground with explicit label and confirmation only for destructive operations. Only one prominent primary per decision surface.
- Empty-state grammar: specific cause + useful consequence + one real action, e.g. “No bank accounts connected. Connect an account to view balances.” Never display a disabled Connect button if the actual operation cannot be performed; offer the supported setup destination.
- Loading before any result: status row with native progress indicator; do not reserve a giant fake chart. Refresh after presentation keeps previous geometry and values with `Updating…`; unavailable plots lose overlays, keyboard handlers, and accessibility adjustable traits.
- Collapse duplicate APIs into `LifeOSPageFrame`, `LifeOSCard`, and `LifeOSTypography`: migrate `pagePadding`/`pageGutter`, `flatCard`/`lifeOSCard`/`lifeOSSurface`, module card wrappers, and competing header/status recipes; delete superseded APIs once callers migrate.

## 5. Shared state model and shell/sidebar
- Every module resolves: live (valid current observation), zero (valid observed zero), empty (no observations in scope), stale (retained observation beyond domain freshness), partial (known subset), error (failed operation), plus loading and redacted. Zero is data, not a source state.
- Freshness and coverage come from existing domain timestamps/windows; views do not invent freshness thresholds. Never collapse HealthKit read-indeterminate into permission-denied, or unsupported Zepp behavior into an actionable permission request.
- Error with retained data = retained values plus error notice; error without data = compact recovery state. Partial displays coverage/source qualification beside totals. Redaction immediately removes protected values, tooltips, cached accessibility strings, and widget titles.
- Owners: `ios/LifeOS/Modules/ModuleNavigation.swift`, `ios/Shared/LifeOSIcon.swift`, `ios/LifeOS/LifeOSApp.swift`, `ios/LifeOSMac/LifeOSMacApp.swift`; shared row/frame own appearance. Preserve release-visible route arrays, not every compatibility enum case.
- Layout: Mac sidebar width 208 (resizable 184–240), header 44, primary destinations Home → Calendar → Finance → Fitness → Tax, Settings at bottom; content frame begins after divider. Collapse sidebar below 900 pt window width; restore user width on expansion.
- iPhone tabs remain Home, Calendar, Finance, Fitness, More; More contains Tax and Settings. Usage opens from Home's factual usage summary. Do not promote unfinished compatibility routes into navigation or create an Advisor replacement.
- SF Symbol map: Home `square.grid.2x2`, Calendar `calendar`, Finance `creditcard`, Fitness `waveform.path.ecg`, Tax `doc.text`, Settings `gearshape`, More `ellipsis`, Usage `chart.xyaxis.line`; Clipper `rectangle.stack`; Bank Connections `link`, Investments `chart.line.uptrend.xyaxis`, Documents `folder`, Business `briefcase`, Tasks `checklist`, Grocery `basket`, Shopping `bag`, Reports `chart.bar.doc` for supported internal destinations only.
- Selected rows follow §19.3: a quiet neutral fill and blue glyph, without a leading rail; unselected rows use secondary text. Hover uses raised fill without lift; selection preserves symbol outline/weight. Avoid filled-vs-outline swaps, emoji, custom icon assets, and multi-hue badges in shell navigation.
- State map: shell remains stable in live/zero/empty/stale/partial/error; module-level state lives in content, not six duplicated sidebar alerts. An invalid deep link opens a compact destination-unavailable state with Back/Home; navigation itself does not pretend to load.
- Priority: current module action is the page primary; sidebar has no competing CTA. Home no-data layout is a single setup status plus compact module rows; unavailable compatibility routes get one cause/action row, not dashboard skeletons.
- Each module owns route stack, selected date/filter, and semantic scroll anchor. Switching modules restores these; deep link changes only its addressed scope. Back restores originating card focus and position; sheet dismissal restores invoking control; don't force focus during background refresh.
- Mac Tab follows visual order; Return/Space activate, Escape dismisses transient inspection before backing out. On navigation VoiceOver focuses the unique page heading; do not announce the whole sidebar again. Preserve native iPhone interactive-back ownership at the screen's leading edge.

## 6. Finance and Usage
- Owners: `ios/LifeOS/Modules/Finance/FinanceView.swift` (`FinanceHeroCard`, `FinanceDetailChartCard`, accounts/categories/wealth), `FinanceChartModeViews.swift` (mode controls/bar rendering), `FinanceAnalyticsView.swift`; `ios/LifeOS/Usage/UsageProjectionChart.swift`, `UsageFactsView.swift`, and `ios/LifeOS/CodexView.swift` own factual Usage.
- Finance layout: header/status → account-total hero with currency and updated/source line → Spend/Income/Cash flow/Net worth detail selector → one chart shell → category list → accounts → wealth/import supporting rows. Remove duplicate hero facts and a second equal-priority category ring.
- Regular content: chart + categories in 2:1 columns only at content width ≥960 (minimum 600/300, gap 24); below that stack. Accounts are 56 pt rows, up to five before Show all; category summary six rows including Other, full list on detail.
- Hero uses 28/30 pt Mac/phone amount, 13/15 pt label, 12/13 pt currency/source/freshness; connect/review is primary only when connection is the next valid action. Live uses chart inspection as primary interaction; refresh is a quiet toolbar action, import a secondary row.
- Plot uses 220/260 pt height, four y ticks and at most five x ticks (three under 360 pt plot width); currency/unit appears once. Line can use padded observed extent; bar/ring always use a truthful zero baseline. Negative cash flow shows a visible zero reference.
- Live: show observed totals/series and source coverage; zero: show zero total with flat zero line or zero-height bar labels, empty arc for a zero budget fraction; never draw a fictitious positive wedge to make a ring visible.
- First-load/no-source: one 72–104 pt status body below header; no axes, fake baseline, ring outline, chart controls, or four unavailable cards. Accounts/wealth use compact 56–72 pt rows only when their next action differs; page has no reserved empty plot.
- Filtered-empty with known source: keep mode/range selectors and current 220/260 plot slot, replace marks with one centered cause and `Show Max`; no chart hit testing. This avoids layout jumps while preserving escape from an empty selection.
- Refresh: retain measured geometry and last values; cancel obsolete requests; failed refresh keeps last timestamp with Retry. Lost availability retains only admissible stale observations and the previously presented plot slot during the visit; if nothing remains, a later fresh visit starts compact.
- Partial: display “Includes N of M accounts” only when denominator is known; otherwise name missing coverage. Never label a subtotal Net worth without qualification. Stale/error badges describe data age/failure, not a fabricated balance change.
| Detail / mode | Availability rule | Selection and empty behavior |
| --- | --- | --- |
| Spend, Income / Line | At least one real in-range point; one point renders a dot plus date/value | No interpolation across missing observations; zero remains selectable |
| Spend, Income / Bar | At least one bucket with non-nil total | Missing bucket = gap with accessible reason, zero = labeled zero; weekly grouping stays in existing domain |
| Spend, Income / Ring | Valid category aggregates for selected range | Zero total displays `0 · no spending/income recorded`, no arc; unsupported categorization explains cause |
| Cash flow, Net worth / Line only | Valid observed series for selected detail | Hide mode switch; retain range choices, never synthesize ring/bar support |
| Net worth / Projection | Existing projection domain returns projected result | Green dashed continuation and assumptions/date; refusal is one compact note, never a flat guessed forecast |
- Ranges remain `1W`, `1M`, `6M`, `1Y`, `Max`; range is persisted per detail. Evaluate availability for `(account scope, detail, mode, range, currency)`, not a single page-wide points.isEmpty. Empty choices stay escapable; unsupported controls explain why.
- Inspection identity = scope + detail + currency + observed-model identity; track dataset revision separately. Different scope/detail/range resets scrub/zoom; different observed model cancels all inspection. Refresh of same model preserves selection only if its exact point ID remains; otherwise clear it without nearest-point jumping.
- Line↔bar can retain a selected timestamp only when a real point/bucket in the same series/window resolves via `FinanceChartSelectionCodec`; explicitly identify the weekly aggregate. Ring uses category ID; clear time inspection entering/leaving ring. Always clear zoom on mode switch; never morph unrelated datasets.
- Scrub presents one value, time, unit, observed/estimated status, and coverage; tooltip width ≤200, clamped 8 pt inside plot. Show a fixed 44 pt inspection row for long labels; pointer tracking has no view-layout feedback loop. Range controls remain outside the gesture overlay.
- Usage layout is replaced by §19.5: compact toolbar → window summaries → chart controls/plot → factual detail. Remove the duplicate hero/ring/metadata/banner stack. Existing non-AI explanatory analytics may live in factual detail; no generic Insights/advice action.
- Usage graph: Actual blue solid, Estimate green dashed, Target neutral dotted, History secondary neutral only when persisted history exists. Keep the current projection anchor at last observation and stop at reset; no provider-specific saturated chart palette or invented history.
- Usage states: live/zero use actual window values; no-source/empty use 72–104 pt setup state; stale/error retain same-window history with timestamp; partial marks gaps; a reset/provider/window identity change clears inspection and old estimates immediately. Ranges without real history cannot fabricate it.
- Usage primary is Connect/Open settings only when necessary; otherwise inspect graph. Prev/Next inspection controls are 44/32 pt, disabled at bounds. A vertical drag scrolls the page; horizontal intent after 8 pt and abs(dx) ≥1.3×abs(dy) scrubs; unresolved diagonals never claim the scroll.

## 7. Calendar: viewport, gestures, and time
- Owners: `ios/LifeOS/CalendarView.swift` owns chrome/date/view/scale state; `ios/Shared/CalendarViews.swift` owns `CalendarTimelineView`, day columns, event/all-day/now rendering and platform bridges; `CalendarLayout.swift` owns pure geometry, arbitration, focal mapping, and DST rules. Coordinator/PeerSync remain data authorities.
- iPhone layout: compact month/date toolbar → one day-header row (today/tomorrow/day-after-tomorrow) → all-day strip → one finite vertical timeline viewport → safe-area bottom actions/tab bar. Keep three settled day columns; each horizontal settle advances exactly one day, never replaces the design with a single-day page.
- One 40 pt time gutter sits inside the vertical scroller beside the shared day surface; one hour label per row, one grid line across the three columns. Timeline is edge-to-edge within 8 pt outer inset; header and all-day columns use the same day-width equation `(viewportWidth−16−40)/3`.
- Scroll architecture: `GeometryReader` measures available viewport outside `ScrollView(.vertical)`; one content stack has explicit `24 × hourHeight` grid height. No vertical ScrollView per day, no enclosing page vertical ScrollView, no infinite-height frame, no full-screen high-priority drag consuming vertical pan.
- Pin header/all-day outside timed scroll; all-day shows at most two 24 pt rows plus a 32 pt `+N more` row, with 44 pt combined touch targets. Expansion opens a scrolling all-day sheet, so timed viewport stays usable; `+` supports all-day creation as well as timed creation.
- Default scale 64 pt/hour; minimum 40, maximum 120, shared across timeline views per device. Hour labels use 13 pt metadata; at AX sizes keep three-day geometry and provide an explicit Agenda reading mode with full-sized event text and accessible editing.
- Late-day contract: actual viewport excludes bottom controls via safe-area inset; remaining occlusion O is measured, not hardcoded. Content height H = 24×scale + O + 24 pt endpoint clearance; max offset = max(0,H−viewportHeight). At bottom, 23:45–24:00 is visible above controls with ≤24 pt spare tail.
- Reject any viewport-sized trailing spacer: reaching late-day content does not require scrolling 24:00 to the viewport top. Do not add an entire blank viewport or double-count home indicator/tab bar. Assert bottom visibility and no oversized tail at 40/64/120 pt/hour.
- Initial today view scrolls once to now−120 minutes, clamped; a non-today first visit starts at 08:00. Preserve wall-time scroll anchor on paging, return, resize, and header expansion. Today button returns date to today and explicitly scrolls to now; refresh never recenters the user.
- Mac keeps existing five-view calendar navigation and inspector. Timed grid owns vertical scroll; horizontal day navigation/overflow is independent. Inspector is 280 pt at window width ≥1180, otherwise presents as a sheet. Header aligns to content columns and time gutter, with no duplicate day titles.
- Mac magnification: capture scale h0, content offset s0, and pointer y p inside timed viewport at gesture begin; focal wall minute m = 60×(s0+p)/h0. Each magnification sample sets h = clamp(h0×factor,40,120), s = clamp(m×h/60−p,0,H(h)−V). Preserve focal time within 1 minute unless edge-clamped.
- Use `MagnifyGesture`/native magnification with an actual start location; use a narrow AppKit event bridge where SwiftUI cannot report the pointer anchor or scroll offset. Never use center-anchored `scaleEffect` on the calendar. Pointer position is frozen at begin; scroll momentum stops before zoom owns the viewport.
- Mac fallback is a keyboard-accessible Density menu: Compact 40, Comfortable 64, Spacious 120 pt/hour; Reset =64. No slider, percentage track or dotted rail. Presets animate 160 ms ease-out around viewport center; direct pinch samples never animate. Persist final scale on successful end; cancel restores start scale/offset.
| Input / start region | Owner and acquisition | End / cancellation policy |
| --- | --- | --- |
| Vertical pan anywhere before edit lock | Timed ScrollView; 8 pt travel, abs(dy) ≥1.3×abs(dx) | Native inertia; cancel pending long-press, no event mutation |
| Horizontal pan in day surface | Day pager after 8 pt, abs(dx) ≥1.3×abs(dy); native back edge wins | One-day settle at 25% column travel or 500 pt/s; otherwise return; cancellation restores settled date |
| Diagonal / ambiguous pan | Remain undecided until dominance; at 16 pt choose vertical default | Never create/move/resize from unresolved movement |
| Tap on event | Event selection after press ends without drag | Open detail; cancelled tap emits nothing; overlapping candidates open selection list |
| Empty-grid long press then drag | Creation after 300 ms with ≤8 pt drift; 15-minute snapping | 30-minute initial ghost, ≥15-minute final duration; release opens editor, not persistence; cancel discards ghost |
| Event long press then drag (iPhone) / deliberate mouse drag | Move after 300 ms hold on phone; 4 pt mouse travel on Mac | Freeze pager/ordinary scroll; edge auto-scroll allowed; commit one valid mutation on release, cancel restores original |
| Selected event top/bottom handle drag | Resize; 6 pt visible handle inside nonoverlapping hit region | Minimum 15 minutes; no inverted interval; cancellation restores original, no outbox write |
| Trackpad pinch in Mac timed viewport | Zoom only, if no edit/pager/scrub owner | Never pages; editing rejects zoom until edit ends; cancelled zoom restores h0/s0 |
| Escape, focus loss, app background, source invalidation | Current interaction session | Discard uncommitted previews, stop auto-scroll, invalidate settlement IDs; committed durable edits remain intact |
- Implement the table’s absolute-value dominance comparisons directly, not multiple simultaneous gesture recognizers with independent mutations. Once acquired, ownership stays fixed until end/cancel; do not switch from a page drag to an edit mid-stream.
- Editing edge auto-scroll is limited to the top/bottom 32 pt of timed viewport, ramps linearly to 180 pt/s, stops immediately on exit/cancel, and reuses the same wall-time mapping; no hidden simultaneous day paging.
- Now marker: one assembly only in today's column: 1 pt red line, 5 pt dot at today's leading edge, one time label in gutter. Suppress adjacent hour label within 12 pt; other day columns get no false “now” line. Update once/minute without animation or automatic scrolling; announce only on explicit navigation.
- Preserve `CalendarTimelineScale`'s 24-hour wall-clock axis even on 23/25-hour days. Derive local day intervals with Calendar, never `+86400`; UTC instants, zone, and occurrence remain authoritative for persistence.
- Spring gap uses existing forward-preserving-minute policy (Berlin 02:30 →03:30) and editor disclosure before save; shade invalid band neutrally and label it once. Fall fold shares y position with overlap lanes; creation defaults earlier occurrence, editing preserves original occurrence and shows offset/zone distinction.
- Cross-midnight events split display segments at local day boundaries, retain one event identity plus occurrence/day-segment render identity, show continuation corners/labels, and edit the original interval. All-day end remains exclusive; midnight is 24:00 on previous display boundary, never a duplicate next-day event.
- States: live shows events; zero/empty known day says “No events” in day header/Agenda, leaves functional grid for creation; stale/partial show one sync status above viewport, preserve known events; error offers Retry there; no-store state must not claim an empty synced day. Pairing uses one focused sheet, not raw keys/transport details in the timeline.

## 8. Recovery, Fitness, Biology
- Owners: `ios/LifeOS/Modules/Fitness/FitnessView.swift` (`FitnessCoreTodayComposition`, readiness/metric/health/timeline cards); `FitnessCoreDetailDomain.swift` retains interpretation; `FitnessStressView.swift` owns stress detail; HealthKit projection/integration retain source fidelity.
- Recovery layout: page/date + one source status → readiness hero → Load/Sleep pair → Stress/Energy reserve pair → Nutrition summary → Health Monitor rows → recent activity. Keep this order; remove decorative accent rails and captions that merely repeat the section title.
- Hero: left label, 22/24 pt Mac/phone score and provenance; one concise factual context line. A meaningful ratio may use one 40 pt supporting ring only if it adds information. Observed height 112–144; unavailable collapses to a 56–72 pt cause/action row, growing for wrapped copy.
- Paired cards use two columns only at shared ≥720 content threshold; phone stacks at 88–112 pt per populated card, 56–72 unavailable. One 22/24 pt Mac/phone metric plus 40 pt sparkline when observations exist; ring/sparkline do not both repeat the same statistic. Details remain tappable where useful evidence exists.
- Live: show value, window, source and trend; valid zero: 0 and meaningful zero track; empty: labeled metric/reason; stale: retained value/time; partial: coverage; error: one source notice plus per-metric missing value, not repeated connect cards. Permission/calibration/unsupported states have distinct copy and only supported actions.
- Recovery primary action is Review sources when source-gated; otherwise Open recovery detail via hero. Supporting load/sleep/stress/reserve cards navigate; no competing filled buttons. Mac uses same focus/hover contract, chart arrow keys, and return-to-card focus.
- Stress detail layout: date/header → metric and coverage → subtype/range controls → 220/260 pt intraday plot → distribution → trend → provenance. Collapse missing subtype to one state row; don't repeat unavailable at hero, plot, and trend. Context contains factual source observations only; remove the Coaching product label and all advice actions under §19.10.
- Stress/energy estimates use green with Derived/Estimated labeling; observed stress series uses blue and textual source scale. No universal 0–100 normalization or thresholds unless the domain supplies them; missing coverage is a plot gap, never “rest”.
- Biology owner: `ios/LifeOS/Modules/Fitness/FitnessBiologyView.swift` (`FitnessBiologyDetailSurface`, biological-age/metric/detail/provenance cards); `ios/Shared/FitnessBiologyDomain.swift` owns eligibility and metric truth.
- Biology layout: unique page title → date row → range row on widths <480 → compact experimental disclosure → biological-age result/state → six source-defined body metric rows/cards → provenance. No leading decorative panel or horizontally clipped fixed grid.
- At ≥720 content width body metrics use two columns; below that one column, each intrinsic 88–112 pt with title, 24 pt value, unit, provenance and optional trend. Show all six labels by default; details hold longer history rather than a Show all expansion hiding basic structure.
- Biological age is secondary to evidence: 24 pt metric, `Experimental · 18+` label, source/coverage and non-diagnostic disclaimer. Domain-ineligible/no-model state is 72–104 pt with exact cause; never derive an age in the view or show zero years as absence.
- Biology states: live/valid zero retain source precision; empty shows available metric names with missing-source reasons; stale/partial preserve timestamp/coverage; error uses retry notice; age eligibility is independent of body-metric availability. Changing date/range clears obsolete selection, cancels stale requests, and preserves page scroll.
- Biology primary is Review sources only when needed; otherwise select metric for detail. Date arrows are 44/32 pt; long date and range never compete in one shrinking HStack. Detail sheet has one chart, coverage, provenance, and full accessible unit; no fixed-height clipping at AX sizes.

## 9. Nutrition and meal-review draft
- Owners: `ios/LifeOS/Modules/Fitness/FitnessNutritionView.swift` (`FitnessNutritionSurface`, macro/net-energy/timeline cards, `FitnessFoodReviewSheet`, editable fields); `ios/Shared/NutritionMealStore.swift`, `NutritionBarcode.swift`, `FoodPhotoPreparationCoordinator.swift` keep durable/source/request semantics.
- Nutrition layout: page/date + Add meal primary → orange calorie total/goal → macro rows (protein teal) → meal timeline → net-energy/quality → trends and hydration/lifestyle. Photo/manual/barcode/recipe/recent choices live in Add meal; no second giant capture panel mid-page.
- Hero 112–144 pt with 28/30 pt Mac/phone kcal, optional goal and coverage; macros are 48 pt rows with gram/percent selector. Meals use 64 pt rows, name/time/source/value; cap preview at five then Show all. Net energy requires intake and expenditure coverage; do not subtract a missing value as zero.
- Live: confirmed intake and source-backed expenditure; zero: explicit no intake logged in a valid local ledger, not a claim about actual eating; empty: “No meals logged” + Add meal in 72–104 pt; stale: timestamp; partial: incomplete macros/energy coverage; error: preserve saved meals plus retry/local-store status.
- Meal review layout: one 18/20 pt Mac/phone title + one-sentence context → source/estimate status → fields → optional preview/differences → local-save disclaimer → fixed footer. No nested card around entire form, duplicated navigation title, or huge warning slab above inputs.
- iPhone uses large detent, 16 pt gutters, body scrolls independently of footer/keyboard. Mac uses 520 pt preferred sheet width (maximum 600), 24 pt gutters; natural height capped at available−48/760. Footer 16 pt inset; Cancel/Close tertiary leading, Save meal prominent trailing (full-width primary below at AX sizes).
- Fields: Name full width → date/time → portion/basis when applicable → Calories → Protein → Carbohydrates → Fat. Labels above, text left aligned, units in trailing 32 pt slot; nutrition fields form two columns only with ≥440 pt inner form width, otherwise one column. Gaps 16 between fields/24 between groups.
- Review actions: `Keep manual only` tertiary, `Apply local preview` secondary, `Save meal` sole primary in brand blue. Keep manual and preview sit beside relevant source/preview content, never share primary emphasis; green denotes estimated data/success state, not a second save CTA.
- Draft owner is the presenting Nutrition flow, not ephemeral sheet-local copies: one stable draft ID, optional persisted meal ID, editable fields, source/proposal version, dirty revision, saved revision, request generation, and immutable save-attempt payload. Sheet binds to this draft; reopening within the flow retains edits.
| Action/state | Persistence | Dismissal / exact transition |
| --- | --- | --- |
| Keep manual only | No durable write or upload; retain typed fields; cancel proposal generation, source becomes manual | Return to parent manual editor with same draft; next Save still required |
| Apply local preview | Validate applicable fields; update in-memory preview/diff only | Stay in review; announce “Preview updated. Not saved.”; never set durable-saved flag |
| Save meal | Validate; persist snapshot keyed by stable meal ID + revision; new records reuse ID across retries | Stay open; show `Saved locally` and Close; refresh parent once after durable receipt |
| Saving / repeated tap | One in-flight attempt per draft revision; disable primary and editing during write | Duplicate triggers no-op; no second random ID, no optimistic success |
| Failed save | Retain edits, source, stable ID, revision and attempted payload | Stay open, inline failure + Retry save; never discard draft or claim success |
| Edit after success | Same meal ID; increment dirty revision; clear saved indicator | Next Save corrects existing meal through store; never adds a duplicate meal |
| Cancel/swipe/Escape with dirty draft | No write; existing persisted meal untouched | Ask Discard changes / Keep editing; clean/saved draft closes directly; restore invoking focus |
- Use store/domain deduplication as the authority, not just a disabled button. Reconcile existing persisted meal ID before retry after an uncertain receipt; persist receipt before showing success. “Sync pending” appears only when actual sync state says pending; local save never implies HealthKit/cloud write.
- Focus order follows field order; keyboard Next advances, Done dismisses keyboard; Return in numeric input does not save. Save validates and scrolls/focuses first invalid field; preserve comma/dot decimal parsing, ≤3 decimals, nonnegative kcal ≤5000 and macros ≤2000 per existing parser. Blank optional value remains nil, distinct from 0.
- Name is required after trimming. Errors appear directly below affected input and in a short footer status; don't use icon-only red outlines. Mac Command-Return invokes Save only if valid/current; selection and text cursor survive unrelated async state changes.
- Photo flow: explain destination/consent before Analyze, show 1–3 bounded thumbnails, preparing/uploading/review/error status, source/confidence/portion uncertainty, editable proposal and confirmation. Never replace user edits with a late result: accept only current draft/request/proposal generation; cancel work on discard/source change.
- Proposal-only values are green and labeled Estimated; calories label stays orange. No confidence invention, auto-confirm, chat, or generic advice. Barcode/manual/recipe keep their own source labels. Unavailable camera/barcode/photo route offers a working manual entry, not a dead sparkle button.

## 10. Widget system: every shipped kind and family
- Owners: `ios/LifeOSWidget/FutureModuleWidgets.swift` (shared chrome and module widgets), `CalendarWidget.swift`, `NextEventWidget.swift`, `UsageWidget.swift`, both widget bundles, and `ios/LifeOS/WidgetSnapshotPublisher.swift`. Publisher resolves availability/privacy; widget views format bounded snapshots only.
- One kind per existing function; retain identifiers/deep links. Use system content margins exactly once (16 pt design target; honor actual WidgetKit margin proposal), intrinsic layout and 12 gap. No extra app card inside the system card; transparent backing from §3 is the sole exception.
- Small = label → one dominant metric/ring → one status line; medium = one dominant left region (42%) and supporting right region (58%), 12 gap; accessory = one fact plus accessible state. No shipped kind currently declares systemLarge: mark large QA N/A with bundle evidence, do not silently add/remove families.
| Existing kind / supported family | Content hierarchy and destination |
| --- | --- |
| `CalendarWidget` / medium | Compact current-month Monday-first 42-date grid plus date/next-event summary; whole grid opens Calendar date, distinct + opens creation; today square uses black-on-white in contrast mode |
| `NextEventWidget` / small, iOS accessoryRectangular | Start/relative time → event title → day; opens exact occurrence; redacted hides title; Mac supports small only |
| `LifeOSUsageSmallWidget` / small | Provider/window → % remaining with single ring → reset/freshness; opens Usage window |
| `LifeOSWidget` / medium | Dominant provider hero/ring, second provider compact row; choose highest used%, then soonest reset, then name; opens selected provider |
| `LifeOSUsageLockScreenWidget` / iOS accessoryCircular | Remaining fraction/value, accessible provider/reset/state; opens Usage; no chart or tooltip |
| `NetWorthWidget` / medium | Qualified net worth → observed mini trend if available → source/freshness; opens Finance net worth |
| `SpendRingWidget` / small | Spent amount hero, budget fraction ring only with valid budget → range; opens Finance spend |
| `CashFlowWidget` / medium | Net amount → labeled income/expense pair and zero reference → range; opens Finance cash flow |
| `HealthMonitorWidget` / medium | Existing aggregate health summary → status/source; opens Fitness health monitor; do not conflate with detailed-kind identity |
| `RecoveryRingWidget` / small | Recovery value/ring only with valid scale → window/source; opens Recovery detail |
| `TasksWidget` / small, medium | Permitted pending count → aggregate status; medium adds bounded summary rows, never private task titles from summary permission; opens existing task/calendar destination |
| `NutritionOverviewWidget` / medium | Kcal/goal → protein/carbs/fat → at most two supported capture links; opens Nutrition; photo action is labeled Photo, not generic AI |
| `CaloriesMacrosWidget` / medium | Orange kcal hero → three labeled macro values with distinct palette; opens Nutrition macros |
| `NetEnergyWidget` / medium | Signed net-energy value → intake/expenditure and coverage; opens net-energy detail; nil operand yields unavailable |
| `DailyOverviewWidget` / medium | Recovery hero → load/sleep compact metrics → date/coverage; opens Fitness Today |
| `FitnessHealthMonitorWidget` / medium | Source-backed vital value/unit rows, max three plus count of additional metrics; opens detailed Health Monitor |
| `FitnessStressWidget` / medium | Stress value/source scale → bounded mini series → coverage; opens Stress date |
| `FitnessEnergyReserveWidget` / medium | Reserve value and domain-backed track → observation window/status; opens reserve detail |
- Calendar medium starts with 124 pt summary region only if grid can fit; derive remainder from proposed width, use 11 pt dates/12 pt metadata, never add duplicate week labels. Grid cells are not 44 pt individual buttons; grid is one date-opening link, + has its own nonoverlapping target where supported.
- All kinds apply six states: live = valid values; zero = real 0/empty progress; empty = one cause and open-app affordance; stale = retained metric + age; partial = qualified metric + coverage; error = cached truth + failure or compact unavailable. Fixed widget size remains stable; remove chart/ring marks when unsupported, not their honest state explanation.
- Additional privacy state: redacted = neutral lock + “Open LifeOS” with safe destination; no sensitive value/name in visual or accessibility output. Unavailable snapshot ≠ observed empty day. Existing app-group/source/permission gates remain explicit and are never inferred from placeholder entries.
- Actions only deep-link or invoke existing supported intents; no pretend button on unavailable capability. Small widget uses one destination; medium's distinct links must not overlap; accessibility names identify destination and current state. Timeline refresh cannot promise arbitrary animation or live second-by-second updates.
- All full-color widget families use the app semantic pairs; tinted families rely on readable system groups and redundant labels. Render zero rings as neutral track with 0, unavailable without a ring, estimated progress with `Est.`; never present missing values as a completed ring.

## 11. Motion and interruption contracts
- Owner: `ios/Shared/LifeOSMotionKit.swift` with token durations in `DesignTokens.swift`; reuse `LifeOSMotionLifecycle` settlement identity and chart presentation ownership. Timings below are targets; spring response is the response parameter, not a claim of exact wall-clock completion.
| Change | Normal contract | Reduce Motion |
| --- | --- | --- |
| Hover / focus | 120 ms ease-out fill/border; focus ring appears immediately; no lift | Immediate visual state |
| Press / release | Immediate pressed fill, 100 ms ease-out release; stationary geometry and labels | Fill state immediately |
| Selector highlight | Spring response 0.24 s, damping 0.90, blend 0; labels stationary | Instant selected fill |
| Page/card-to-detail | §19.6: fixed-shell Mac route host, 180 ms opacity/8 pt detail entry; native iPhone NavigationStack push/pop; no card-to-page zoom | 100 ms opacity only or instant; preserve direct back interaction |
| Chart first presentation | Final marks immediately; no decorative reveal or numeric interpolation | Same |
| Meaningful ratio ring | Final arc/value immediately; no sweep; Home/Usage hero rings removed | Same |
| Scrub / drag / pinch | Exact sample tracking, no spring; tooltip opacity in/out 80 ms, no positional lag | Same direct manipulation, no fade needed |
| Sheet entry/exit | Platform-native interactive transition; custom internal content 180 ms ease-out opacity, no stacked slide | Respect native preference; internal content immediate |
| Refresh / data correction | Existing content stable; replace values/paths without interpolation or reveal; status fade 100 ms | Immediate replacement |
| Calendar pager settle | Spring response 0.28 s, damping 0.92; finger-bound while dragging | Immediate settle after direct drag |
- No animation for identity/range/model changes, nil↔zero, privacy redaction, source invalidation, request errors, timestamps, validation, save receipts, changing permissions, axis-domain replacement, or Dynamic Type reflow. Selector highlight may animate independently of replaced data.
- Cancel/interrupt starts from current presentation, invalidates old completion token, and emits at most one commit/discard. Motion completion must never save data or navigate after the user has left. Reduce Motion changes mid-animation cancel work and settle to final state immediately.
- Charts/rings never replay decorative reveal; remove obsolete reveal timers when the last owned caller is migrated. Offscreen animation pauses; no continuous shimmer/glow/parallax, recursive timers, or looping breathing cards. One selection haptic per bucket change, rate-limited to 10 Hz on supported iPhone.
- Replace misleading `GlowRing`/ignored `hue` compatibility APIs with the actual final ring contract once all callers migrate; remove dead modifier/token cases. Native scroll and sheet gestures retain platform physics rather than layering a second animation over them.

## 12. Visual evidence, rejection gates, and implementation quality
- Capture evidence is required during implementation, not supplied by this document. Save named before/after PNGs and short recordings with exact build SHA/worktree ID, device/OS, viewport in points, appearance, content size, locale, time zone, state fixture/source, and expected result; fixtures must show `DEMO · NOT LIVE` and never enter normal data paths.
- Viewport set: P-small 375×667; P-modern 393×852; P-large 430×932; Mac-narrow 800×700; Mac-wide 1440×900. Every screen below: dark/light, default/AX3; P-small also AX5. Calendar Agenda supplies full-text AX evidence alongside the three-column timeline.
| Evidence ID / concrete capture | Required states and visible acceptance | Recording |
| --- | --- | --- |
| shell-Psmall / shell-MacWide | All visible routes selected/hover/focus; empty and stale module; equal icon boxes, one heading, preserved focus | Keyboard traverse, tab switch/back, deep-link return, interrupted navigation |
| finance-Psmall / finance-MacWide | Live, observed 0, no-source, filtered-empty, stale, partial, error; compact setup versus stable presented plot | All modes × all five ranges, vertical pan over graph, scrub, reset selection, refresh during inspection |
| usage-Pmodern / usage-MacNarrow | Live, 0 remaining, empty, stale, partial gaps, reset/error; blue actual/green estimate/neutral target | Provider/window switch, range escape, scrub boundary and keyboard arrows |
| calendar-Psmall-top/middle/bottom | 00:00, 10:00, 23:45–24:00 at 40/64/120 scale; three day columns, single labels/now assembly | Continuous scroll to final event, day paging, diagonal drag, edit cancellation, all-day create |
| calendar-MacWide-focal / MacNarrow | Pointer at 09:30 and 22:45 before/after min/default/max; one-minute focal tolerance except clamp | Real trackpad pinch, reverse/interruption, edit-vs-zoom rejection, density preset menu |
| calendar-DST-crossMidnight | Europe/Berlin 2026-03-29 gap, 2026-10-25 fold, 23:30–00:30 event; empty/stale/partial/error sync | Create gap, edit both fold occurrences, resize across midnight, cancel and verify no mutation |
| recovery-Psmall / recovery-MacWide | Live, 0, empty, calibrating, stale, partial, error; complete source copy and compact missing cards | Hero drilldown/back, stress scrub, focus return, refresh with stable geometry |
| biology-Psmall-AX5 / biology-MacNarrow | Eligible/experimental, ineligible, zero-valid metric, empty, stale, partial, error; six labels and disclosure unclipped | Date/range change, metric detail, scroll to provenance, keyboard navigation |
| nutrition-Pmodern / mealReview-Psmall-AX3 / mealReview-MacNarrow | Live/0/empty/stale/partial/error; manual/photo proposal, invalid input, saving, saved, save failure | Next/Done, keyboard footer visibility, manual/preview/save, duplicate tap, retry, edit-after-save, dirty dismissal |
| widgets-[kind]-[family]-[state]-[mode] | Every §10 kind at each declared family in live/0/empty/stale/partial/error/redacted; no invented large family | Tap exact destinations; supported quick actions and locked/unavailable routing |
| widgets-grey606060/808080/A0A0A0 | Each kind/family dark/light full-color and supported transparent/accented/vibrant modes at default/xxxLarge; 4.5:1 text | Real iPhone/macOS wallpaper and tint change, privacy redaction, retained stale snapshot |
- Repeat interaction recordings with Reduce Motion enabled: hover/press, selector, page/back, chart/ring entrance, scrub, sheet, Calendar scroll/pinch/cancel and refresh. Record 10–20 seconds each; native trackpad/hardware/widget behavior requires direct runtime evidence, not simulator-only claims.
- Signoff rejects the reported baseline if any Finance no-source plot reserves 220+ pt, unsupported plots still intercept input, duplicate unavailable blocks remain, navigation mixes symbol weights, Calendar repeats hour/day labels or hides late events, or blank tail exceeds its stated clearance.
- Signoff rejects white-on-grey widgets without proven contrast, blue-on-blue semantic collisions, non-green estimates, non-orange calorie identity, sparse oversized Recovery cards, clipped Biology/disclosure, hidden lower sheet fields, misaligned inputs, or multiple prominent meal actions.
- Signoff requires all state rows above evidenced; no clipped essential text at specified sizes; ≥44 pt phone targets; visible Mac focus/hover; VoiceOver meaningful grouping and error announcements; one save receipt per revision; restoration and gesture cancellation verified; Advisor absent and calorie-photo flow retained.
- Performance: scrolling/scrubbing budget ≤16.7 ms/frame at 60 Hz and ≤8.3 ms on 120 Hz hardware; inspect Instruments for hitches >50 ms. No network, aggregation, image decoding, file I/O, or formatter construction in SwiftUI body/gesture sample callbacks.
- Build immutable display models once per source revision: one-pass O(n) totals/grouping; dictionary lookup for selected IDs; sort once O(n log n) only where chronological order is needed. Ban nested unbounded filter/first scans per row/day and string-joining whole datasets as animation identity.
- Bound work: page transaction/meal lists in batches of 100, render lazily; Calendar materializes at most seven day columns around the three-day pager plus visible-month cells, caches indexed day occurrences, and windows dense timelines; widget rows follow §10 caps. Never truncate authoritative stored data silently.
- Usage retains its 240-point observed render budget; other chart surfaces have a maximum 512 samples/series unless the existing contract is stricter. Split gaps before decimation; preserve extrema, segment endpoints and singleton observations. Inspect raw indexed observations with at most one additional selected-point overlay. If endpoints exceed the budget, report reduced visible coverage using an existing aggregation policy; never bridge missing intervals. Preserve raw observations for domain calculations. Bound thumbnails to three per meal and decode to display dimensions off-main; use existing retention policy.
- Stable identity uses source record ID + occurrence/window where necessary, never list index, formatted label, or fresh UUID in body. Scope tasks to revision/route; cancel stale work and validate generations before publishing. Avoid AnyView erasure in repeated rows when generic composition suffices.
- Implementation workers must inspect current dirty files before edits, keep assigned scopes disjoint, remove dead APIs/files/resources with all references, and produce focused logic/UI/snapshot evidence for changed behavior plus unsigned iOS/macOS/widget builds. Never suppress warnings, weaken assertions, or count old test runs as new evidence.
- Final visual approval is separate from bank consent, HealthKit/Zepp hardware, Personal Team/App Group signing, WidgetKit system rendering, and Windows/Tailscale runtime approval. Record exactly which are observed; this planning document certifies none of them.


## 13. Inspection ledger and screenshot-to-implementation contract

Historical inspection ledger: retain its domain/gesture requirements, but use §19 wherever typography, geometry, icon, motion, evidence, or dispatch rules conflict. Where `tasks/design-overhaul-plan.md` says all empty states reserve populated-chart geometry, §6 wins: a never-connected screen is compact; an already presented chart retains geometry through refresh/filter-empty. This is a presentation correction, not a new source/consent policy. Line anchors below describe the inspected revision and will drift; resolve the named symbol before editing.

| Evidence | Visible defect / reported behavior | Exact implementation target | Reject the replacement if |
| --- | --- | --- | --- |
| Screenshot 1, Finance | Equal-weight unavailable cards, redundant account/update/source facts, three stacked control bars, large blank chart, isolated category box | `ios/LifeOS/Modules/Finance/FinanceView.swift`: `FinanceHeroCard` (around 987), `FinanceDetailChartCard` (1373), `FinanceResponsiveLayoutContract`; mode controls in `FinanceChartModeViews.swift` | No-source repeats the same cause across hero/plot/categories/metrics; unsupported Line/Bar/Ring intercept input; sparse page still reserves a 220 pt plot; two values compete for hero emphasis |
| Screenshot 2, sidebar | Selected Finance icon and rail crowd each other; icon geometry/weight feels inconsistent | `ios/Shared/LifeOSIcon.swift`, `ios/LifeOS/Modules/ModuleNavigation.swift`, both app shells | Any visible route uses a custom glyph, per-module selection hue, different symbol weight/box, clipped rail, or invisible keyboard focus |
| Screenshot 3, Calendar | Five repeated 08:43 labels/dots on a full-width now line; user reports timeline cannot scroll down | `ios/Shared/CalendarViews.swift`: `CalendarTimelineView`, `CalendarDayTimeline`, shared gutter; `CalendarLayout.swift`; parent `CalendarView.swift` | Future/non-today columns show now markers; 23:45 event is unreachable; pan on an event starts a move before deliberate edit acquisition |
| Screenshot 4, Mac chrome | Unexplained slider-like expansion control, awkward stacked toolbar, absent expected pinch interaction | `ios/LifeOS/CalendarView.swift`: `monthExpanded`, month chrome and `toggle` path; timeline zoom bridge in `CalendarViews.swift` | Primary month expansion depends on a slider; header pinch changes time density or timeline pinch changes month mode; pointer focal time jumps |
| Screenshot 5, Recovery | Empty giant readiness panel, duplicate availability sentence, color rails and captions competing with values, empty rings | `FitnessView.swift`: `FitnessCoreTodayComposition` (1538), `FitnessCoreReadinessHero`, `FitnessCoreMetricCard` (1913), `FitnessCoreColumns` | Shared cause repeats per card; unavailable metrics render rings/sparklines; blank hero remains taller than its useful content; pair grid clips at narrow width |
| Screenshot 6, Biology | Competing oversized title/date spinner; truncated experimental copy; panel wider than available content | `FitnessBiologyView.swift`: `FitnessBiologyDetailSurface`, `biologyHeader`, `biologyRangeControl`, `FitnessBiologicalAgeCard`, `FitnessBiologyPresentationPolicy` | Embedded mode duplicates date/title/scroll ownership; disclosure or units clip; absent biological age appears as numeric zero |
| Screenshot 7, meal review | Generic Review/Recipe/Manual titles, nested form card, ragged inputs, three competing actions and implementation prose | `FitnessNutritionView.swift`: `FitnessFoodReviewSheet` (2968), `sheetFooter`, `FitnessNutritionFormGrid`, field editors | More than one filled primary; values align by varying label lengths; keyboard obscures final field/save; preview claims saved; raw storage terminology overwhelms the form |

Inspected source already has SF Pro `RoleModifier` with scaled metrics; responsive Finance contracts; Biology embedded/standalone ownership; source-reason suppression in Recovery; meal keyboard dismissal, a safe-area footer and dirty-dismiss handling; Calendar zoom session logic; deterministic motion lifecycle; transparent widget backing. Preserve these useful behaviors and test their integration. Do not rewrite already-correct domain logic merely to match a new component name.

Historical drift observations here previously named target-color, spacing, target-size and module-layout mismatches. They are not a current work queue: Luna reports the required shared tokens already exist. Ticket A checks actual values against §§19.2–3 and returns only demonstrated discrepancies; module owners inspect callers. Do not replay old token migrations or Settings refactors from this ledger.

Migration decisions: use existing `lifeOSTypography(_:)` and `LifeOSResponsiveContentContainer` as canonical implementations; `LifeOSPageFrame` in §4 is a semantic contract, not a requirement for a second wrapper. Fold required header/viewport behavior into one facade. Keep a forwarding compatibility wrapper only while active callers require it, then remove it in the owning tranche. Set canvas to existing brand black `#000000`. Add separate `primaryActionFill` (blue600), `onPrimaryAction` (white), `linkForeground`, and `focusStroke` roles: do not repurpose adaptive `accent` indiscriminately. Blue300 chart marks, blue600 button fill, and blue100/900 selection must not become adjacent similar-value series. Protein stays teal. Use neutral dotted targets beside green dashed estimates.

## 14. Overview: useful daily entry point

Owner: `ios/LifeOS/OverviewView.swift`, especially `regularDashboard`, `noSourceDashboard`, `OverviewSupportingLayout`, `OverviewMetricCard`; navigation/data supplied by existing shell and coordinators. Preserve existing section preferences and stable IDs. The current implementation leads with factual Usage; retain that default and do not silently add an unimplemented agenda or reorder saved preferences. Calendar remains one action away in the persistent navigation. An existing configured Calendar entry must open the actual Calendar date, not a new preview-only planner.

Hierarchy: unique Home title and date → one page-level connection notice only when required → existing lead Usage summary → existing Finance/Fitness/Clipper supporting summaries in their saved order → existing secondary destinations. Header subtitle is optional and removed when it repeats the title. On compact content stack cards; at 720–959 use two supporting columns; at ≥960 use three only if each is ≥300 pt after gaps, otherwise two. Lead card spans the content width; no forced equal height between cards of unrelated content. Card target is 112–144 pt populated, intrinsic 72–96 absent; these are default-size guides, never fixed heights. Show a maximum of three meaningful facts per summary and one destination link. Avoid a nested button inside a tappable card; separate any quick action as a sibling control.

| Overview state | Hierarchy/content | Action / retention |
| --- | --- | --- |
| Loading, never observed | Header + one progress row; available local content remains visible | Existing module navigation stays usable |
| Unavailable, all remote sources absent | One setup statement + compact module rows; no repeated empty graph per module | Open Settings; local Calendar and saved meals remain accessible |
| Live | Real lead value, source/window, then bounded supporting facts | Card opens exact existing detail and restores focus on return |
| Zero / known empty | Real zero with its unit, or “No entries for this period” | Keep destination useful; never label a valid zero disconnected |
| Partial | Display available summaries independently; identify missing coverage next to affected aggregate | Review affected source; do not paint the entire dashboard unavailable |
| Stale | Last values with explicit last-updated time per independently dated source | Refresh retains cards and scroll position |
| Error | One operation notice plus retained truthful cards, or compact failure if none | Retry affected coordinator only; no whole-dashboard spinner |
| Redacted | Hide sensitive numbers, mini charts and accessibility values immediately | Safe module link, no tooltip leak |

No chart may imply a combined freshness guarantee for independent sources. Use existing indexed display models; do not sort provider history or recompute all summaries inside a hover callback. Evidence: Home at no-source, one-connected-source, all-observed, zero, stale, failed-refresh and redacted; verify returning from every card and a refresh during keyboard focus.

## 15. Settings: connection tasks without implementation noise

Owner: `ios/LifeOS/Settings.swift`: `SettingsView` (1244 body), `SettingsHubCard`, `ProviderConnectionsSettingsView`, `FinanceConnectionsSettingsView`, `RetainedHealthDataSettings`, Health settings and sync/privacy detail surfaces. Reuse existing recovery actions and diagnostic sanitization. Source-status facts are not permission to expose internal identifiers or secrets.

Hierarchy: Settings title → Connections group (Bank accounts, Apple Health, Usage, existing Clipper connection) → Sync & devices → Privacy & data → existing app preferences. Preserve existing destinations/availability; do not add controls for unsupported background sync or signing. Root is a readable grouped row list, maximum 600 pt; each row label + status + disclosure, 56 pt minimum, 12 inset, grows with wrapping. Detail pages use the same 600 pt form width, section gap 24. On Mac list/detail may share the existing navigation container only when each column fits; below 900 window width use pushed detail. There is no 390 pt minimum grid cell on a narrower phone.

A connection detail starts with friendly source name, last successful read, one state sentence and one valid recovery action; then account/device coverage; then optional expanded diagnostics. Replace “reviewed source”, “durable local storage”, “authority chain”, and “gateway preflight” in primary user copy with task language. Keep precise technical facts in explicitly opened diagnostics, scrub secrets before rendering/copying. Example meal receipt is “Saved on this device”; connection copy is “No account data received yet”, not “fully connected” merely because configuration exists.

| Settings state | Required presentation | Action |
| --- | --- | --- |
| Not configured / unavailable | Source name + concrete missing prerequisite, no success indicator | Open the supported setup path; external step shown as instructions if not executable here |
| Loading / refreshing | Existing status remains; Updating indicator in reserved action slot | Coalesce repeated refresh; keep navigation usable |
| Live | Last successful observation and source coverage | Quiet Refresh; connection management secondary |
| Empty after successful read | “No records returned” with timestamp, distinct from access denial | Review source filters/settings |
| Partial / read-indeterminate | Named missing categories; explain that empty Health reads cannot prove permission denial | Open Apple Health guidance or existing permission action as applicable |
| Stale | Last success timestamp and age, no green Connected assertion | Refresh/recovery |
| Error | Specific recoverable cause and retained last success; expand diagnostics on intent | One Retry; preserve form edits and current focus |
| Revoked / restricted / protected data unavailable | State the actual restriction; remove inaccessible values | Supported system settings/unlock instructions, never an endless reconnect loop |

Pairing/revoke, export/delete, and diagnostics remain scoped sheets. Show device name and human-readable confirmation material in pairing; never keys or raw frames in routine chrome. Destructive removal names affected device/data and consequence; cancel is default. A failed connection change preserves current values and error text. Command-Return submits the current valid form only; Escape closes transient detail or asks about unsaved edits. Verify narrow Mac keyboard traversal and the existing failing Calendar/Settings UI paths before visual signoff.

## 16. Calendar expansion, state transitions, and geometry refinements

The two Mac pinch tasks are separate. Pinch that BEGINS over the month/date header or expanded month region expands/collapses month presentation; pinch that begins inside timed rows changes hour density using §7 focal mapping. Freeze region ownership at gesture begin and keep it through boundary crossing. Reject either while event editing/paging owns the calendar. The visual slider shown in screenshot 4 is removed; retain a labeled chevron/disclosure fallback for Expand month/Collapse month.

Header pinch: capture starting expansion and factor 1.0; outward ≥1.12 requests expanded, inward ≤0.88 requests collapsed; dead band leaves state unchanged. One state transition per gesture, with 240 ms ease-out layout settlement after release; reversing before release recalculates the pending target from the captured start. Cancel restores the captured state without changing selected date or scroll anchor. The fallback uses the same transition and accessible expanded value. This is a LifeOS interaction decision, not a claim that this threshold reproduces Notion internals. Prove direction/threshold feel with a real Mac trackpad before approval.

| Transition | Layout and ownership invariant | Evidence |
| --- | --- | --- |
| No stored snapshot → first load | One compact sync notice above fully usable local calendar; no fabricated empty-server assertion | Create a local event while remote read is unavailable |
| Local/live → refresh/error | No recentring, density reset, day change or temporary disappearance of committed events | Refresh at 23:45 while an event is selected |
| Partial/stale → live | Replace by stable event identity; retain viewport/selection if record remains | Same event before/after; new data cannot finish a cancelled drag |
| Local save → pending sync → acknowledged | Event appears after durable local receipt; one subtle pending icon/status in detail, no repeated page banners | Retry cannot duplicate event; remote errors cannot claim synced |
| Empty day → creation | Grid remains interactive; no full-viewport empty overlay | Vertical/diagonal pan does not create; deliberate creation does |
| Month expand → collapse | Preserve selected date and wall-time anchor; only header allocation changes | Before/after focal anchor with small and tall all-day areas |

On iPhone the native leading-edge back gesture wins where a pushed Calendar surface has back navigation. Do not install a global magnification recognizer above sheets, lists, or editor fields. Any retained iPhone pinch is limited to timed viewport and follows the same owner exclusion; no accidental calendar zoom while navigating a photo. Scroll-to-field in an editor executes after keyboard/layout measurement and only for user focus/validation, never every render.

## 17. Dispatch migration

The former A–H dispatch is retired. §19.9 is the current primary dispatch, with sequential follow-on coverage in §20.2: A foundations, B shell/Home, C Usage, D Finance, E Calendar, F Recovery/Training, G Biology, H Nutrition, I Widgets, J evidence. Do not execute historical worker queues or repeat already-correct foundation/Calendar work without a demonstrated mismatch. Settings/Tax retain their contracts and use their separate §20.2 follow-on scopes; they are not added to B–I scopes.

## 18. Evidence ledger and honest completion boundary

Add Overview and Settings to §12's capture matrix: `overview-[viewport]-[state]` and `settings-[connection]-[viewport]-[state]`, covering every row of §§14–15. Add `calendar-month-pinch` and `calendar-timeline-pinch` as separate recordings. Capture actual iPhone 17 simulator logical dimensions rather than treating the generic 393×852 fixture as a measured device specification. Sweep content widths 359/360, 479/480, 719/720, 959/960 and maximum 1040; also test Mac window 899/900 and inspector 1179/1180. At AX sizes intrinsic content growth overrides nominal card heights. Test de-DE long dates, comma decimals, large money values and Europe/Berlin time.

Acceptance packet must identify app binary path, build identifier, capture time, selected source/window and freshness. Screenshots contain sensitive personal data: keep originals local; redact copies before public GitHub evidence. For reproducible unavailable/partial/error states use isolated visual fixtures labeled `DEMO · NOT LIVE`; supplement them with authorized live readback in the actual app. No fixture may publish widgets, sync or replace persisted personal records.

For each screenshot defect, reviewer compares the replacement against the same approximate viewport/content state and records pass/fail with a reason. Still images establish hierarchy/contrast/clipping; recordings establish scroll/pinch/keyboard/interruption; domain tests establish invariants. None substitutes for the other. Pixel contrast must be measured on final composed colors, including pressed/selected and wallpaper modes; numbers in this specification are targets until measured. Performance budgets in §12 require Instruments on the actual build; no claim of optimal complexity from inspection alone.

Unverified in this planning pass: current app rendering, actual Mac trackpad feel, all screenshot-state permutations, real WidgetKit clear/tinted contrast, UI test repairs, installed SF Symbol availability across deployment targets, physical iPhone HealthKit/Zepp data and authorization, bank consent/readback, Windows runtime, photo accuracy benchmark and personal signing/renewal. Historical green logic tests do not certify these. Reference observations are reconciled in §19.1; both sites supply inspiration, not runtime evidence. Original Bevel/Revolut/Figma functional evidence has not been re-audited here and remains a separate acceptance input under existing instructions.

Advisor/generic AI removal is a product boundary across routes, intents, providers and UI. This pass inspected relevant navigation but does not certify repository-wide absence. Implementation review must audit all product references and preserve factual Usage monitoring and calorie-photo estimation only; `.assistant` used as a Usage-provider icon should become `.usage` in that context, not a reason to delete provider consumption data. Do not introduce chat, generated life advice, coaching, a replacement Advisor label, or scheduling infrastructure. Completion means everyday workflows are visibly and behaviorally proven, with remaining hardware/provider constraints explicitly recorded rather than hidden behind polished placeholders.

## 19. Corrective visual contract — reconciled 2026-09-12

### 19.1 Current evidence / release meaning
- Baseline: direct source inspection at 996ccf4, branch lifeos-foundation-checkpoint-20260812, clean before this edit. Coordination records Usage at 7877ec5; this pass does not independently recertify tests or later commits. Resolve historical anchors against each worker’s checkout. Calendar source/current decisions agree on 40/64/120; Usage caps observed rendering at 240. This pass does not certify a running app.
- Evidence labels are separate: **source present** = inspected implementation; **tested** = named test result at an identified revision; **visual accepted** = opened captures reviewed at known scale; **interaction accepted** = runtime recording/trace; **live accepted** = authenticated real-source read in the normally launched app. A source/security GREEN establishes none of the other labels. Missing runtime is recorded UNVERIFIED, never PASS or evidence that a feature is absent.

| Area | Current or explicitly historical source evidence | Next acceptance evidence |
| --- | --- | --- |
| Foundation | `Typography.swift` already resolves native SF Pro; tokens/motion and `LifeOSIcon` catalog exist | Correct caller density, semantic icon coverage, composed contrast and component captures; do not rewrite working primitives |
| Shell/Home/Usage | Existing LifeOSMacSceneState/LifeOSMacRouteSnapshot, OverviewView and UsageView; coherent Usage packet and fixture paths exist; charts distinguish observations/estimates/gaps | Current-build Home/Usage hierarchy and interrupted route recordings; normal launch and live refresh |
| Calendar | Finite timed scroll, magnification path, today-only now marker; screenshot slider absent from inspected source; geometry tests exist | Current scroll to 24:00, header/timeline pinch, cancellation and return at late-day anchor; do not reimplement fixes merely because old screenshots fail |
| Health/meal | Training templates/sessions/sets/history exist; Nutrition has scrolling sheet body, fixed footer and local persistence | Compact layouts, training interaction and meal keyboard/save evidence; source presence does not certify Zepp fidelity |
| Widgets | 60% black inner backing, 8 pt padding/12 radius; fixture/live separation and snapshot tests exist | Actual WidgetKit modes on grey wallpaper; offscreen tests cannot certify system compositing |
| Product boundary | No production generic Advisor/chat found in handoff; visible source-authored Coaching remains in Fitness and Stress | Remove Coaching product sections or retain strictly factual data under “Context”; no recommendations, generated advice, or replacement advisor route |

- All seven original screenshots and the later Home/Usage screenshots remain rejected qualitative baselines. They may represent older binaries or fixture paths; record build/launch identity before attributing them to current source. Their unknown crop/scale cannot establish logical-point measurements or animation performance.
- Reference pages fetched for this revision: Clean supports whitespace hierarchy, one primary action, consistent primitives, three neutral surface levels, explicit accents and complete hover; it warns against empty composition, excess greys and generic blue treatment. Infinite supplies roaming/zoom/reset/lazy-tile inspiration only for an appropriate canvas. Fetched pages establish controls/principles, not native timing or visual parity. Do not copy branding/code or add web dependencies; measurements below are LifeOS decisions.
- Retain time/DST/editing, consent, privacy, receipt and source-fidelity rules. Explicit user requirements win. No overall completion percentage follows from this plan; §19.11 records the remaining independent release evidence.

### 19.2 Platform typography and spacing
Use only the existing `LifeOSTypography` system-font facade. Resolve platform at the facade, with semantic roles at callers; no view scaling, named SF font files, rounded face, or arbitrary `.font` overrides. Mac default density is independent of the iPhone content-size anchor. iPhone respects Dynamic Type through existing scaled metrics; preserve user-selected larger text on Mac. Values below are default sizes in points, not caps on enlarged text.

| Role | macOS | iPhone | Weight / rule |
| --- | --- | --- | --- |
| pageTitle | 22 | 24 | semibold; exactly one, tracking −0.3 |
| sectionTitle | 15 | 18 | semibold; use only for a real group |
| cardTitle | 14 | 16 | semibold; no oversized card subtitles |
| body / editable input | 13 | 17 | regular; natural system leading |
| label / button | 13 | 15 | label medium; button semibold as existing facade; emphasize with fill, not oversized type |
| metadata / chart ticks | 12 | 13 | regular; essential copy never below these sizes |
| metric | 28 | 30 | semibold, tabular digits, tracking −0.4; at most one per screen |
| metricCompact | 22 | 24 | semibold, tabular digits; Home summaries use this ceiling |
| inline monitoring value | 20 | 22 | semibold, tabular digits; Usage window summaries |

- The inlineMonitoringValue role already exists; reuse it in the same facade. Mac form body must not inherit old 17 pt inputs. Units are metadata, baseline-aligned, 4 pt gap; never manually offset using bottom padding. Labels are sentence case. Brand lockup is `LifeOS`, 14 pt semibold on Mac, without a house illustration or oversized uppercase wordmark. Home's page title is `Home`.
- Spacing remains 4/8/12/16/24/32/48. Mac page gutter 24, phone 16, major group gap 24, related controls 8, summary facts 12, cards 16 inset. Home/Usage card corner 12; existing generic 16 pt radius may remain for content-heavy detail cards and sheets use native geometry. No per-screen 18 pt inset patches.
- Mac sidebar default 208 pt, user-resizable 184–240; collapse to 52 below 900 pt outer window width, preserving chosen expanded width. Header toolbar 44 pt, sidebar row 32 pt with 4 pt inter-row gap. Content measurements exclude sidebar, dividers, toolbar and safe areas. Reading surfaces max 1040 pt, forms max 600, Calendar uses viewport width. At widths above max, center the whole content column rather than stretching individual cards.
- Layout breakpoints refer to available content: <720 compact; 720–959 regular; ≥960 wide. Grid gaps 16. Below 720 stack; regular two supporting columns of ≥300; wide three only when each ≥300. No horizontal page scrolling except deliberate Calendar/canvas surfaces. Intrinsic heights win when text wraps; default geometry budgets are assertions for default-size English fixtures, not fixed clipping frames.

### 19.2a Canonical dimensions and surface grammar
The following resolves earlier generic geometry; all values are logical points at default text size. Heights are minimums or target content budgets, never clipping frames. Respect native sheet geometry where it is system-owned.

| Primitive | macOS | iPhone | State/layout invariant |
| --- | --- | --- | --- |
| Page/card | Gutter 24; max reading width 1040 | Gutter 16; available width | One vertical scroll owner; Calendar alone owns a finite viewport |
| Card | Padding 16; Home/Usage radius 12, detailed card 16 | Same | One surface and hairline; intrinsic height; no nested form card or blank plot reservation |
| Status | 20 icon box, 12 gap; 56–72 row or 72–104 multiline cause/action | Same, action target ≥44 | One shared absence notice; independent causes may be separate compact rows |
| Button/selector/input | Height ≥32, radius 10; input 13 pt | Height ≥44, radius 10; input 17 pt | Button horizontal padding 16; input 12; labels above with 8 gap, errors below with 4; no label-dependent field alignment |
| Navigation | Sidebar 208 (184–240), collapsed 52 below window 900; row 32, gap 4, toolbar 44 | Native tab safe area; tab target ≥44, internal list row ≥48 | §19.3 glyphs; no selection rail or animated symbol replacement |
| Editing sheet | Preferred width 520, max 600, lower bound min(420, available−48); height ≤min(760, available−48); gutter 24 | Available width, large editing detent, gutter 16 | Title 18/20 Mac/phone; body scroll; footer padding 16 and ≥32/44 action height plus system safe area; one primary Save |
| Form grid | Numeric columns only with inner width ≥440, gap 16 | Same width rule, otherwise one column | Name full width; label above; text/entry leading and unit trailing; no entire-form enclosing card |

- Exactly three neutral fills: dark canvas `#000000`, surface `#131315`, raised `#1B1B1E`; light canvas `#FAFAFA`, surface `#FFFFFF`, raised `#F4F4F5`. Sidebar uses canvas, selection uses raised, floating surfaces reuse surface/raised. Borders/text remain the §3 roles; do not add near-identical grey fills per screen.
- §19.3 owns accent semantics, §19.2 owns platform type, and §§19.4–7 own surface-specific height budgets. These tables override legacy 36 pt heroes, 17 pt Mac inputs, 560 pt sheets and wider sidebar defaults. Extend an existing primitive only for a verified mismatch; apply caller corrections first.

### 19.3 Icons and color discipline
- One monochrome SF Symbol family: Home `square.grid.2x2`, Calendar `calendar`, Finance `creditcard`, Fitness/Recovery `waveform.path.ecg`, Tax `doc.text`, Usage `chart.xyaxis.line`, Clipper `rectangle.stack`, Biology `point.3.connected.trianglepath`, Nutrition `fork.knife`, Workout `dumbbell`, Settings `gearshape`, More `ellipsis`. Actions: Back `chevron.left`, disclosure `chevron.right`, search `magnifyingglass`, refresh `arrow.clockwise`, add `plus`, close `xmark`, date expansion `chevron.down`. Use exact existing deployment-target availability checks; Biology fallback `circle.grid.2x2`, Workout fallback `figure.walk`, Usage fallback `chart.bar`. Validate in symbol-catalog captures; do not silently show missing symbols.
- Mac navigation glyph 15 pt regular in 20×20 box; card glyph 14 regular in 20 box; toolbar glyph 14 medium in 18 box. iPhone navigation glyph 18 regular in 24 box, toolbar 17 medium in 24. Glyph size is owned by `LifeOSIcon` semantic context, never by an outer `.frame` shrinking its existing fixed 24 pt inner box. Labels align on optical center with 8 pt Mac/12 phone gap. Keep native SF Symbols; correct mapping, weight and optical sizing within that family.
- Neutral unselected icons; selected glyph uses brand blue300 dark/blue600 light, selected label primary, selected row neutral raised fill. Remove the bright selection rail and saturated full-row blue slab. No glow, gradients, filled/outline swaps, arbitrary module colors or icon animation on navigation. Detailed data marks retain semantic colors; every card does not need an icon.
- Icon-only allowed for universally recognizable toolbar actions with tooltip, accessible name and visible keyboard focus. Primary creation/save actions, source recovery and ambiguous module links retain text. Mac targets ≥32×32, iPhone ≥44×44; visual glyph and hit target are distinct. Focus must not resize layout.
- Palette remains exactly the user's blue ramp from `colors.md`: action fill #0253C4, observed marks #5DA0FD dark/#0253C4 light, informational link #B8D5FE dark/#013174 light. Do not present action blue and observed blue as competing adjacent series. Estimates green #60D386 dark/#01773B light, dashed; calories orange and protein teal retain §3 roles. Targets neutral dotted. Limit surfaces to canvas/surface/raised; retain composited contrast rules and grey-widget backing from §3.

### 19.4 Home: concise daily overview
- Header = Home, date as 12/13 pt secondary text, quiet refresh action. Remove “A quiet view…” when it adds no actionable context. Header content height 44–56 Mac at default text. Use one small `Demo · not live` badge in the fixture toolbar; do not append a shouting warning to the page subtitle or repeat it per card. Fixture identity remains unambiguous and in capture metadata. Production must not start with fixture flags or fall back to demo when disconnected.
- Lead Usage summary is 112–144 pt at default text: title/provider/window row; 22/24 pt remaining value; reset/freshness inline metadata; optional real sparkline 36 pt high. No mini-ring row, no absent-provider dashes, no duplicated 58 and 42 presentation. Provider comparison is a destination detail; missing configured sources use one compact status, never decorative vacant rings. If no actual trend exists, omit the sparkline and its reserved height.
- Supporting cards: 112–144 pt observed, 64–88 unavailable at default text; ≤3 relevant facts, metricCompact ceiling, one destination affordance. No promotional subtitles, duplicate source statements, nested tappable controls, or forced equal-height empty cards. Health can expose further metrics in detail; don't discard underlying functionality. A never-connected Home groups shared setup reasons once and leaves local Calendar/meal workflows usable.
- At 1280×800 Mac, Home header, lead summary and first supporting row must be entirely visible in the viewport. At 800×700, lead and first supporting card must be visible without horizontal clipping. At 393×852 phone, title, lead and at least the first supporting card are visible above bottom chrome at default text. More content scrolls naturally; do not force-fit AX text. Respect saved section order and restore card/scroll focus after detail.

### 19.5 Usage: operational monitoring, not a showcase dashboard
- `UsageView` in `CodexView.swift` is the screen owner; do not create a parallel Usage screen. Layout order: fixed-shell breadcrumb/back + Usage title/provider menu + refresh; concise source timestamp; window summaries; chart controls; observed plot and inspection; factual details. Remove separate jumbo hero, limits ring card, duplicated honesty banner, all-caps metadata strip and the Graphs/Facts/Insights pill row.
- Window summaries: one row per actual provider-defined window, 64–76 pt high with label, 20/22 pt `% remaining`, reset time/countdown and a 4 pt progress track. At ≥720 content width, two windows sit side-by-side; compact stacks them. Explicit track caption `Used` avoids confusing the remaining numeral with the filled fraction. Unknown = em dash + cause, never a zero-length observed bar. Extra windows use the same rows. Zero remaining is exact 0%; no celebratory state from invented thresholds.
- Keep provider selector to intrinsic 160–240 pt on Mac and full-width when required on phone; title and refresh remain adjacent. `Last updated …` belongs once below controls or beside provider when it fits. Banked resets appears in Facts only when supported or explicitly requested; no permanent giant unavailable column. Stale/error retains previous values with a concise reason and one recovery action.
- Chart toolbar is one leading-aligned group: metric menu (`Remaining` / `Token activity` when supported), period selector with actual labels, and optional factual-detail button. Mac 32 pt controls, phone 44; gaps 8. No placeholder `GRAPH`/`RANGE`, huge equal-width buttons, nested capsules or trailing acres of blank space. At <560 content width wrap into two labeled rows; do not shrink labels to fit.
- Plot height 224 Mac compact/regular, 256 wide, 200 phone; four y ticks maximum, three x ticks under 360 plot width otherwise five. Reserve axis label width using measured formatted maximum, not a guessed trailing margin. Units once at axis/title. Leading chart title 14/16; legend 12/13 wraps below. Plot appears by y=300 within Mac content at 1280×800 and by y=420 phone at 393×852 in the two-window default fixture.
- Remove ring status text entirely from this page. Hover/scrub inspection is a fixed 36 Mac/44 phone row below plot with time, value, source and observed/estimated state; grows for long text without truncating values. No overlay may cover the controls. Maximum tooltip width 220 with 8 pt plot-edge clearance; fall back to inspection row for long text.
- Facts is a disclosure/list below plot, 28–32 pt Mac rows/44 phone, wrapping values. Existing model-mix/activity information is factual detail, not an empty Insights promise and not generated advice. Sparse/no-source state is a 72–104 pt setup row and supported facts; no axes, ring or reserved plot. A previously populated filtered-empty/refreshing chart retains geometry with clear cause and an escape to a valid range.
- Same observed model append/value refresh retains exact inspected point ID; removal clears it, never selects latest. Model identity = provider/account/window epoch/metric/provenance, not every point ID. Dataset revision is separate. Reset/provider/window changes invalidate previous estimates and inspection. No artificial combined provider percentage, history interpolation across gaps or estimate past reset.
- Acceptance: full provider/window/value/reset readable at every width; no clipped `UNDER NOR…`; no horizontal overflow; selected values agree with source; primary chart visible within the stated budget. Long German labels and values must wrap or select a labeled menu, never ellipsize essential state.

### 19.6 Navigation and direct manipulation
- macOS: retain sidebar/toolbar and one bounded content host. Represent Home/detail route explicitly with scene-owned presentation state and an outgoing/current route snapshot. A ZStack aligns both route layers top-leading in the same measured viewport; incoming detail opacity 0→1 and x 8→0 over 180 ms ease-out; outgoing opacity 1→0 over 120 ms with no scaling. Back reverses the 8 pt direction. Keep chrome fixed, outgoing layer noninteractive/accessibility-hidden immediately, and remove it on current-generation completion. At most two route layers; no duplicate coordinator/task start.
- On reversal at 50/100/150 ms, start from the current presentation rather than replaying endpoints; the latest route intent alone owns hit testing and completion. Route mutation and transition occur in one owned animation transaction; plain `.transition` on a conditional branch is insufficient. Preserve semantic scroll anchors and selection in scene state, restore after destination layout, and cancel obsolete restore/completion work on a new route. Repeated click/Back during entry resolves the latest intent without flash, ghost hit target or reset to page top. Root module switches use 120 ms opacity only, no lateral motion or full-page height animation.
- iPhone: use native NavigationStack push/pop for Home→Usage/Clipper and retain interactive leading-edge back. Remove custom zoom-source/destination wrappers for these routes; a small card should not expand into an unrelated full dashboard. Sheet behavior stays native. No simultaneous custom push and native transition. Deployment availability must be checked before using any newer navigation API; the contract does not require an iOS-18-only effect.
- Reduce Motion: no translation/zoom; instant change or ≤100 ms opacity, preserve direct interactive back/scroll. Hover uses 100–120 ms fill/border only; no lift, bouncing scale, animated icon stroke or global spring. Button press uses immediate fill; selectors can retain the bounded §11 highlight spring with stationary labels. No chart reveal on return, refresh or provider change. Charts are fully drawn on ordinary entry; remove decorative ring/reveal timers from removed components.
- Calendar: retain §7 time/edit/gesture arbitration and §16 region-specific pinch. Month header pinch begins at factor 1, outward ≥1.12 expands/inward ≤0.88 collapses; timeline pinch changes hour density 40–120 about captured pointer wall-time. Direct samples never spring; header settles in 240 ms, cancel restores start. Scroll anchor and initial-position flag must live in the existing scene-owned Calendar presentation state, not a conditionally mounted timeline; resolve its current symbol before editing, and do not invent a parallel `CalendarPresentationState` type merely to match this prose. Preserve late-day location across module switch and return. Native vertical scroll wins ambiguous drag; iPhone glide/pager and drag previews remain finger-bound.

- Sheet keyboard: focus traversal follows visual order, Command-Return submits only a valid draft, Escape preserves dirty-dismiss confirmation, and keyboard appearance scrolls only the focused/invalid field after measured layout. Keep Save and its error reachable above keyboard; dismissing keyboard never saves. Test last field at the smallest viewport, multiline error, keyboard hide/show and interrupted photo response. Preserve existing draft/revision/receipt guards.

### 19.7 Remaining screen acceptance
| Surface / owners | Required correction | Concrete rejection / proof |
| --- | --- | --- |
| Finance / `Modules/Finance/{FinanceView,FinanceChartModeViews,FinanceAnalyticsView}.swift` | 28/30 hero ceiling; one chart; shared compact controls; Wealth full detail in Analytics only, root has compact entry; no-source one cause/action | Reject repeated unavailable hero/plot/categories/metric grid. Chart/categories split at 960 content width only. Check 719/720 and 959/960, observed zero, six-digit EUR and changing currency. Preserve per-detail ranges and exact selected point on same-model append; clear on removal. Remove unused metric-grid production helper and `detail(for:)` if references confirm no callers. |
| Calendar / `CalendarView.swift`, `Shared/{CalendarViews,CalendarLayout}.swift` | One gutter/now assembly in today's column, compact date toolbar, no expansion slider; finite single timed scroll viewport | Show 23:45–24:00 above safe-area controls at 40/64/120 pt/hour; no blank full-screen tail. Record module return at 22:45, both pinch regions, diagonal scroll and interrupted event edit. One-minute focal tolerance except edge clamp. |
| Recovery / `Modules/Fitness/{FitnessView,FitnessStressView}.swift` | 22/24 main value unless a single meaningful hero; observed supporting cards 88–112 pt, absent rows 56–72; one shared source notice | No blank rings, repeated source paragraph or unexplained huge panel. Stress/reserve retain independent source eligibility; single missing metric does not erase observed neighbors. |
| Training / `Modules/Fitness/{FitnessTrainingView,FitnessTrainingSessionView}.swift` | Workout title/date → active exercise → compact set rows → session action → history. Rows ≥32 Mac/44 phone; exercise name leading, reps/load with explicit units; one primary Start or Finish according to actual state. PRs are factual history, never coaching | Record add/edit/remove set, exercise reorder if already supported, finish/cancel/reopen and history navigation. No swipe/scroll committing a set, clipped units, fabricated Zepp sets/loads, or duplicate finish receipt. Unknown source fields remain absent with explanation. |
| Biology / `Modules/Fitness/FitnessBiologyView.swift` | 22/24 result, compact experimental disclosure, native date control within measured width; source facts before long methodology | No clipped date/disclosure or giant ineligible-age panel. Methodology in disclosure; exact necessary uncertainty stays visible. Six source metric labels remain accessible in one compact column/two columns at ≥720. |
| Nutrition / `Modules/Fitness/FitnessNutritionView.swift` | 28/30 kcal maximum; macro rows and compact meal list; native edit sheet with one heading, no enclosing form card | Mac sheet 520 preferred width, 420 minimum only if available, max 600; body ≤available height−48, scroll with fixed footer. Labels above, fields 32 Mac/44 phone, unit trailing; 2 numeric columns only at ≥440 inner width. One blue Save meal primary, Cancel secondary; preview actions near proposal. Replace storage/medical-accuracy boilerplate with concise source and saved-state copy while preserving uncertain estimates and receipt semantics. Keyboard must not cover Save/error. |
| Settings / `Settings.swift`; Tax / existing tax screen owners | Retain §15 grouped connections, 600 pt form maximum, task-oriented copy, restrained type; apply same components to Tax after confirming owners | No developer-facing review/authority prose in normal flows, no success from configuration alone. Do not refactor Tax persistence in a visual tranche. |
| Widgets / existing §10 files/families | Preserve system-family density; do not globally apply smaller Mac app typography. Grey-backed contrast, concise labels, estimates green/full-color and explicit text in monochrome | Every shipped family readable on #606060/#808080/#A0A0A0; actual transparent/dark/tinted Home Screen captures required. No clipped primary metric, tiny shrink-to-fit text or unsupported quick action. |

### 19.8 Evidence and manual acceptance workflow
- Canonical matrix in logical points (expanded release matrix §19.11): Mac compact 800×700, regular 1100×800, wide 1440×900; Home/Usage also 1280×800 fold checks. iPhone 375×667, 393×852, 430×932, plus actual iPhone 17 dimensions/OS recorded from the available physical device or installed simulator runtime, identifying which supplied the evidence. Sweep content breakpoints 559/560,719/720,959/960 independently of outer window. Record backing scale and safe-area/chrome sizes; compare images only at identical scale.
- For every changed screen capture light/dark × observed/live-shaped fixture/unavailable at all standard sizes. At regular Mac and middle phone add zero, partial, stale, failed refresh, redacted, long de-DE strings/large values and loading. At smallest sizes add Dynamic Type AX3 and AX5 phone, enlarged Mac text; switch to wrapping/stacked/Agenda layouts. Repeat navigation, sheet, chart scrub, hover and Calendar recordings with Reduce Motion on/off. Theme/text changes mid-interaction must settle without loss of state.
- Deterministic fixtures: frozen Berlin clock/timezone, seeded stable IDs, fixed locale/source timestamps/series, explicit source state, font/OS/build identity. Disable network, publishing, sync, personal-store writes and animation clocks only in the isolated screenshot harness. One visible fixture badge per screen. Never ship launch arguments selecting fixtures; capture a separate authorized live readback from the normally launched app, including launch configuration. Demo screenshot success cannot certify live integration.
- Establish BEFORE capture from the same current binary/source state if available; user attachments remain qualitative rejected references. AFTER captures must be opened and inspected at native 1× logical scale and at device scale. Produce contact sheets for cross-screen consistency plus full-resolution crops for labels/axes. Do not approve merely by zooming out until oversized typography looks acceptable.
- Baselines: first corrected captures require named manual approval; never bless current output automatically. Store approved PNGs with manifest (commit/worktree hash, OS, scale, viewport, fixture, locale, theme, text size) in an explicitly assigned test-artifact directory. Baseline updates require before/after/diff review and reason. For later runs on the same renderer, fail if >0.5% pixels differ by >8/255 per channel; additionally fail changed text/control regions above 0.1%. These are initial alert thresholds to calibrate once against unchanged repeated captures, never automatic aesthetic approval. Different OS/font renderer requires a separately reviewed baseline set, not loosened thresholds.
- Geometry assertions operate on exported view/accessibility frames: essential labels/values inside viewport and parent bounds within 1 pt; no overlapping hit regions; 32/44 pt targets; one visible page heading; one source notice for shared absence; lead/plot vertical budgets from §§19.4–5; chart axis labels inside reserved inset; bottom Calendar event clear of controls. Test exact point retention and route/scroll restoration independently. Large-text fixtures may exceed fold budgets but must scroll and never clip.
- Manual checklist, recorded pass/fail per evidence ID: hierarchy reads title→state/value→action→evidence; Mac feels appropriately dense at 100%; no decorative vacancy; icon family/optical alignment consistent; one primary action; labels uncut; source meaning truthful; contrast measured ≥4.5:1 text/3:1 essential graphics; keyboard/focus/hover legible; navigation/back/interrupt smooth; direct manipulation tracks input; no replayed reveal or accidental edit; live launch clean of fixture data. Seven original defects plus two new Home/Usage captures must each have a disposition.
- Motion evidence: 60 fps recording where supported of Home→Usage→Back→Clipper, rapid reversal, module switch/return, pinch, sheet/keyboard and refresh while inspecting. Record device refresh rate, dropped-frame observations and one Instruments hitch trace for affected routes; target no visible discontinuity and no main-thread stalls >50 ms during these flows. A still frame cannot approve a transition; an offscreen harness cannot approve physical trackpad or real WidgetKit rendering.
- Resource hygiene: one native build/test process at a time, `-jobs 1`, at most two disjoint code workers, no concurrent simulator farm. Keep an owned PID/session ledger; stop only finished/stalled task-owned test hosts, previews or servers after evidence capture, never broad kill-by-name. Close completed workers. Clean assigned disposable DerivedData only after logs/artifacts are saved. Audit current active processes before further builds; this read-only design pass does not claim cleanup occurred.

### 19.9 Disjoint Luna Max worker tickets and Astra Medium gates
Dispatch this list and the sequential follow-on scopes in §20.2 only. Paths are relative to the repo; brace lists enumerate named files, never directory-wide permission. Legacy numeric anchors below are hints from an earlier handoff; §20.1 records current symbols. Never patch by line number without resolving its symbol. Each worker first resolves current symbols with `rg`, returns the actual anchors and a bounded patch plan, then edits only its named files. A symbol already satisfying its contract needs evidence, not a rewrite. Foundation APIs and domains are read-only to other tranches. Any necessary extra file gets an explicit revised dispatch before editing; do not invent APIs, test filenames or broad ownership.

| Ticket / dependency | Exclusive production scope and entry anchors | Required change; prohibited scope | Focused tests and proof; Astra Medium gate |
| --- | --- | --- | --- |
| **A Foundations** / first | `ios/Shared/{Typography,DesignTokens,LifeOSComponents,LifeOSIcon,LifeOSMotionKit,LifeOSInteractionKit,LifeOSResponsiveContainer}.swift`; `LifeOSTypography`/system role near Typography:9, tokens :657, motion :978, `LifeOSResponsiveContentContainer` | Compare current roles to §§19.2–3; correct only exact mismatches, semantic glyph contexts and compact controls. Preserve scaled SF Pro and existing motion lifecycle. No screen, model, store, route, project-setting or font-asset edits; do not globally shrink widgets | Own `ios/LifeOSTests/LifeOSDesignSystemTests.swift`: platform roles/targets, composed contrast and interruption invariants where changed. Component board Mac/phone, light/dark, focus/hover/press/disabled/long labels. **Gate A:** approve measured primitives or record already conforming; no speculative foundation rewrite |
| **B Shell/Home** / A | `ios/LifeOS/{OverviewView,LifeOSApp}.swift`, `ios/LifeOSMac/LifeOSMacApp.swift`, `ios/LifeOS/Modules/ModuleNavigation.swift`; Overview header :442/status :482/composition :522, `OverviewMetricCard`; Mac shell :553/sidebar :609/Home layers :816/hover :969/navigation :1106; iPhone tabs :974/:1044/:1079 | §§19.3–4,6; compact sidebar/Home, remove mini rings/strong rail/ad hoc tab glyphs and symbol replacement, stable route reversal. Own live/fixture route wiring. Preserve coordinator lifetime, deep links and preferences. No Usage internals, Settings, provider or shared-token edits | Own `ios/LifeOSTests/OverviewDomainTests.swift` only for affected presentation invariants; J owns shell UI tests. All Home state captures, all destinations/deep links, Home→Usage→Back→Clipper rapid reversal, scroll/focus restoration and live launch. **Gate BC** with C |
| **C Usage** / A; integrate B route contract | `ios/LifeOS/CodexView.swift` (`UsageView` near :81), `ios/LifeOS/Usage/{UsageProjectionChart,UsageFactsView,UsageTokenActivity}.swift` near :722/:11/:6 | §19.5: one chart-led hierarchy, real window rows, clear period/metric labels, factual disclosure; remove jumbo/ring/empty Insights helpers after reference inventory. No shell, shared-token, ingestion, projection-domain or provider changes | Own `ios/LifeOSTests/{UsageFactsTests,UsageAnalyticsTests}.swift`: same-point retention, reset/provider identity, gaps/zero/unsupported windows. Current Mac/phone screenshots, scrub+refresh recording and normal-launch real read or explicit unavailable state. **Gate BC:** both B/C visual and route proof; source-only results remain pending |
| **D Finance** / A | `ios/LifeOS/Modules/Finance/{FinanceView,FinanceChartModeViews,FinanceAnalyticsView}.swift`; Finance composition :459/header :738/connection :1253/hero :1343/chart :1696, Analytics :26; resolve `FinanceHeroCard`/`FinanceDetailChartCard` | §6/§19.7: one absent-source cause/action, qualified observed totals, one chart/control group and useful categories; no redundant unavailable metric grid. No banking API/session/import/storage or shared-token changes | Own `ios/LifeOSTests/FinanceChartModeWiringTests.swift`: actual mode/range, zero/gaps/selection retention. Capture 719/720 and 959/960 content widths, large EUR, partial/stale/error; record range escape and refresh inspection. **Gate DF** with F; banking readback remains separate |
| **E Calendar** / A+B scene contract | `ios/LifeOS/CalendarView.swift` header/density :1001/:1114; `ios/Shared/{CalendarViews,CalendarLayout}.swift`, timeline :720/magnification :1190/scroll :1754/:1937/now :4262; resolve `CalendarTimelineView`, `CalendarTimelineScale`, existing presentation state | Preserve existing finite-scroll/now fixes; correct only reproduced failures against §§7,16,19.6. Month header pinch and timed density have fixed distinct owners. No coordinator/sync/domain edits, source-time inference or global gesture recognizer | Own `ios/LifeOSTests/CalendarLayoutTests.swift`, `ios/LifeOSUITests/CalOverhaulEvidenceUITests.swift`: 40/64/120, bottom clearance, Berlin DST, single now, focal/cancel invariants. Real Mac header/timeline pinch and phone scroll/pager/edit recording; return at 22:45. **Gate E** separately: source tests cannot pass trackpad/scroll gate |
| **F Recovery/Training** / A | `ios/LifeOS/Modules/Fitness/{FitnessView,FitnessStressView,FitnessTrainingView,FitnessTrainingSessionView}.swift`; composition :1694/recovery :1977, Coaching :2611 and Stress :258, training :20/session :49 | §§8,19.7: compact independent signals, one shared notice, usable set/history rows. Remove Coaching heading/entry/action; factual observations may survive under Context with source/time, never advice. No Biology/Nutrition, HealthKit/Zepp, training model/store/coordinator or token edits | Own `ios/LifeOSTests/FitnessCoreDetailDomainTests.swift` only for affected presentation contracts; run existing `FitnessTraining{Domain,Coordinator,Projection,Store}Tests.swift` read-only as relevant. Partial/absent/zero captures; set editing, finish retry, history/back recordings. **Gate DF:** include no-coaching copy audit and independent source eligibility |
| **G Biology** / A | `ios/LifeOS/Modules/Fitness/FitnessBiologyView.swift`; `FitnessBiologyDetailSurface` :167, header :256/metrics :339/age :454/cards :526 | Compact date/result/disclosure, evidence-first hierarchy, six metric labels and unclipped provenance. No root Fitness, Nutrition, model eligibility, age computation or shared tokens | Own `ios/LifeOSTests/FitnessBiologyDomainTests.swift` for relevant presentation regressions only. Eligible/ineligible/partial and long de-DE date/AX captures, date change/detail/back recording. **Gate GH** with H |
| **H Nutrition** / A | `ios/LifeOS/Modules/Fitness/FitnessNutritionView.swift`; composition :805/hero :958/macros :1129/meals :1487, `FitnessFoodReviewSheet` :2968/footer :3081/photo :4323/fields :4811 | §9/§19.7: one heading, labels above compact fields, no form-card enclosure, fixed one-primary footer, concise truthful saved/estimate copy. Preserve photo-only AI. No API `nutrition-photo.ts`, meal store, photo coordinator, root Fitness/Biology or shared-token edits | Own `ios/LifeOSTests/NutritionDomainTests.swift` for affected draft/presentation assertions. Manual/photo/invalid/saving/failure/retry states; smallest-width keyboard, duplicate Save, stale photo response and dirty dismissal recordings. **Gate GH:** draft/receipt semantics and visible footer both pass |
| **I Widgets** / A; B destination contract | `ios/LifeOSWidget/{FutureModuleWidgets,UsageWidget,CalendarWidget,NextEventWidget}.swift`; type :42/backing :196 in Future, Calendar :132 | Retain every shipped kind/family and §3 backing/§10 density. Consolidate owned glyphs, unclipped units/state; estimates explicit in monochrome. No app typography rewrite, WidgetSnapshotPublisher, timeline data store, entitlement or signing edits | Own `ios/LifeOSWidgetSnapshotTests/LifeOSWidgetSnapshotTests.swift`; run `ios/LifeOSTests/WidgetSnapshotPublisherTests.swift` read-only. All declared families/state contact sheets and actual WidgetKit dark/transparent/tinted grey captures, destination taps. **Gate I:** offscreen + system evidence required; device gaps stay pending |
| **J Visual evidence** / prepare before B; captures after each owner | No production ownership. Own `ios/LifeOSMacSnapshotTests/LifeOSMacSnapshotTests.swift`, `ios/LifeOSUITests/LifeOSUITests.swift`, `ios/LifeOSMacUITests/LifeOSMacUITests.swift`; proposed artifacts only under `ios/VisualEvidence/` (manifest, README, PNGs/diffs, recordings or local artifact references) | §19.8/§19.11: current-binary BEFORE/AFTER, measured frames and manually inspected captures. No E/I test files, production instrumentation, project settings or fixture writes to personal stores. If harness needs extra production hooks, return a narrow proposal to its owner | Fix image-existence-only acceptance; record build/runtime/launch identity, state and gaps. Cross-screen contact sheets plus 60 fps/interruption evidence. **Final Astra gate:** review matrix by evidence ID; never auto-bless baselines or equate test count with quality |

- Run order: A and J's read-only inventory → B/C → D/F → E → G/H → §20.2 follow-on surfaces → I → integrated J. J may capture each finished surface earlier, but serializes all builds/runtime capture with owners. At most two disjoint coding workers; one native build/test at a time, `-jobs 1`, no simulator farm. Close finished worker sessions and owned disposable processes after saving evidence.
- Astra Medium reviews A, BC, DF, E, GH, I and final J packets. Each packet includes requirement/defect IDs, current anchors, exact diff/dead-code inventory, focused test outcomes, opened screenshots, recording/trace IDs and unverified items. Return separate source and visual/runtime verdicts. A failed surface gets one bounded correction; accepted surfaces stay intact. Blocked hardware proof does not cause speculative rewrites or prevent independent source work.
- Tests listed above are existing suites, not permission to weaken domain assertions or add tests mirroring constants. Confirm schemes/destinations in `ios/project.yml` read-only; run only relevant tests and one integrated build per batch. Do not rerun backend suites for pure layout edits. Settings/Tax and remaining Fitness/Clipper surfaces use sequential follow-on scopes in §20.2 after shared owners finish. Obsidian needs its own compatibility plan and exact-file dispatch; its release row stays open.
- Remove owned dead helpers only after repository-wide reference search, including test/preview/availability uses. No blanket O(n) promise: preserve §12 one-pass aggregation/indexed lookup and bounded rendering; record measured hot-path behavior. A necessary sort remains O(n log n). No production domain change merely to make a visual state attractive.

### 19.10 Defect and state disposition
Use identifiers **S1 Finance, S2 sidebar, S3 Calendar now/scroll, S4 Calendar expansion, S5 Recovery, S6 Biology, S7 meal review, H1 Home, U1 Usage, M1 route motion** in packets. §§13/19.4–7 contain the corrective contract; each ID needs current-build AFTER proof and a pass/fail reason. Reject giant 58%/42% emphasis, repeated absent-source panels, mixed/ad hoc glyphs, any repeated now label, unreachable late event, expansion slider, oversized empty Recovery/Biology, nested meal form, clipped essential copy or a discontinuous route reversal. No “looks better” waiver.

| State | Presentation invariant across affected surfaces | Proof |
| --- | --- | --- |
| Live | Source observation, actual timestamp/window and meaningful content; one primary action | Normal launch with real readback; inspect selected value against source without exposing secrets |
| Unavailable / first load | One compact shared cause/action; no axes, vacant rings or giant metric panels; local workflows stay usable | Isolated absent-source fixture; separate actual unavailable runtime state if provider blocked |
| Observed zero / known empty | Display true 0 with unit or scoped “No entries”; never disconnected; Calendar grid/meal creation remain usable | Valid zero source state; no fake positive wedges or gap-to-zero conversion |
| Partial | Retain independently available values; qualify incomplete totals; only unsupported portions absent | Missing one account/health signal/chart interval; no page-wide collapse |
| Stale | Retain admissible observation with its original timestamp; freshness from domain | Refresh recording retains scroll/selection and does not relabel old data live |
| Error | One operation error plus retained data, or compact retry if none | Failed refresh/save; form edits and durable receipts remain truthful; retry is idempotent |
| Redacted | Remove protected values, charts, inspection/tooltip and accessibility strings immediately | Redact while inspecting and while widget is visible; local originals remain private |
| Loading / filtered-empty | First load compact; refresh retains content; empty filter retains controls and a supported escape | No decorative skeleton/unsupported gestures; never trap user in disabled range |

- No production Advisor, assistant/chat, coaching product, generic AI action, or renamed advice/Insights destination. Inspect visible copy plus route/deep-link/intent entry points. At FitnessView ~2611 and FitnessStressView ~258 remove the Coaching product treatment; retain only independently useful source facts as neutral Context, with no prescriptive/generated advice. Report references outside F scope to their owner. Factual provider usage stays; calorie-photo estimation is the sole in-app AI and remains visibly estimated until user-confirmed.

### 19.11 Final acceptance matrix and independent limits
Every cell records evidence ID + build/launch identity + PASS/FAIL/UNVERIFIED + reviewer reason. Manifest metadata must separate **isolated fixture**, **normal live launch**, **simulator**, **offscreen**, and **physical/system capture**. Do not replace personal data, publish fixture widgets or enable fixture flags in production. Retain sensitive originals locally; share redacted copies only.

| Matrix row | Required coverage | Closure gate |
| --- | --- | --- |
| Mac | 800×700, 1100×800, 1280×800 Home/Usage fold, 1440×900; content breakpoint sweeps 559/560, 719/720, 959/960 | Light/dark live-shaped and unavailable captures at all sizes; zero/partial/stale/error/redacted at regular; enlarged text/long de-DE at compact. Real trackpad and hover/focus recordings |
| iPhone | 375×667, 393×852, 430×932; separately record actual iPhone 17 dimensions/OS when available | Same base states/themes; smallest AX3/AX5 and keyboard; middle state matrix; native back, glide/drag, Calendar scroll. Generic dimensions are not iPhone 17 certification |
| Widgets | Every declared kind/family; #606060/#808080/#A0A0A0 plus user's actual grey wallpaper; supported full-color/dark/transparent/tinted modes | Offscreen state matrix plus real WidgetKit captures; ≥4.5:1 text/3:1 essential marks in final composition, unclipped primary values and working destinations |
| Motion | 60 fps recordings where available, measured refresh rate; normal/Reduce Motion on/off and mid-interaction change | Home→Usage→Back→Clipper and rapid reversal; calendar pinch/scroll/edit cancel; sheet keyboard and scrub+refresh. Review frames and Instruments hitch trace, no visible discontinuity/main-thread stalls >50 ms. Missing 60 fps/trace remains an explicit evidence gap |
| Live data | Normal app launch, provider/window identity, source timestamp, selected values, fixture flags absent | Authenticated real read and displayed-value match; provider unavailable is honest behavior proof, not live integration PASS |
| Training / Zepp / HealthKit | Native set/session/history interactions plus separately sourced imported workout fields | No full Zepp accuracy claim from UI/tests. Verify device/source field-by-field, preserve unavailable sets/reps/load/Training Effect; unsupported parity remains open |
| Enable Banking / Windows | Separately recorded deployed service health, authorization and actual account/transaction readback | Configuration, bank-link existence, BitLocker, preflight or a visual fixture cannot certify runtime data flow/security |
| Device / signing / Shortcuts | Physical iPhone install/launch, Health authorization, actual Shortcut run and renewal/relaunch receipt | A script/intent or planned USB workflow does not prove signing renewal; no device/runtime success without its own evidence |
| CoreSimulator | Actual installed runtime availability and successful launch/capture | Source build or offscreen rendering is not simulator execution; blocked runtime remains UNVERIFIED |
| Obsidian mind map | Separate compatibility/storage/conflict/canvas plan and real vault round-trip evidence | No claim of implementation from reference inspiration or this plan; not assigned to Calendar's visual ticket |
| Whole-app release | All above plus existing Settings/Tax workflows, device/provider requirements and independent final security review | Astra may approve bounded visual batches; no whole-app “flawless/complete/secure” claim while required rows fail or remain unverified |

This is an implementation plan, not a build result. This authoring task changes only root `design.md`; it runs no tests, simulators or deployment, and changes no coordination files, production code or project settings.


## 20. Complete surface coverage and measured finish

### 20.1 Current source index and anti-rework rules
This section closes coverage gaps, not a new visual direction. Use §§2–11 for behavior, §19 for geometry/dispatch and this section for completeness. Component names describe contracts, not a requirement to create duplicate wrappers. Paths are relative to this repo.

| Source / current symbol | Inspected consequence |
| --- | --- |
| ios/Shared/Typography.swift → LifeOSTypography.Role | SF Pro, scaled platform roles and inlineMonitoringValue exist. Fix oversized callers, not the facade. Default maximum: page 22 Mac/24 phone; hero 28/30; secondary metric 22/24. User enlargement may exceed these and must reflow. |
| ios/Shared/LifeOSIcon.swift → LifeOSIconName.systemImageName | Current usage = chart.bar.xaxis and clipper = chart.xyaxis.line; §19.3 targets chart.xyaxis.line and rectangle.stack. Migrate centrally with all caller/widget review, never per-view overrides. Legacy home = house, primary Home uses overview; preserve route IDs. |
| ios/Shared/CalendarLayout.swift → CalendarInteractionLayout, CalendarTimelineZoomSession | Source density 40/64/120. Reuse existing transactions/arbitration/DST helpers; old screenshots alone do not justify geometry rewrites. |
| ios/LifeOS/Usage/UsageProjectionChart.swift → maximumRenderedSamples | 240 observed points. Preserve gaps, raw indexed selection and authority/revision behavior. No visual helper may revive unavailable cached values. |
| ios/LifeOSMac/LifeOSMacApp.swift → LifeOSMacSceneState, LifeOSMacRouteSnapshot, homeDetail(for:interactive:) | Extend current route owner. No recursive root reconstruction; prove bounded lifecycle rather than assuming two layers imply correctness. |
| ios/LifeOS/OverviewView.swift → OverviewView, OverviewMetricCard, ClipperAnalyticsView | Home/Clipper workers share a file: run sequentially. Clipper has 18/28 padding and separate unavailable detail panels; migrate to tokens and one shared cause. |
| ios/LifeOS/TaxDocumentsView.swift → TaxDocumentsView, TaxDocumentReviewView | Existing import/review is actual scope. Preserve privacy/receipts; correct header/form/navigation only where reproduced. |
| ios/LifeOS/Modules/ModuleNavigation.swift → macPrimaryModules, moreGroups, hasWorkingView | Six working roots: Home, Calendar, Finance, Fitness, Tax, Settings. Compatibility cases do not establish completed modules. |
| ios/LifeOSMacSnapshotTests/LifeOSMacSnapshotTests.swift | Reuse testFinanceSpendSnapshot, testFinanceWealthHoldingsUnavailableSnapshot, testFitnessTodaySnapshot, testFitnessBiologyLightDarkSnapshots, testFitnessNutritionSnapshot, testUsageSnapshot, testTaxDocumentsSnapshot. Image existence is not visual acceptance. |
| ios/LifeOSWidgetSnapshotTests/LifeOSWidgetSnapshotTests.swift | Reuse testUsageSmallUnavailableSnapshot, testGreyWallpaperWidgetStates, testNutritionGreyWallpaperTransparentAndTintedSnapshots, testCaloriesMacrosMediumLayoutKeepsAllEssentialFramesInBounds, testCalendarUnavailableSnapshot, testNextEventSmallUnavailableSnapshot; preserve domain assertions. |

Before dispatch record requirement → existing symbol → observed discrepancy → smallest patch → evidence. A conforming implementation becomes “retain; needs capture”, not replacement code. Review findings need a failing invariant/reproducer. After one unsuccessful correction, Astra Medium supplies a causal plan before the next Luna patch. Do not repeatedly redesign accepted surfaces or treat usage consumption as productivity.

### 20.2 Full module disposition and follow-on contracts
Shipped functionality is **retain/refine** unless explicitly retired below. Follow-on scopes run sequentially after §19.9 shared-file owners finish. These are future instructions; this authoring task writes only design.md. Confirm test ownership before dispatch; no guessed paths or directory-wide grants.

| Surface / exclusive follow-on source scope | Layout/action/state contract | Closure |
| --- | --- | --- |
| Home, Usage, Finance, Calendar, Recovery, Biology, Nutrition, Training | Retain/refine under B–H. Resolve S1–S7/H1/U1/M1; preserve accepted primitives/domain contracts. | Every specified state, interaction and current-build capture passes; no decorative substitute data. |
| Clipper / ios/LifeOS/OverviewView.swift, after B | Title/scope → timestamp → views/subscribers/revenue → actual trend → account/bot breakdown. Two supporting columns at ≥720, otherwise stack. No-source one 72–104 cause/action; partial preserves independent facts. Revenue has currency/period. Review connection primary only when needed; otherwise inspect existing breakdown. | Home→Clipper→Back restores focus/scroll; no repeated unavailable panels, invented growth or combined currencies. |
| Tax / ios/LifeOS/TaxDocumentsView.swift | Title + Import PDF primary → progress → supported search/filter → document rows (name/date/kind/review state) → review. Rows ≥48 Mac/56 phone, names wrap two lines. Detail identity → grouped fields/evidence → one Save corrections if supported; export secondary. | Empty import works; errors preserve records/edits; read-only distinct; cancel cannot remove saved records. Existing identifier masking persists; no storage changes in visual ticket. |
| Settings / ios/LifeOS/Settings.swift | §15 grouped connections/devices/privacy/preferences; one task/action per detail; diagnostics collapsed. No automatic-signing/Zepp promise from an unverified Shortcut. | Recovery has truthful result/focus; no secrets or false data-success badge from configuration alone. |
| Strength / ios/LifeOS/Modules/Fitness/FitnessStrengthView.swift | Date/range → exercise → volume/sets/reps with units → actual progress → sessions. No invented combined strength score. Known zero valid; missing external sets unavailable. Start belongs in Training; existing detail action only here. | Long names, kg/lb and gaps fit; return preserves state; no per-render history aggregation. |
| Lifestyle/journal / ios/LifeOS/Modules/Fitness/FitnessLifestyleView.swift | Date → actual entries → supported add/edit → factual history/correlation disclosure. Hydration/caffeine/alcohol keep units; missing log is not no consumption. Tags wrap; one Save. | Retry retains draft/no duplicates; coverage/window disclosed without causation or advice. |
| Supplements / ios/LifeOS/Modules/Fitness/FitnessSupplementsView.swift | Today rows: product, entered dose/unit, due time, Taken/Snoozed/Skipped. Selected row owns contextual primary; Add is quiet entry. Inventory/expiry in detail; retain notification actions. | One dose affects intended receipt only; no doubling advice; long labels/time zones fit; unavailable notifications never imply scheduled delivery. |
| Remaining Fitness tabs / ios/LifeOS/Modules/Fitness/FitnessView.swift, after F | Audit actual Health Monitor/sleep/load/activity/performance/journal sections. Detail: identity/date → value/coverage → applicable plot → records/provenance. Missing metric compact; observed neighbors remain. | Every section accounted for; no duplicate heading/scroll owner or renamed advice; no new medical model. |
| Bank Connections, Investments, Documents compatibility roots | Preserve working Settings/Finance/Tax paths. Unsupported destination offers one truthful state and safe working destination, no duplicate manager/model/store. | Safe deep links; compatibility routing not counted as implemented feature. |
| Business, Tasks, Grocery, Shopping, Reports compatibility roots | Retain stored route compatibility, omit primary navigation until implemented. Tasks widget exposes only supported aggregate/destination, not proof of a task manager. Required wider scope remains explicit. | Every case disposition recorded; no fake report, dead-end CTA or deleted user records. |
| Obsidian mind map | **Pending separate compatibility implementation**, not retired or a fake editor. Future Calendar entry after actual vault read/write. Target project picker → bounded pan/zoom canvas → labeled shapes/connections; node opens Markdown inspector Mac/sheet phone. | Later plan settles vault/schema/portable styles/links/conflicts/offline writes and Obsidian/Mac/phone/Windows round trip. Coordinates never sole semantic storage. |
| Advisor, generic chat/assistant, coaching/advice | **Retire entry points and exclusively supporting dead code.** Preserve factual usage/observations and calorie-photo only. No renamed advice under Insights/Context. | Audit routes/menus/deep links/intents/copy; photo flow survives; distinguish legitimate provider terminology before removal. |

### 20.3 Widget audit and family invariants
Inspected bundles declare **18 iPhone kinds and 17 Mac kinds**; the iOS-only Usage accessory explains the difference. §10 lists every kind/family. **Retain/refine all listed kinds**; none silently retired. Large-family QA N/A because none declared. Fixture-only types are not shipped kinds.

- Audit key: kind/platform/family/state/rendering mode; record display name, main fact, coverage, destination/action, privacy and evidence. Account for every bundle entry; separately search unreferenced view types.
- Distinguish overlap: HealthMonitorWidget aggregate vs FitnessHealthMonitorWidget vital table; NutritionOverviewWidget entry actions vs CaloriesMacrosWidget intake/macros vs NetEnergyWidget qualified balance; DailyOverviewWidget day summary vs RecoveryRingWidget one metric. Future consolidation needs kind/deep-link migration and lost/retained capability explanation before removal.
- Small priority: source/metric, value, unit/window, state; drop sparkline before provenance. Medium: main fact, up to three supporting facts, state; remove optional subtitle before shrinking text. Accessories one fact/nearest event, system geometry/safe link.
- Use actual WidgetKit size/margins. Medium split: left 0.42×(usableWidth−12), right remainder; widget-specific stacking when needed, never overflow. Do not assume universal pixel dimensions.
- Clear full-color: one 60% black backing behind essentials, opaque text, 8 inset/12 radius. Test #606060/#808080/#A0A0A0/user wallpaper plus bright stress case. No global opacity reduction/double card.
- System-remapped modes use supported foreground groups/native contrast; custom hue/backing survival is not guaranteed. Estimates retain Est./Estimated, calories kcal; distinction survives monochrome. System captures decide acceptance.
- Taps, locked/stale/redacted states need system proof. No continuous animation/timer, unsupported action, demo publication or instant-refresh promise.

### 20.4 Route, input and measured performance
**Ownership:** one scene coordinator set and active content tree, at most one outgoing presentation layer without a second fetch/store/observer lifecycle. Any ephemeral viewport raster stays local/nonpersistent, invalidates immediately on privacy change and releases on completion. Prefer current host. Rapid intents coalesce to latest, never a queue of hidden full screens.

**Choreography:** atomically select intent, disable outgoing input/accessibility, animate from current presentation with §11 timing, detach outgoing on current-generation completion and restore focus after layout. Same-route intent no-op. Back restores semantic anchor without recreating modules. Test reversal at 50/100/150 ms and 20 Home→Usage→Back→Clipper→Back cycles: settled route count one, tasks/subscriptions return to baseline, no continuing per-cycle memory growth after warmup.

**Curve:** custom ease-out = cubic Bézier (0.16,1,0.3,1). Direct samples have no implicit animation. Springs remain selector response 0.24/damping 0.90 and pager 0.28/0.92; no global spring around route/data/geometry. Native navigation/scroll/sheet physics remain platform-owned.

| Input/state | Acceptance |
| --- | --- |
| Hover/focus/press | Exit cancels hover, release outside cancels action; focus independent. Busy preserves width/label/spinner slot; disabled reason available without hover. |
| Scroll/scrub | Undecided until 8 pt and 1.3× horizontal dominance; vertical wins otherwise. Tooltip cannot capture next drag or cover selector; route exit cancels; never disable page scrolling globally. |
| Calendar pinch | Begin region fixes owner across boundary crossing; 40–120 density vs 1.12/0.88 expansion. Preset/chevron share state; cancel restores start; no root-window recognizer over sheets. |
| Drag/edit | Preview until valid release; Escape/background/invalidation stops preview/auto-scroll. Text/input drags never reorder; animation completion never persists. |
| Keyboard | Tab/Shift-Tab follow visual order; Return/Space activate; Escape dismisses transient UI first. Command-Return only focused valid editor; preserve native Back; Calendar shortcuts never intercept text input. |
| Reduced motion switch | Cancel old animation/completion, settle latest intent without translation; retain direct tracking/focus/pressed feedback/information. |

At 60 Hz full-frame deadline 16.7 ms: target main-thread interaction work p95 ≤8 ms and presentation p95 ≤16.7 ms in a warmed 20-second scripted run, no app-caused stall >50 ms. At 120 Hz aim ≤8.3 ms presentation; record hardware limitations, do not infer performance from simulator. Report p95/p99/max/hitch count and Instruments trace per affected interaction family. 60 fps recording alone cannot prove CPU/GPU budgets.

Per revision use O(n) aggregation/index, O(1) ID lookup, O(log n) time selection after justified O(n log n) sort. Calendar overlap packing may remain O(n log n); linear sorting is not promised. Per-frame work depends on bounded visible elements, not history. Check scaling with isolated 100/1,000/10,000-record fixtures; production remains real. Preserve storage/ingestion caps, no destructive truncation. Cache axis formatting by locale/domain; no I/O/network/decoding/formatter creation/unbounded tree rebuild in gesture samples.

### 20.5 Content finish
- One primary means one prominent action for current decision; it may be absent for inspection. Sheet owns primary while background inert; set/dose actions contextual, not a wall of blue buttons.
- Copy: “No account data received yet” for configured/unread, “Connect an account” only unconnected, “Updated 12 min ago”, “No meals logged today”, “Saved on this device”, “Photo estimate · review portions”. Unsupported/unobserved/revoked remain distinct.
- Developer language (reviewed source/preflight/authority/durable storage/generation) belongs in opt-in sanitized diagnostics. Consent/uncertainty stay visible in plain language; local save never claims sync, configuration never claims live.
- Cure sparsity with compact absent states and useful existing records/actions, not filler. Outer whitespace beyond max width is deliberate; large empty space inside low-information cards is not.
- Brand file supplies black/white and blue ramp only. Green/orange/teal/violet are retained semantic extensions here, not falsely attributed to brand file; no decorative saturated module backgrounds.

### 20.6 Sequential implementation/QA checklist
Record source, behavior, visual, live and device verdicts separately. This task delivers the plan only.

1. **Inventory/reproduce:** current binary/tree identity, route/widget catalog, S1–S7/H1/U1/M1 disposition and native-scale captures. Pass: every defect and §20.2/§10 surface has owner/state matrix; conforming source gets evidence, not rewrite.
2. **Foundation A:** component board for type/icons/controls/states. Pass: default maxima, target geometry, measured text 4.5:1/essential graphics 3:1, three fills, one symbol family, no duplicate facade. Gate before screen token adoption.
3. **Shell/Home + Usage B/C:** existing packet, bounded route host, compact hierarchy. Pass: fold budgets, no redundant rings/banners, normal-launch live-or-truthful-unavailable, 20-cycle check, exact source/selection, opened captures/interruption recording. Astra reviews integrated batch.
4. **Finance + Recovery/Training D/F:** compact absent/partial and usable forms. Pass: independent neighbors survive, no phantom plots, one save/finish receipt, long currency/exercise units fit; source/visual verdicts.
5. **Calendar E:** verify current fixes first. Pass: one now marker, aligned columns, final event reachable at 40/64/120 without blank viewport tail, real Mac region pinches/focal tolerance, phone arbitration, DST/cancel/return-at-22:45 evidence. Simulator cannot pass physical trackpad.
6. **Biology + Nutrition G/H:** source-first density/draft-aware editor. Pass: six labels/uncertainty readable, one heading/Save, final field/error/footer above keyboard, duplicate save/retry/late response safe, green estimates/orange calorie identity.
7. **Follow-on §20.2:** sequential Clipper/Tax/Settings/Strength/Lifestyle/Supplements/remaining Fitness after shared owners. Pass: retained routes/states/action-return captured, Advisor absent; Obsidian pending explicitly, not counted complete.
8. **Widgets I:** all 18 iPhone/17 Mac kinds/families. Pass: state contact sheets, actual system grey/clear/dark/tinted captures, no clipped values, privacy/monochrome semantics/correct links. Never retire a kind to evade QA.
9. **Integrated J:** §19.8/§19.11 matrix, matched before/after crops, traces. Pass: essential frames within bounds ±1 pt, no overlap, explained pixel diffs, no app-caused >50 ms hitch, restored focus/anchors. One build/test at a time with -jobs 1; stop only owned completed processes after evidence saved.
10. **Independent Astra Medium:** requirement map, bounded diff/dead-reference inventory, exact tests, opened captures/traces/uncertainties. Separate source/visual/runtime verdicts. Banking/Windows security/signed device/renewal/Zepp/Obsidian need independent evidence. Failed criterion gets targeted correction; accepted surface reopens only for reproduced defect.

### 20.7 Unresolved facts
- Root design.md is the existing implementation contract; external design-coordination supplies governing context. No alternate authority file invented. Current user requirements settle Iconoir/amber conflicts; source/decisions settle Calendar density.
- This pass read source/test names, not a newly rendered app. Trackpad feel, physical WidgetKit composition and SF Symbol availability across supported OS versions need runtime evidence. Reference pages were read; live animations not profiled/copied.
- Measure actual iPhone 17 dimensions/OS/backing scale/safe areas from device/runtime; generic QA sizes are stress fixtures, not device claims. Fixtures cannot certify live integration.
- Obsidian vault/style round-trip/conflicts unresolved; native workouts do not establish Zepp parity. Preserve both requirements and truthful missing fields.
- Zero dead code, optimal complexity, security and flawless UX require reference audit/measurement/independent evidence. This authoring task changes only design.md, runs no builds/runtime tests, and makes no commits/pushes.
