# LifeOS final execution plan
Planning baseline: 2026-09-13, current source HEAD `5ebc17e`. This remains a
planning and dispatch authority; it does not claim product completion.

## 1. Reconciled truth and authority
- Local HEAD and origin tracking ref match `5ebc17e`; GitHub CLI confirms PR #1
  is OPEN/DRAFT/MERGEABLE against `main` at the same head. Recheck before
  every dispatch.
- Release is **NO-GO**. `python3 -B scripts/validate_acceptance_registry.py --json` verified FROZEN, 258 leaves, 7 aliases, 0 accepted, no structural errors; `--score` reports the P0 failure. This measures accepted evidence, not zero implemented functionality or a reliable effort percentage.
- HANDOFF/PHASE_STATUS/todo now name the current source checkpoint and
  distinguish deployable candidate `6baa1f3` from the documentation/code
  head. Historical test counts are scoped receipts, not final acceptance.
- Fresh read-only SSH observation: GEONQSERVER is reachable; C: and D: are
  BitLocker-protected; no LifeOS recovery/install process is running;
  `LifeOSAPI` is stopped and `LifeOSGateway` is absent. The marker remains
  active; its journal is `artifacts-complete` with 31,401 units and progress
  sequence 59,167. The earlier resume stopped after 45 minutes without a
  final stage checkpoint. Canonical recovery/install and live bank readback
  remain unverified.
- Missing: current process/SCM/marker observation; completed recovery/cutover; exact deployed live banking; physical signing/App Groups/HealthKit/widgets; full runtime/visual/performance evidence; workout fidelity; Canvas implementation/round trip; final operational security and operator approval.
- Read together: Coordination/{HANDOFF,PHASE_STATUS,DECISIONS}.md; tasks/{ACTIVE,todo,design,visual-overhaul-plan,bank-consent-spec,human-action-checklist}.md; docs/LIFEOS_ACCEPTANCE_REGISTRY.md; design-repo developers/design-coordination/00-READ-FIRST.md and referenced feature contracts; actual source. Issue #2 was read through `gh issue view 2`, remains OPEN.
- Current user corrections and DECISIONS override older design recipes: SF Pro everywhere (including wordmark), estimates GREEN, calories orange, semantic SF Symbols (Fitness ECG), native Home NavigationStack; no AppKit raster host or card-flight route replacement. Older Manrope/Inter/Space Grotesk, amber estimates, giant KPIs, Iconoir adoption and embedded/Keychain sync-bearer provisioning are superseded.
- Calendar density is 40/64/120 pt per current DECISIONS, overriding tasks/design.md's 38/54/110. Timeline pinch owns density; header month expansion needs separately scoped gesture acceptance consistent with the user's request; no slider. Never let one pinch mutate both.
- Frozen RM-01 hero-morph wording conflicts with current route decisions; BF coaching wording cannot authorize generic AI. T0 must record a reviewed contract correction with provenance and refreeze through the validator, not implement obsolete behavior or silently mark it accepted. Preserve all unrelated leaves/aliases/thresholds.
- ACTIVE phases 1–9 remain scope: Finance/ledger, Tax/Documents, Business, Investments, Reminders Tasks/Grocery/Shopping, shell/settings/reports, Fitness, hardware. Its “minimal Fitness” wording does not cancel the full Bevel functional contract. Missing modules are work, not permission to hide them as completion.

## 2. Execution protocol and evidence commands
- This file governs future bounded dispatches; each implementation still needs
  its own reviewed diff, tests, commit, push, and acceptance evidence.
- One Luna Max implementer at a time; Astra Medium plans/reviews after bounded batches. Request these tiers only if actually available; record actual worker/tool identity and effort, never infer a model from its nickname. Long code reads return file:symbol summaries and uncertainties.
- Before every dispatch: pin base SHA, inspect dirty/index changes, expand the row's paths to an exact allowlist, refresh symbol line numbers with `rg -n`, assign matching tests, forbid edits outside scope. Rows below are sequential ownership transfers, never simultaneous shared-file ownership. New files are explicitly marked proposed.
- Worker returns diff, producing SHA/base, complexity argument, executed commands/counts, artifact paths and unverified conditions. Parent inspects diff and evidence; Astra reviews security/data contracts before visual batching. RED patches are quarantined and fixed/reviewed or reverted only within that worker's owned changes; never reset unrelated work.
- Native test template N: `xcodebuild -project ios/LifeOS.xcodeproj -scheme <scheme-from-scripts/native_lane_manifest.json> -destination '<recorded simulator UDID or platform=macOS>' -jobs 1 -parallel-testing-enabled NO -derivedDataPath /private/tmp/lifeos-<SHA>-<lane> -resultBundlePath /private/tmp/lifeos-<SHA>-<lane>.xcresult -only-testing:<target>/<suite> test CODE_SIGNING_ALLOWED=NO`. Resolve placeholders before execution; unsigned simulator tests are not device/signature evidence.
- Test template P: `python3 -B -m unittest discover -s scripts/tests -p '<assigned test_file.py>'`; gateway tests use its pinned environment and `python -m pytest services/gateway/test_gateway.py services/gateway/test_enablebanking.py`. API: `npm --workspace services/api run typecheck` then `npm --workspace services/api test`; contracts build first if required by imports. Never install unpinned dependencies merely to make tests pass.
- Final native lanes derive schemes/plans/counts from `scripts/native_lane_manifest.json`; use `scripts/validate_xcresult.py` and `scripts/validate_native_release.py` according to their CLI help. `run_prerelease_lanes.sh` currently lacks explicit `-jobs 1`: execution owner must correct/review that before using it. Zero tests, canceled jobs or damaged result archives do not pass.
- Evidence root: `artifacts/final/<tranche>/<run-id>/`; index exact source/build SHA, device/OS, viewport/theme/state, command/exit/test count, expected versus observed, sanitized receipt, inspected image/video and digest. Keep raw private captures locally protected; commit only privacy-reviewed evidence acceptable to registry contracts.
- After a reviewed tranche, execution owner alone updates HANDOFF/PHASE_STATUS/DECISIONS (<100 lines each), ACTIVE/todo (<200), commits scoped files and pushes, verifies PR/base/head parity with CLI. No merge while release gates remain open; do not mark code-only green as product acceptance.

## 3. Critical path and serialized Luna write sets
| ID / dependency | Exact owning paths and symbols (relative to repository unless noted) | Work and acceptance / rollback boundary |
|---|---|---|
| T0 / first | Coordination/{HANDOFF,PHASE_STATUS,DECISIONS}.md; tasks/{ACTIVE,todo,design}.md; docs/LIFEOS_ACCEPTANCE_REGISTRY.md; scripts/validate_acceptance_registry.py only if a reviewed validator defect exists | Reconcile checkpoint versus candidate, current decisions versus frozen obsolete claims, all ACTIVE phases and issue #2. Enumerate every materialized leaf with owner, source, tests, missing evidence and blocker; map absent-scope deliverables separately. Never lower thresholds or prune scope to improve score. Preserve original hash and audited amendment/refreeze history. |
| T1 / T0 | services/windows-service-host/deploy/Deployment.Common.ps1: Read-RecoveryJournal, Invoke-RecoveryStage, Restore-LifeOSServiceSnapshots, Reconcile-LifeOSServiceSnapshotState, Complete-LifeOSRecoveryState; deploy/{rollback,install}.ps1; deploy/tests/Deployment.{Behavior,Static}.Tests.ps1; scripts/tests/test_windows_deployment_source.py | Instrument bounded stage durations/scan counts first; reproduce 31,401-unit resume in disposable Windows state. Eliminate repeated validation only with descriptor-bound journal AND progress-sidecar identity/bytes/sequence and exact validated transaction/stages/snapshots. Post-barrier verification must NEVER create/delete/reconfigure/start/stop services. Changed identity/hash/ACL/ancestor or incomplete journal fails closed; fresh validation before any repair and final release. Retain safe `6baa1f3` recovery candidate until Astra GREEN and actual PS5.1 tests. |
| T2 / T1 GREEN | scripts/build_windows_release.sh; services/windows-service-host/deploy/{preflight,verify,verify-candidate,tailscale_snapshot}.ps1; services/windows-service-host/src/{WindowsAclProtector,ChildProcess,ChildSupervisor,ServiceHostConfig,HealthProbe}.cs plus service-host tests; protected remote deployment only | Execute recovery/cutover protocol below; verify real service SID/ACL/reparse/health/restart/restore. Do not mix deployment edits with banking implementation. Failure retains authority data and transaction receipts; rollback via verified manifest only. |
| T3 / T2 healthy | services/gateway/{enablebanking,main,test_enablebanking,test_gateway}.py; ios/Shared/{FinanceDomain,FinanceCoordinator,TailscaleSyncClient}.swift; ios/LifeOS/Settings.swift; ios/LifeOSTests/{BankConsent,SettingsConnectionLifecycle,FinanceCoordinator,TailscaleSyncClientSecurity}Tests.swift | Verify existing consent before reconnecting. Repair EnableBankingService and requestBankConsent/status only as evidence requires. Both personal accounts must reach gateway→Mac→iPhone with actual balances/transactions/currency/source/freshness; expiry/revoke/partial errors tested. No provider secrets on client. T3 owns Settings/client exclusively until review. |
| T4 / T3 contracts; live proof may await operator | ios/Shared/Finance{StatementImporter,ImportedTransaction,ImportedTransactionStore,BudgetDomain,BudgetStore,AllocationDomain,AllocationStore,TrackingPreferences,TrackingPreferencesStore,WealthProjection,WealthAllocation,TransactionCategory,Categorizer}.swift; ios/LifeOS/Modules/Finance/{FinanceView,FinanceAnalyticsView,FinanceImportView,FinanceChartModeViews}.swift; matching Finance*Tests.swift | RF-01–21/PC-04: account ledger, manual cash audit, search/filter/inspector, budget/cycle, allocation, categories, recurring/bills/goals, income/spend/cash-flow modes/ranges, net worth/holdings/travel Tools and context restoration. Real TR sample preview→confirm→reimport/correction must conserve cents and avoid duplicate balances. Restore pre-import backup on migration failure; retain originals. |
| T5 / T4 ledger | ios/Shared/TaxDocuments.swift; ios/LifeOS/TaxDocumentsView.swift; ios/LifeOSTests/{TaxDocument,DurableStorageSafety}Tests.swift; services/gateway/main.py document routes and test_gateway.py (exclusive handoff from T3) | Complete ACTIVE Tax archive/OCR/editable metadata/conflicts/transaction matching/Missing Documents. Windows encrypted original archive plus hash/readback receipt; sync privacy-safe metadata, never raw OCR pages. Atomic crash recovery, masked identifiers and CSV formula defenses. Staging deletion only after confirmed archive receipt, never delete original Files/iCloud source. |
| T6 / T4–T5 | Proposed ios/Shared/{BusinessLedgerProjection,InvestmentLedgerProjection}.swift and ios/LifeOS/Modules/{Business/BusinessView,Investments/InvestmentsView}.swift plus corresponding tests; no second mutable Finance ledger | Complete ACTIVE Business customers/suppliers/invoices/VAT/uploads/reports and Investments lots/dividends/interest/allocation/performance/gains over shared source records. Identify required ledger extensions in a separate T4 follow-up before writing projections. Replay/import/export totals and tax links must reconcile; unsupported provider capability stays explicit, no production pytr/PayPal. |
| T7 / T0; shell integration waits T11 | Proposed ios/Shared/{RemindersTaskRepository,ShoppingListDomain}.swift; ios/LifeOS/Modules/{Tasks/TasksView,Grocery/GroceryView,Shopping/ShoppingView}.swift and tests | Audit docs-repo modules first; Reminders is task authority, distinct Grocery/Shopping semantics. Permission/revoke/offline/edit/complete/relaunch and Calendar read-only projections; no competing task store. Needed entitlements/routes go through T10/T11 owners, never concurrent edits. |
| T8 / T3 transport, T10 device proof | ios/Shared/{HealthKitAdapter,HealthKitDomain,HealthKitReconciliation,HealthKitAnchorStore,FitnessTrainingDomain,FitnessTrainingStore,FitnessTrainingProjection,FitnessStrengthDomain,FitnessJournalStore,FitnessBiologyDomain,FitnessRetentionDomain,FitnessLifestyleLedger}.swift; ios/LifeOS/HealthKit*.swift, FitnessTrainingCoordinator.swift; Modules/Fitness/Fitness{View,TrainingView,TrainingSessionView,StrengthView,BiologyView,StressView,LifestyleView}.swift; matching tests | Complete every BF leaf: source-qualified recovery/load/sleep/stress/reserve/monitor/body/journal/activity/strength views and drilldowns. Local templates/exercises/sets/reps/load/rest/pause/PR/history/report; atomic finish once, relaunch timers, offline edits. Import only proven HealthKit fields; read denial ≠ zero. Link imported summary to local session only through reliable match/explicit selection; retain distinct provenance. |
| T9 / T8 stores, T2 gateway | ios/Shared/Nutrition*.swift, FoodPhoto*.swift, Supplement*.swift; Modules/Fitness/Fitness{NutritionView,SupplementsView}.swift; services/api/src/{nutrition-photo,open-food-facts}.ts and their tests; services/gateway/{main,supplement_catalog,test_supplement_catalog,test_gateway}.py after T5; matching native tests | Complete manual/barcode/recipe/recent/photo draft→confirmed meal, editable macros and failures; confirmed-only totals/widgets/HealthKit writes. Explicit external-photo consent, sanitized bounded images, correction lineage and NU-02 held-out benchmark. Supplements plans/inventory/refill/Taken/Snooze/Skip once-only, terminated/background/DST/locked/denied states; no medical advice. 12-month ≤10GB retention, 8GB warning/9GB structured-only, export/delete/encrypted restore. |
| T10 / early entitlement feasibility, final after T8–T9 | scripts/install_personal_device{.sh,_checks.py}; scripts/tests/test_personal_device_installer.py; ios/project.yml; ios/LifeOS/{Info.plist,LifeOS.entitlements}; ios/LifeOSWidget/LifeOSWidget.entitlements; ios/Shared/SigningStatus.swift; ios/LifeOS/Modules/Automation/LifeOSAppIntents.swift and related tests | Verify actual Personal Team capability availability before promising widgets/HealthKit. Sign/install/renew without bypassing Apple checks; Morning/USB Shortcut evidence below. Only this worker owns project/entitlements and adds explicit Planning exclusions from widgets later. Physical actions remain operator gates. |
| T11 / T0, then functional tranche handoffs | ios/Shared/{DesignTokens,Typography,LifeOSIcon,LifeOSMotionKit,LifeOSComponents,LifeOSResponsiveContainer,LifeOSInteractionKit,LifeOSChartKit}.swift; ios/LifeOSMac/LifeOSMacApp.swift; ios/LifeOS/{LifeOSApp,OverviewView,CodexView}.swift; ios/LifeOS/Usage/*.swift; Modules/ModuleNavigation.swift; design/route/Usage tests and Mac snapshots | Foundation first, shell second, then individually return module views to their owners for visual passes. One NavigationStack/route authority, bounded history, cancel retired callbacks, preserve selections/drafts. Home agenda, compact Usage/Clipper, functional navigation/inspector/command/keyboard/deep links/settings/report flows. Remove migrated dead code only after references/tests prove replacement. |
| T12 / T11 tokens | ios/Shared/{CalendarViews,CalendarLayout,CalendarDomain,CalendarCoordinator,CalendarPeerSync,CalendarStore,CalendarIconAsset}.swift; ios/LifeOS/CalendarView.swift; Calendar*Tests.swift | CalendarTimelineZoomSession/Transaction focal zoom 40–120/default64; 00:00–24:00 mobile scroll over blank/event/resize areas, one today marker, no gutter drift; paging/edit/zoom arbitration, DST/recurrence/undo. Preserve pairing/replay/bounded decode/outbox/tombstones; rerun unavailable-SF-Symbol reviewer gap. |
| T13 / T3,T8–T12 | ios/LifeOS/{WidgetSnapshotPublisher,LifeOSBackgroundRefresh}.swift; ios/Shared/{FutureWidgetSnapshot,CalendarWidgetDateGrid}.swift; ios/LifeOSWidget/{LifeOSWidget,CalendarWidget,NextEventWidget,UsageWidget,FutureModuleWidgets}.swift; widget tests | All WG-01–16 and WS leaves/families; versioned privacy-filtered atomic snapshots, coalesced reload, freshness/expiry/lock/redaction, each deep link correct. WidgetKit reload request is not immediate display guarantee. Actual dark/clear/tinted grey-wallpaper proof required. |
| T14 / T10 access, T12 route handoff | Proposed ios/Planning/{PlanningVaultBinding,PlanningDomain,PlanningCanvasCodec,PlanningMarkdownCodec,PlanningVaultStore,PlanningMutationJournal,PlanningConflictResolver,PlanningGraphProjection,PlanningSpatialIndex}.swift; Modules/Planning/{PlanningCanvasView,PlanningNodeInspector}.swift and tests; proposed services/gateway/planning.py, packages/contracts/src/planning.ts | Implement issue #2 in substeps: access→lossless codec→durable proposals/conflicts→Mac canvas→phone canvas→Windows mirror. Route edits deferred to T11/T12, project exclusions to T10, gateway registration to a separate exclusive main.py dispatch. Acceptance boundary below; preserve all conflicts/original notes on failed publication. |
| T15 / T2–T14 stable | services/api/src/{server,local-auth,ingest-secret,claude-ingest,codex-adapter,atomic-file,history,json-boundary}.ts and tests; scripts/usage/*.py and tests; services/gateway/main.py; packages/contracts/src; apps/dashboard/src/{main.tsx,styles.css} only if release-served; apps/dashboard/index.html; dependency locks/.github workflows in separately enumerated scope | Reconcile live Usage/Clipper collectors, freshness/retries/idempotency across Windows/Mac without prompt/file-content collection. Final Astra threat review of full diff and deployed boundary; fixes return to original domain owner. No added usage-limit scheduler or advisor. Web dashboard gets production-truth/CSP checks if served, never substitutes for native visual proof. |
| T16 / all above | Evidence artifacts, registry acceptance entries, release/human checklist and canonical docs only; code fixes return to owners | Run final matrix, reconcile every leaf AND ACTIVE/issue scope, operator walkthrough, exact signed candidate/deployed SHA parity, PR comments/CI/base review via gh. Commit/push reviewed evidence, then merge only after full gates; preserve prior release/candidate and data restore receipts. |

### Tranche test dispatch (N/P refer to the commands above)
| Tranche | Required focused command/suite and non-test witness |
|---|---|
| T0/T16 | Registry `--json` and final `--score`; P:test_validate_acceptance_registry.py; leaf-by-leaf source/evidence hash audit |
| T1/T2 | P:test_windows_deployment_source.py and test_windows_release_builder.py; Windows `powershell.exe -NoProfile -File <candidate>/deploy/tests/Deployment.Static.Tests.ps1` then Deployment.Behavior.Tests.ps1 and Deployment.LegacyServe.Tests.ps1; service-host tests from tests/LifeOS.ServiceHost.Tests.csproj; disposable crash/resume and live verified recovery receipts |
| T3/T4 | Gateway/API commands P above; N:BankConsentTests, SettingsConnectionLifecycleTests, FinanceCoordinatorTests, FinanceStatementImporterTests, FinanceTradeRepublicImportIntegrationTests, FinanceImportViewModelSyncTests and Finance budget/allocation/projection tests; live receipts separate |
| T5–T7 | N:TaxDocumentTests, DurableStorageSafetyTests plus new BusinessLedgerProjectionTests, InvestmentLedgerProjectionTests, RemindersTaskRepositoryTests, ShoppingListDomainTests; real import/export and permissions round trips |
| T8/T9 | N:HealthKitReconciliationTests, HealthKitProductionBridgeTests, HealthKitFitnessRepositoryTests, FitnessTrainingCoordinatorTests, FitnessTrainingStoreTests, NutritionMealStoreTests, FoodPhotoSanitizerTests, SupplementNotificationAdapterTests, SupplementStoreTests and matching domain tests; signed source sample matrix, held-out food benchmark, notification/restore receipts |
| T10 | P:test_personal_device_installer.py; `bash -n scripts/install_personal_device.sh`; N:SigningStatusTests, LifeOSAppIntentsTests, HealthKitCapabilityConfigurationTests; actual signed install/renewal and consent receipts |
| T11–T13 | N:LifeOSDesignSystemTests, LifeOSChartInteractionTests, CalendarLayoutTests, CalendarPeerSyncTests, CalendarConflictIntegrationTests, WidgetSnapshotPublisherTests; Mac/Widget snapshot targets from lane manifest; inspect recordings from full matrix |
| T14 | New PlanningCanvasCodecTests, PlanningMarkdownCodecTests, PlanningMutationJournalTests, PlanningConflictResolverTests, PlanningSpatialIndexTests; gateway planning auth tests; real selected-vault round trip and physical gestures |
| T15 | Full API/gateway suites, P source/security regressions, collector tests under scripts/usage/tests and scripts/clipper/tests; installed dependency audit plus scoped deployed unauthorized requests; reviewed findings linked to regression artifacts |

## 4. Windows operational protocol (T1–T3)
1. Read-only SSH first: verify process command lines/start times for rollback/install, SCM states, current marker/journal identity/phase/sequence, disk/memory and C:/D: BitLocker protection. Observe live handle before waiting; an active marker alone is not a running process. Do not launch duplicate recovery or kill an unknown process.
2. Canonical abandoned manifest: `D:\Hermes\lifeos-backups\install-20260912T001103Z-47cdd8bb\manifest.json`; marker `D:\Hermes\lifeos-backups\.lifeos-deployment-transaction.json`. Preserve journal/progress/backups/authority files. Never delete/edit marker to unlock install, manually label recovered, or reset dirty legacy checkout.
3. T1 regression corpus must exercise production readers (mock only OS boundaries): absent-service drift without deletion, forged context, changed/replaced/truncated journal/progress, same-length/hash mismatch, ACL/reparse/ancestor replacement, interrupted tails, retry/crash at each stage, changed service configuration, writer barrier and final validation. PS5.1 static AND behavioral suites on disposable Windows paths; measure full 31,401-unit scan counts/time/RSS before live retry. Astra rejects unsafe shortcut or tests that mock away trust checks.
4. Use verified `deploy/rollback.ps1 -ManifestPath <canonical manifest>` under correct operator/admin identity and transaction mutex; same candidate source must pass verifier. Poll the same PID and stage evidence; slow CPU-active validation is not terminal. If unsafe resource pressure or corruption occurs, stop only owned process safely and preserve recovery-required state; investigate before another retry.
5. Only recorded successful recovery/postconditions permit new install. Build fresh candidate using `bash scripts/build_windows_release.sh --node-source <verified-node.exe> --wheelhouse <pinned-wheelhouse> --output-dir <unique-dir>`; compare SOURCE_SHA/zip SHA/manifest after transfer. Run verify-candidate, preflight (no mutation), then install with same protected input paths. Keep legacy task/backups until healthy cutover/rollback proof; no token values in arguments/output.
6. Validate API auto, gateway delayed-auto, virtual service identities, correct absolute binaries, local listeners only 127.0.0.1:8787/8421; Tailscale Serve exact approved HTTPS host/8420 and allowlisted identity; snapshot task SYSTEM, fresh identity snapshot unwritable by service SIDs. Check icacls/effective unauthorized access, reparse refusal, DPAPI service access, BitLocker and recovery-key custody without printing keys. Run deploy/verify.ps1 and restart/reboot recovery with sanitized receipts.
7. Banking: inspect existing linked sessions before requesting fresh consent; use protected paths for app ID/key/cert, registered callback and exact ASPSP mapping. Verify real account count/currency/balance/transaction sample against user account on gateway, Mac, iPhone; preserve observedAt/partial states. Test invalid callback state/replay/expired session/revoke using controlled cases, never revoke user's live consent just for unattended testing. Needs geonq if consent expired.

## 5. Physical device, workouts, Shortcuts and Canvas boundaries
- Georg must unlock/trust USB iPhone, enable Developer Mode when requested, authenticate Personal Team if needed, authorize HealthKit/Reminders/notifications and select the intended non-Uni vault. Record actual model/OS/team capability/provision expiry; simulator or unit tests cannot supply these receipts.
- T10: inspect signed app AND extensions, entitlements/App Group consistency and provisioning expiration; install via reviewed installer, launch, persist real record, widget readback, relaunch and renew preserving app data. Free signing is platform-managed approximately seven days, not seven weeks; do not promise silent self-renewal. Verify current Apple entitlement support with official documentation/toolchain before any paid-license decision; unsupported capability is a blocker to discuss, not a reason to silently drop widgets.
- Mac “LifeOS USB Refresh”: Shortcut invokes reviewed installer with explicit device/team paths, validates result and displays actual installed build/expiry. Missing/locked device, failed signing/install, wrong group or canceled auth cannot yield success. Reinstall/renew once now and record actual near-expiry/expiry test when possible; no invented future seven-day result.
- iPhone “LifeOS Morning Sync”: open Zepp, invoke official action only if present, otherwise instruct visible Sync tap; then LifeOS refresh/status. Launching Zepp is not proof of sync; denied/no samples remains honest. AppIntents morning/reauthentication reports must reflect actual steps, not pretend to install or sign.
- Zepp issue #2 device matrix: exact sensor/firmware/Zepp/iOS; strength, outdoor GPS, pauses/intervals, associated HR/gaps, duplicate sources, edit/delete/replay, lock/offline/revoke, DST/backfill. Compare same workout Zepp→Health→LifeOS; preserve UUID/sync version/reported duration separately from elapsed time and energy without double counting. Sets/reps/load are LifeOS-entered; routes/distance/laps/zones only after adapter and source proof; proprietary readiness/load/Training Effect parity is unverified.
- Canvas requires operator decisions still open in issue #2: selected vault, rectangular stock-Obsidian fallback versus maintained plugin, phone publication when Mac returns, Windows mirror/proposals versus canonical editing. Implement proposed safe model behind explicit setup; do not label these choices user-approved or promise identical shapes everywhere.
- Proposed vault `LifeOS/Projects/<uuid>/{Project.md,Map.canvas,Notes/,Assets/,_lifeos/}`: Markdown/YAML identity and semantic links, standard Canvas spatial layout, sidecar custom shapes/bends, rebuildable private index. Mac canonical writer; phone durable proposals; Windows protected mirror/proposals outside iCloud. Unselected notes read-only, no transitive vault export.
- Canvas bounds: note 1MiB, canvas 5MiB, 1,000 nodes/3,000 edges, finite coordinates, safe relative paths, collision/reparse rejection. Preserve unknown valid fields or switch read-only; exact-byte base hashes, coordinated reread, journaled same-directory atomic replace, manifest last, crash/replay/conflict tests. “Saved to vault” only after readback; queued and conflicted distinct.
- Canvas runtime: separate Calendar Projects viewport; Mac pan/pinch/space-drag/marquee/undo/Escape, phone background pan/pinch and explicit node drag, inspector note editing, colors/shapes/edge reconnect/arrows/labels, explicit Obsidian/external URL action. O(V+E) index/commit; spatial query O(log V+k), node move updates incident edges only; no file rewrite per pointer frame. Prove Obsidian→LifeOS→Obsidian metadata/text/layout preservation and visible shape fallback, offline proposals and no unrelated note leakage on physical devices.

## 6. Visual contract and exact evidence matrix
- Use tasks/design.md roles: Mac title 22, body/control 13, metadata12, primary value28; phone24/15/12/30; SF Pro/system only, monospaced digits, compact 12pt-radius cards, one title/source notice. Follow readable geon/colors.md main #0253C4/action, #5DA0FD observed, green dashed estimate, neutral dotted target, orange calories, teal protein; adjacent meanings never rely on similar blues.
- Preserve current 40/64/120 calendar and native stack decisions. Resting surfaces restrained, no halos/empty giant scaffolds; one Save/Cancel footer, left-aligned fields/units, inline failures, no developer-storage prose. Direct gestures track input, hover120ms/press80ms, selectors .24/.90; native phone push/pop and cancellable Mac transitions, no entrance replay on refresh.
- References are quality/feature inputs: Linear/Vercel, supplied Figma/Revolut/Bevel and tasks/design.md's SkillsUI Clean/Forever Infinite summaries. No rendered external animation review occurred here; do not claim otherwise. Any further reference inspection belongs to visual execution, not an excuse to reset implemented data logic.
| Surface / evidence artifact | Matrix and actions | Pass criteria |
|---|---|---|
| All release destinations: Home, Usage, Clipper, Calendar, Finance+Tools/import, Fitness all subpages, Nutrition/photo/manual, workouts, supplements, Tax, Business, Investments, Tasks/Grocery/Shopping, Projects, Settings | PNGs per screen: Mac 900/1200/1512/1800×982 plus design 800×600 and 1024×768; actual iPhone17 portrait+landscape and compact390×844; light/dark, default/largest text; keyboard-open forms | One hierarchy, no clipping/horizontal overflow/duplicated notices, edges within1pt, readable status/units, all actions usable. Inspect exact built binary, record viewport not just window outer size. |
| Every applicable data surface | Live/observed-zero plus deterministic LABELED layout fixtures: loading/never-connected/stale/partial/error/pending/denied/revoked/locked/redacted; long80-char names and €1,234,567.89 | Production fixtures unreachable; real zero distinct from missing; failed save retains draft; source/freshness accurate. Fixture images accept layout only. |
| Home→Usage/Clipper, sidebar, deep links, sheets | Videos:10 forward/back cycles; reversal60/120ms; refresh/keyboard/resize/background mid-transition; Reduced Motion; hover/focus/pressed/disabled | One interactive destination, no teleport/replay/stale callback/lost draft; saved context restored; native interactive phone back works. |
| Calendar | Videos scroll00–24 over blank/events/handles, reach23:45+24 label; slow/fast/diagonal day paging; focal pinch09:15/23:45 at40/64/120, cancel/edit/auto-scroll/DST; header expansion separately | Today-only now marker; gutter drift≤1pt, focal drift≤2pt except bounds; scroll/pinch/edit do not steal each other; no slider. |
| All supported widget kinds/families | WidgetKit captures + physical Home/Lock checks full-color/dark/clear/tinted, wallpaper#606060/#808080/#A0A0A0/white, live/stale/empty/locked/redacted, deep link/reload | Text contrast≥4.5:1, essential graphics≥3:1 after compositing, no leaked private values or fake refresh guarantee; seven Bevel aliases accounted once. |
| Performance / source correctness | Exact QA-04A/B/C manifest thresholds: physical Mac+iPhone,3×30s scroll/drag/scrub/tab at frozen widths/themes/Reduce Motion; launch traces; storage benchmark/crash corpus | p95 frame≤16.7ms, none>50ms, input-visible p95≤100ms, zero lost input; evaluate every other frozen launch/storage threshold verbatim. No simulator-only physical claim or unjustified universal O(n) claim. |

## 7. Astra batches, threat checks and release acceptance
- Batch A T0–T2: transaction identity, descriptor lifetime, service non-mutation, crash recovery, actual Windows receipts. Batch B T3–T7: bank/data authority/cents/import/privacy and module completeness. Batch C T8–T10: health/workout/nutrition/supplement/installer threat and physical boundaries. Batch D T11–T14: rendered visuals/gestures/Canvas conflicts/widgets. Batch E T15–T16: final integrated cybersecurity and product review. Reopen affected batches after fixes; GREEN requires evidence, not worker confidence.
- Threat corpus on disposable data/owned endpoints: unauthenticated peer/invitation, forged names/identity headers, replay/future timestamps/deletions/duplicate IDs/bad Codable; body/response/content-type/redirect/time bounds; local Host/DNS-rebinding and CORS; tampered icons before decoder; credential legacy cleanup/DPAPI/ACL; raw tax text leakage, CSV prefixes/regex long input, atomic-write crash/symlink races; history concurrent ingestion/corrupt lines; absolute Codex executable/ComSpec/PATH injection; constant-time secret verification; dependencies/lockfile advisories, pinned CI actions, fork-secret isolation and served-dashboard CSP.
- Planning additions: path traversal/case collisions/reparse, unauthorized project enumeration, remote previews/scripts, conflicting offline writers, stolen mutation receipt/replay and oversized maps. Run non-destructive negative tests on live auth boundaries; destructive corruption/penetration only in isolated copies. Sanitize all reports; never expose secrets or personal bank/health/note payloads on GitHub.
- Frozen process: maintain 258-leaf/7-alias denominator unless an explicit audited scope correction requires refreeze; materialize every leaf's exact evidence types/threshold/claim-kind. Evidence commit E and tracked repo:// artifact SHA-256 must bind source and substantive observations; no remote URLs, future/self-referential SHA, empty receipt or source-only live claim.
- Run validator normally after every evidence tranche; final `python3 -B scripts/validate_acceptance_registry.py --score`. Registry minimum (all P0,≥95%,each workstream≥90%,operator visual approval) is only a checkpoint: requested FULL completion also requires remaining explicit ACTIVE/user/issue requirements resolved and verified, not an unexplained 5% omission.
- Release requires signed Mac/iPhone app+widgets, reproducible passing nonzero test lanes, operational Windows restart/backup/restore, real account and health provenance, all visual/runtime matrices, final Astra security review without unresolved release-blocking findings, geonq's physical/product acceptance and accurate human instructions. PR review comments resolved, CI and branch parity via gh; only then merge and publish final handoff.

## 8. Stop conditions and current-state matrix
- Stop affected tranche immediately for fabricated/live-demo fallback, unsupported provider/sensor/device/signing claims, unsafe/unreviewed worker patch, changed authority without receipt, stale/conflicting handoff, or loss of base-SHA ownership. Continue independent verified work; never fabricate acceptance to unblock.
- Before native work check owned agents/xcodebuild/simulator processes and macOS memory_pressure/vm_stat. One native build/test and one required simulator; no overlapping DerivedData writers. Stop new work at warning/critical memory pressure or sustained swap growth; retire owned idle processes, record peak RSS and retry only after recovery. Never kill unrelated user processes.
- Windows timing alone is not a failure signal: inspect same live PID/CPU/stage; do not restart due polling timeout. Unexpected ACL/identity drift or memory danger stops mutation and retains marker/journal; no automatic destructive cleanup. No scheduling/Claude-limit watcher.
| Area | Current verified or recorded status | Evidence required next |
|---|---|---|
| Git/PR | Observed HEAD/origin/PR `056c1b4`, draft OPEN/MERGEABLE | Recheck current SHA before every worker; canonical docs now distinguish candidate and current head |
| Registry | Observed valid FROZEN,0/258 accepted; score failure | Audited conflict reconciliation and per-leaf immutable acceptance evidence |
| Windows | Recorded `6baa1f3` candidate checks; active recovery marker/service stopped | Fresh SSH process/SCM/journal observation, measured reviewed recovery, cutover/live health |
| Finance/device | Source foundations and historical scoped tests; live/physical unproven here | Existing consent readback; USB/team/HealthKit/widget/renewal receipts from Georg |
| UI/workouts/Canvas/security | Scoped prior UI/source evidence; issue#2 OPEN; full acceptance absent | Matrix captures, fidelity comparisons, Canvas decisions+round trips, final security receipts |

**First next action:** pin `git status`/HEAD/PR again, then give T1 Luna the
sanitized recovery facts and the bounded performance/identity test contract.
Do not launch install while the marker is active. Run T10 entitlement
feasibility early when the physical phone is available so a platform
constraint cannot surface only at release.
